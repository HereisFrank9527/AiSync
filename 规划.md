# AI 小说创作平台 — 项目规划

## 零、关键设计决策（已确认）

| 决策点 | 选择 | 技术影响 |
|--------|------|---------|
| **部署形态** | 桌面应用 | Tauri（Rust 内核，体积小）+ Python 后端 sidecar |
| **多模型支持** | 支持自定义 API | LLM Client 适配器模式（Anthropic / OpenAI / 自定义兼容端点） |
| **协作需求** | 单人使用 | 权限系统大幅简化，无需多角色设计 |
| **版本控制** | 暂不考虑 | 去掉 .history/ 复杂设计，保留简单操作日志 |
| **离线能力** | 暂不考虑 | 不打包本地模型，始终依赖在线 API |

---

## 一、核心设计理念

```
用户 <──> 前端UI(编辑器+可视化) <──> 主Agent <──> 工具系统 <──> AI模型(LLM)
                 |                        |
            WebSocket               asyncio.Lock
            (流式+事件)              (并发安全)
                 |                        |
            Tauri 桌面壳            项目文件系统 + 向量数据库
```

### 核心通信机制：双通道 + 事件总线

```
┌──────────────┐   WebSocket (流式)   ┌──────────────┐
│   前端 UI     │◄────────────────────►│   Python 后端  │
│              │   REST (CRUD)        │   FastAPI     │
│   编辑器      │◄────────────────────►│              │
│   可视化组件   │                      │   Agent 循环  │
│              │                      │   Tools      │
└──────────────┘                      └──────────────┘
```

1. **Agent → 前端（被动展示）**：工具调用结果通过 WebSocket 实时推送到前端，前端根据 `ui_hint` 渲染对应组件，不阻塞 Agent 循环。

2. **前端 → Agent（主动干预）**：用户在可视化组件上的操作，通过 WebSocket 发回后端，作为伪用户消息插入 Agent 消息队列，Agent 下一轮推理自然感知。

3. **并发安全**：Agent tool_use 循环和前端操作共享 `ProjectContext`，使用 `asyncio.Lock` 保护文件读写。

---

## 二、技术栈

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **桌面壳** | Tauri (Rust) | 体积小（~10MB），启动快，比 Electron 轻量得多 |
| **前端** | React + TypeScript | 组件化，生态丰富 |
| **后端** | Python (FastAPI) | AI 生态最强，作为 Tauri sidecar 进程运行 |
| **AI 调用** | 适配器模式 | 统一接口，支持 Anthropic / OpenAI / 自定义 OpenAI 兼容端点 |
| **向量数据库** | ChromaDB（本地嵌入） | 轻量，无需额外服务 |
| **项目存储** | 文件系统 (md + yaml + json) | 直观可读，可后续接入 Git |
| **通信** | WebSocket + REST | 流式输出 + 常规 CRUD |

---

## 三、模块化架构

### 3.1 工具系统（Plugin Architecture）

采用**注册制 + 基类继承**。插入新工具只需继承基类、放入 `tools/` 目录，自动注册。

```python
# ===== 工具基类 =====
class BaseTool(ABC):
    name: str
    description: str
    required_permissions: list = []  # 单人使用，初期不启用校验

    @abstractmethod
    def schema(self) -> dict:
        """返回工具参数的 JSON Schema，供 LLM 调用"""
        pass

    @abstractmethod
    async def execute(self, params: dict, context: ProjectContext) -> ToolResult:
        """执行工具逻辑"""
        pass


# ===== 工具返回结果 =====
class ToolResult:
    content: str              # 纯文本结果（供 Agent 阅读）
    ui_hint: Optional[dict]   # 前端渲染指令（可选，工具不感知前端）
    # ui_hint 示例：
    #   {"type": "graph", "data": {...}}         → 关系图组件渲染
    #   {"type": "timeline", "data": {...}}      → 时间线组件渲染
    #   {"type": "stream", "channel": "editor"}  → 流式推送到编辑器
    #   None                                     → 纯文本，前端无需特殊渲染


# ===== 工具注册器 =====
class ToolRegistry:
    _tools: dict[str, BaseTool] = {}

    def auto_discover(self, tools_dir: str):
        """扫描目录，自动加载所有 BaseTool 子类"""

    def register(self, tool: BaseTool):

    def get_tool(self, name: str) -> BaseTool:

    def get_all_schemas(self) -> list[dict]:
        """返回所有工具的 schema，注入 LLM system prompt"""
```

### 3.2 主Agent 系统

```python
class MasterAgent:
    """主Agent — 编排所有工具"""

    def __init__(self, llm_client, tool_registry, project):
        self.llm = llm_client
        self.tools = tool_registry
        self.project = project
        self.memory = ConversationMemory()
        self.vector_store = project.vector_store
        self._interrupted = False       # 用户中断信号

    async def run(self, user_input: str):
        """主循环：理解意图 → 检索上下文 → 调用工具 → 返回结果"""

        # 1. 从向量库检索相关上下文
        relevant_context = await self.vector_store.query(user_input)

        # 2. 构建 prompt（含工具 schemas + 项目上下文 + 模型配置）
        messages = self._build_messages(user_input, relevant_context)

        # 3. LLM 推理（tool_use 循环）
        while True:
            if self._interrupted:
                self._interrupted = False
                return "操作已中断，等待新指令。"

            response = await self.llm.chat(
                messages=messages,
                tools=self.tools.get_all_schemas()
            )

            if response.has_tool_calls:
                for call in response.tool_calls:
                    tool = self.tools.get_tool(call.name)
                    result = await tool.execute(call.params, self.project)
                    messages.append(tool_result(result))
                    # 通过 WebSocket 推送结果到前端
                    await self._push_to_frontend(result)
            else:
                return response.content

    def interrupt(self):
        """前端调用，设置中断信号"""
        self._interrupted = True

    async def _push_to_frontend(self, result: ToolResult):
        """通过 WebSocket 推送工具结果到前端"""
```


### 3.3 项目上下文（并发安全）

```python
class ProjectContext:
    _lock: asyncio.Lock

    async def read_file(self, path: str) -> str:
        async with self._lock:
            ...

    async def write_file(self, path: str, content: str):
        async with self._lock:
            ...
```

---

## 四、项目文件存储结构

```
my-novel-project/
├── project.yaml                    # 项目元数据 + 模型配置
├── world/                          # 世界观（直观阅读 → Markdown）
│   ├── overview.md
│   ├── magic-system.md
│   ├── geography.md
│   ├── history.md
│   └── rules.yaml                  # 世界规则（结构化）
│
├── characters/                     # 角色
│   ├── index.yaml                  # 角色索引
│   ├── protagonist/
│   │   ├── profile.md
│   │   └── profile.yaml
│   └── relationships.json          # 人物关系图（前端渲染）
│
├── plot/                           # 剧情
│   ├── outline.md                  # 大纲
│   ├── arcs/                       # 故事弧
│   │   ├── arc-01-起源.yaml
│   │   └── arc-02-冲突.yaml
│   └── timeline.json               # 时间线（前端可视化）
│
├── chapters/                       # 章节正文
│   ├── vol-01/
│   │   ├── ch-001.md
│   │   ├── ch-002.md
│   │   └── ch-meta.yaml            # 章节元数据（状态、字数、摘要）
│   └── vol-02/
│
├── assets/                         # 辅助资源
│   ├── name-dictionary.yaml
│   └── style-guide.md
│
├── .vectordb/                      # 向量数据库（本地）
│   └── chroma/
│
└── operation.log                   # 简单操作日志（代替 .history/）
```

### project.yaml 补充内容

```yaml
name: "我的小说"
model:
  provider: "anthropic"        # anthropic | openai | custom
  api_base: "https://api.deepseek.com/anthropic"
  api_key_env: "DEEPSEEK_KEY"
  model_name: "deepseek-v4-pro"
  parameters:
    temperature: 0.7
    max_tokens: 8192
```

### 三种格式的分工原则

| 格式 | 适用场景 | 示例 |
|------|---------|------|
| **Markdown** | 需要直接阅读/编辑的长文本 | 章节正文、世界观描述、角色传记 |
| **YAML** | 半结构化、人可读可编辑 | 大纲节点、角色属性、项目配置 |
| **JSON** | 复杂结构、前端渲染 | 人物关系图、时间线、统计数据 |

---

## 五、向量化方案

```python
class ProjectVectorStore:
    """项目级向量存储，用于语义检索"""

    COLLECTIONS = {
        "chapters":    {"chunk_size": 500, "overlap": 50},
        "characters":  {"chunk_size": 300},
        "world":       {"chunk_size": 400},
        "plot":        {"chunk_size": 200},
    }

    async def index_file(self, file_path: str):
        """文件变更时自动重新索引"""

    async def query(self, text: str, collections: list = None, top_k: int = 10):
        """语义检索：写作时自动拉取相关设定，保证前后一致"""

    async def check_consistency(self, new_content: str):
        """一致性检查：新写内容是否与已有设定矛盾"""
```

### 向量化的核心价值

- 写第 50 章时，自动检索前面提到的相关伏笔
- 创建新角色时，自动对比已有角色避免雷同
- 世界观一致性校验（新设定 vs 已有设定）

---

## 六、核心工具清单（首批）

### 工具双模式说明

每个工具返回 `ToolResult`，其中可选的 `ui_hint` 字段让前端决定如何渲染：

| 工具 | 纯 Agent 模式 | 前端模式 |
|------|:---:|:---:|
| `write_chapter` | 流式输出文本 | 编辑器实时显示 + token 级流式 |
| `relationship_graph` | 文本描述关系 | 可视化关系图（可拖拽编辑） |
| `timeline_manage` | 文本列表 | 时间线拖拽组件 |
| `plot_outline` | 树形文本 | 大纲树编辑器 |
| `create_character` | 文本描述 | 表单 + 角色卡片预览 |

### 工具列表

| 工具名 | 功能 | ui_hint | 优先级 |
|--------|------|---------|--------|
| `write_chapter` | 撰写/续写章节 | `stream:editor` | P0 |
| `edit_chapter` | 修改已有章节 | `stream:editor` | P0 |
| `create_character` | 创建角色档案 | `card:character` | P0 |
| `update_worldview` | 更新世界观设定 | — | P0 |
| `search_project` | 向量语义搜索 | `list:search_results` | P0 |
| `consistency_check` | 一致性校验 | `list:issues` | P1 |
| `plot_outline` | 生成/调整大纲 | `tree:outline` | P1 |
| `relationship_graph` | 管理人物关系 | `graph:relations` | P1 |
| `style_transfer` | 文风调整 | — | P2 |
| `timeline_manage` | 时间线管理 | `timeline:events` | P2 |
| `export_novel` | 导出 epub/pdf | — | P2 |
| `chapter_summary` | 自动生成摘要 | — | P2 |

---

## 七、开发阶段规划

### Phase 1 — 基座搭建（2-3 周）

- [x] 工具基类 & 注册系统（含 `ui_hint`）
- [x] 主 Agent 循环（含中断机制）
- [x] LLM Client 适配器（Anthropic / OpenAI / 自定义）
- [x] 项目文件系统（CRUD + asyncio.Lock）
- [x] FastAPI + WebSocket 通信层
- [x] Tauri 壳 + 基础前端框架
- [x] LLM 运行时配置 API（GET/PUT /api/config/llm）
- [x] 前端 LLM 设置面板（SettingsPanel 组件）
- [x] CSS 变量设计系统 + 前端目录结构重构
- [x] Sidebar 组件（项目选择、视图切换、连接状态）
- [x] ChatPanel 组件（事件流展示、消息输入、中断控制）
- [x] SettingsPanel 迁移 + App 重构（三视图布局）
- [x] 后端预设系统（Preset CRUD API + 内置/自定义预设管理）
- [x] 前端预设集成（usePresets hook + SettingsPanel 完整表单 + WebSocket preset_id 同步）

### Phase 1.5 — 验收修复（进行中，进入 Phase 2 前必须完成）

> Phase 1 验收发现的问题，按优先级排列。截至 2026-05-01，WebSocket 健壮性部分已修复。

**高优先级**

- [x] WebSocket 健壮性：useAgentSocket.ts 已支持自动重连（指数退避）、`onerror`、`JSON.parse` try/catch；后端 WebSocket 异常会返回 error 事件而不是崩溃断连
- [x] 事件数组内存泄漏：useAgentSocket events 保留最近 200 条
- [x] 预设更新后 Agent 缓存未失效：get_agent cache key 已加入 preset.updated_at，避免保存 provider/base/model/API Key 后仍复用旧 LLM client
- [ ] 预设加载/错误状态未展示：usePresets 返回 loading/error 但 App.tsx 未消费
- [ ] 错误消息颜色：SettingsPanel.tsx:274 `.settings-msg` 未按错误/成功区分，CSS 有 `.error` 类但 JSX 不用
- [ ] 颜色对比度：`--color-text-tertiary` (#b4b4b0) 对比度 ~2.1:1，需 ≥ 4.5:1

**中优先级**

- [ ] 删除预设无确认对话框
- [ ] 无 React Error Boundary
- [ ] Agent 循环无迭代上限（agent.py while True 无 max_iterations）
- [ ] `presets.json` 使用相对路径 `Path("presets.json")`，启动目录不同会错位
- [ ] `PresetStore._load` 的 `except (json.JSONDecodeError, Exception): pass` 静默吞异常
- [ ] 项目 ID 输入无防抖/校验，每次按键触发 WebSocket 重建

**低优先级**

- [x] OpenAI 客户端可直接使用应用内保存的 API Key；未填写时才回退到环境变量，并返回明确错误
- [x] Anthropic 流式响应收尾协程未 await：已修复 `stream.get_final_message()` 导致的 coroutine/content 错误
- [ ] 向量存储为空实现（NullVectorStore / ProjectVectorStore），Phase 3 再补
- [ ] 无暗色模式 / 响应式布局
- [ ] 聊天消息无角色区分（用户 vs Agent 样式相同，无 sender 字段）
- [ ] 设置保存成功提示不自动消失
- [ ] `vite`/`@vitejs/plugin-react` 在 dependencies 而非 devDependencies
- [ ] 少量硬编码像素值未使用 CSS token（style.css:113, SettingsPanel.css:91）

### Phase 2 — 核心写作（2-3 周，未开始）

- [ ] 工具 UI 框架（前端 ToolDrawer + SchemaForm + 后端 GET/POST /api/tools）
- [ ] write_chapter（有基础版）/ edit_chapter（待开发，含流式输出）
- [ ] 角色 & 世界观管理工具（create_character, update_worldview 待开发）
- [ ] Markdown 编辑器集成
- [ ] 对话式交互界面（ChatPanel 基础版已完成，待增强角色区分等）

### Phase 3 — 智能增强（2-3 周）

- [ ] 向量化引擎集成
- [ ] 语义搜索 & 上下文自动注入
- [ ] 一致性校验
- [ ] 人物关系图可视化（前端从 `ui_hint` 渲染）

### Phase 4 — 体验打磨（2 周）

- [ ] 大纲可视化编辑
- [ ] 时间线工具
- [ ] 导出功能
- [ ] 文风分析 & 调整
