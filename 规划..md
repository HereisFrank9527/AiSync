现在正在进行：工作流编写

# AI 小说创作平台 — 项目规划

## 零、关键设计决策（已确认）

| 决策点 | 选择 | 技术影响 |
|--------|------|---------|
| **部署形态** | 桌面应用 | Tauri（Rust 内核，体积小）+ 后端源码资源 + 内置 Python runtime |
| **多模型支持** | 支持自定义 API | LLM Client 适配器模式（Anthropic / OpenAI / 自定义兼容端点） |
| **协作需求** | 单人使用 | 权限系统大幅简化，无需多角色设计 |
| **版本控制** | 暂不考虑 | 去掉 .history/ 复杂设计，保留简单操作日志 |
| **离线能力** | 部分考虑 | 不打包本地模型，LLM 仍依赖在线 API；桌面运行时应逐步做到无系统 Python 也能启动 |

---

## 一、核心设计理念

```
用户 <──> 前端UI(编辑器+可视化) <──> 主Agent <──> 工具系统 <──> AI模型(LLM)
                 |                        |
            WebSocket               asyncio.Lock
            (流式+事件)              (并发安全)
                 |                        |
            Tauri 桌面壳            项目文件系统 + 向量数据库
```

### 核心通信机制：双通道 + 事件总线

```
┌──────────────┐   WebSocket (流式)   ┌──────────────┐
│   前端 UI     │◄────────────────────►│   Python 后端  │
│              │   REST (CRUD)        │   FastAPI     │
│   编辑器      │◄────────────────────►│              │
│   可视化组件   │                      │   Agent 循环  │
│              │                      │   Tools      │
└──────────────┘                      └──────────────┘
```

1. **Agent → 前端（被动展示）**：工具调用结果通过 WebSocket 实时推送到前端，前端根据 `ui_hint` 渲染对应组件，不阻塞 Agent 循环。

2. **前端 → Agent（主动干预）**：用户在可视化组件上的操作，通过 WebSocket 发回后端，作为伪用户消息插入 Agent 消息队列，Agent 下一轮推理自然感知。

3. **并发安全**：Agent tool_use 循环和前端操作共享 `ProjectContext`，使用 `asyncio.Lock` 保护文件读写。

---

## 二、技术栈

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **桌面壳** | Tauri (Rust) | 体积小（~10MB），启动快，比 Electron 轻量得多 |
| **前端** | React + TypeScript | 组件化，生态丰富 |
| **后端** | Python (FastAPI) | AI 生态最强，安装版优先用随包 Python runtime 启动，自动 venv 作为回退 |
| **AI 调用** | 适配器模式 | 统一接口，支持 Anthropic / OpenAI / 自定义 OpenAI 兼容端点 |
| **向量数据库** | ChromaDB（本地嵌入） | 轻量，无需额外服务 |
| **项目存储** | 文件系统 (md + yaml + json) | 直观可读，可后续接入 Git |
| **通信** | WebSocket + REST | 流式输出 + 常规 CRUD |

---

## 三、模块化架构

### 3.1 工具系统（Tool Descriptor + Tool Run）

采用**注册制 + 基类继承 + 描述符协议**。插入新工具只需继承基类、放入 `tools/` 目录，自动注册；同一个工具既可被主 Agent 的 tool calling 调用，也可在前端工具中心以表单方式手动执行。

```python
# ===== 工具基类 =====
class BaseTool(ABC):
    name: str
    description: str
    has_frontend_ui: bool = True
    required_permissions: list = []
    default_preset_id: str | None = None  # 设置里的默认 LLM 方案；default_agent 仅保留旧兼容

    @abstractmethod
    def schema(self) -> dict:
        """返回工具参数的 JSON Schema，供 LLM 和前端 SchemaForm 共用"""
        pass

    @abstractmethod
    async def execute(self, params: dict, context: ProjectContext) -> ToolResult:
        """执行工具逻辑，读写项目文件并返回结果"""
        pass

    def file_access(self) -> ToolFileAccess:
        """声明可能读取、修改、生成的文件模式，执行前展示给用户"""

    def presentation(self) -> ToolPresentation | None:
        """声明前端结果呈现类型，如 stream:editor / card:character"""

    def frontend_descriptor(self) -> dict:
        """返回工具中心需要的完整描述符"""


# ===== 工具返回结果 =====
class ToolResult:
    content: str              # 纯文本结果（供 Agent 阅读）
    ui_hint: Optional[dict]   # 前端渲染指令
    metadata: dict            # 工具补充元数据


# ===== 工具运行记录 =====
class ToolRunRecord:
    run_id: str
    tool_name: str
    mode: "execute" | "invoke"
    status: "completed" | "failed"
    started_at: str
    finished_at: str
    file_access: dict
    params: dict
    result: ToolResult | None
    error: str | None
```

当前执行 API 先采用同步执行 + 持久化记录：每次 `execute` 或 `invoke` 都写入 `.aisync/tool_runs/{run_id}.json`。后续如需长任务，可把同一记录结构扩展为 `queued/running/cancelled`。

### 3.2 主Agent 系统

```python
class MasterAgent:
    """主Agent — 编排所有工具"""

    def __init__(self, llm_client, tool_registry, project):
        self.llm = llm_client
        self.tools = tool_registry
        self.project = project
        self.memory = ConversationMemory()
        self.vector_store = project.vector_store
        self._interrupted = False       # 用户中断信号

    async def run(self, user_input: str):
        """主循环：理解意图 → 检索上下文 → 调用工具 → 返回结果"""

        # 1. 从向量库检索相关上下文
        relevant_context = await self.vector_store.query(user_input)

        # 2. 构建 prompt（含工具 schemas + 项目上下文 + 模型配置）
        messages = self._build_messages(user_input, relevant_context)

        # 3. LLM 推理（tool_use 循环）
        while True:
            if self._interrupted:
                self._interrupted = False
                return "操作已中断，等待新指令。"

            response = await self.llm.chat(
                messages=messages,
                tools=self.tools.get_all_schemas()
            )

            if response.has_tool_calls:
                for call in response.tool_calls:
                    tool = self.tools.get_tool(call.name)
                    result = await tool.execute(call.params, self.project)
                    messages.append(tool_result(result))
                    # 通过 WebSocket 推送结果到前端
                    await self._push_to_frontend(result)
            else:
                return response.content

    def interrupt(self):
        """前端调用，设置中断信号"""
        self._interrupted = True

    async def _push_to_frontend(self, result: ToolResult):
        """通过 WebSocket 推送工具结果到前端"""
```


### 3.3 项目上下文（并发安全）

```python
class ProjectContext:
    _lock: asyncio.Lock

    async def read_file(self, path: str) -> str:
        async with self._lock:
            ...

    async def write_file(self, path: str, content: str):
        async with self._lock:
            ...
```

---

## 四、项目文件存储结构

```
my-novel-project/
├── project.yaml                    # 项目元数据 + 模型配置
├── world/                          # 世界观（直观阅读 → Markdown）
│   ├── overview.md
│   ├── magic-system.md
│   ├── geography.md
│   ├── history.md
│   └── rules.yaml                  # 世界规则（结构化）
│
├── characters/                     # 角色
│   ├── index.yaml                  # 角色索引
│   ├── protagonist/
│   │   ├── profile.md
│   │   └── profile.yaml
│   └── relationships.json          # 人物关系图（前端渲染）
│
├── plot/                           # 剧情
│   ├── outline.md                  # 大纲
│   ├── arcs/                       # 故事弧
│   │   ├── arc-01-起源.yaml
│   │   └── arc-02-冲突.yaml
│   └── timeline.json               # 时间线（前端可视化）
│
├── chapters/                       # 章节正文
│   ├── vol-01/
│   │   ├── ch-001.md
│   │   ├── ch-002.md
│   │   └── ch-meta.yaml            # 章节元数据（状态、字数、摘要）
│   └── vol-02/
│
├── assets/                         # 辅助资源
│   ├── name-dictionary.yaml
│   └── style-guide.md
│
├── .aisync/
│   └── conversations/               # Agent 对话历史（项目内存储）
│       └── {conversation_id}.json
│
├── .vectordb/                      # 向量数据库（本地）
│   └── chroma/
│
└── operation.log                   # 简单操作日志（代替 .history/）
```

### project.yaml 补充内容

```yaml
name: "我的小说"
model:
  provider: "anthropic"        # anthropic | openai | custom
  api_base: "https://api.deepseek.com/anthropic"
  api_key_env: "DEEPSEEK_KEY"
  model_name: "deepseek-v4-pro"
  parameters:
    temperature: 0.7
    max_tokens: 8192
```

### 三种格式的分工原则

| 格式 | 适用场景 | 示例 |
|------|---------|------|
| **Markdown** | 需要直接阅读/编辑的长文本 | 章节正文、世界观描述、角色传记 |
| **YAML** | 半结构化、人可读可编辑 | 大纲节点、角色属性、项目配置 |
| **JSON** | 复杂结构、前端渲染 | 人物关系图、时间线、统计数据 |

---

## 五、向量化方案

```python
class ProjectVectorStore:
    """项目级向量存储，用于语义检索"""

    COLLECTIONS = {
        "chapters":    {"chunk_size": 500, "overlap": 50},
        "characters":  {"chunk_size": 300},
        "world":       {"chunk_size": 400},
        "plot":        {"chunk_size": 200},
    }

    async def index_file(self, file_path: str):
        """文件变更时自动重新索引"""

    async def query(self, text: str, collections: list = None, top_k: int = 10):
        """语义检索：写作时自动拉取相关设定，保证前后一致"""

    async def check_consistency(self, new_content: str):
        """一致性检查：新写内容是否与已有设定矛盾"""
```

### 向量化的核心价值

- 写第 50 章时，自动检索前面提到的相关伏笔
- 创建新角色时，自动对比已有角色避免雷同
- 世界观一致性校验（新设定 vs 已有设定）

---

## 六、核心工具清单（首批）

### 工具双模式说明

每个工具返回 `ToolResult`，其中可选的 `ui_hint` 字段让前端决定如何渲染：

| 工具 | 纯 Agent 模式 | 前端模式 |
|------|:---:|:---:|
| `write_chapter` | 流式输出文本 | 编辑器实时显示 + token 级流式 |
| `relationship_graph` | 文本描述关系 | 可视化关系图（可拖拽编辑） |
| `timeline_manage` | 文本列表 | 时间线拖拽组件 |
| `plot_outline` | 树形文本 | 大纲树编辑器 |
| `create_character` | 文本描述 | 表单 + 角色卡片预览 |

### 工具列表

| 工具名 | 功能 | ui_hint | 优先级 |
|--------|------|---------|--------|
| `write_chapter` | 撰写/续写章节 | `stream:editor` | P0 |
| `edit_chapter` | 修改已有章节 | `stream:editor` | P0 |
| `create_character` | 创建角色档案 | `card:character` | P0 |
| `update_worldview` | 更新世界观设定 | — | P0 |
| `search_project` | 向量语义搜索 | `list:search_results` | P0 |
| `consistency_check` | 一致性校验 | `list:issues` | P1 |
| `plot_outline` | 生成/调整大纲 | `tree:outline` | P1 |
| `relationship_graph` | 管理人物关系 | `graph:relations` | P1 |
| `style_transfer` | 文风调整 | — | P2 |
| `timeline_manage` | 时间线管理 | `timeline:events` | P2 |
| `export_novel` | 导出 epub/pdf | — | P2 |
| `chapter_summary` | 自动生成摘要 | — | P2 |

---

## 七、开发阶段规划

### Phase 1 — 基座搭建（2-3 周）

- [x] 工具基类 & 注册系统（含 `ui_hint`）
- [x] 主 Agent 循环（含中断机制）
- [x] LLM Client 适配器（Anthropic / OpenAI / 自定义）
- [x] 项目文件系统（CRUD + asyncio.Lock）
- [x] FastAPI + WebSocket 通信层
- [x] Tauri 壳 + 基础前端框架
- [x] LLM 运行时配置 API（GET/PUT /api/config/llm）
- [x] 前端 LLM 设置面板（SettingsPanel 组件）
- [x] CSS 变量设计系统 + 前端目录结构重构
- [x] Sidebar 组件（项目选择、视图切换、连接状态）
- [x] ChatPanel 组件（事件流展示、消息输入、中断控制）
- [x] SettingsPanel 迁移 + App 重构（三视图布局）
- [x] 后端预设系统（Preset CRUD API + 内置/自定义预设管理）
- [x] 前端预设集成（usePresets hook + SettingsPanel 完整表单 + WebSocket preset_id 同步）
- [x] 主 Agent 可调用工具按预设配置：默认全部工具，可在设置页按工具勾选
- [x] 对话运行时工具选择：ChatPanel 可为本次主 Agent 请求选择可调用工具，默认全部
- [x] 对话工具选择体验打磨：已选摘要、全选/全不选/恢复默认、空选择禁发提示

### Phase 1.5 — 验收修复（基本完成，低优先级遗留）

> Phase 1 验收发现的问题。截至 2026-05-03。

**高优先级** — 全部已修复
- [x] WebSocket 健壮性：自动重连（指数退避）、错误处理、JSON.parse try/catch
- [x] 事件数组内存泄漏：保留最近 200 条
- [x] 预设更新后 Agent 缓存未失效
- [x] 预设加载/错误状态展示
- [x] 错误消息颜色区分成功/失败
- [x] 颜色对比度达标

**中优先级** — 全修
- [x] 删除预设确认对话框
- [x] presets.json 迁移到 `~/.aisync/`
- [x] 项目选择改为 Tauri 原生目录对话框
- [ ] 无 React Error Boundary（非阻塞）
- [ ] Agent 循环无迭代上限（非阻塞，Phase 3 加）

**低优先级**
- [x] OpenAI 客户端应用内 API Key
- [x] Anthropic 流式响应 await 修复
- [x] 聊天消息角色区分（用户蓝色右对齐，Agent 左对齐）
- [x] SettingsPanel 错误消息颜色
- [ ] 向量存储为空实现（Phase 3）
- [ ] 暗色模式 / 响应式布局
- [ ] 设置保存成功提示不自动消失
- [ ] vite 在 dependencies 而非 devDependencies
- [ ] 少量硬编码像素值

### Phase 2 — 核心写作（基础版完成，需 UI 打磨）

> 截至 2026-05-03，所有 Phase 2 功能已有**最低可用实现**，但 UI 质量粗陋，需持续打磨。

- [x] 工具 UI 框架：ToolDrawer + SchemaForm + `/api/tools`（基础版，表单可用但不美观）
- [x] 写作工具：write_chapter、edit_chapter、create_character、update_worldview（后端逻辑可运行，前端表单粗糙）
- [x] Markdown 编辑器：CodeMirror 6，lazy 加载（可用但无工具栏）
- [x] 对话交互：ChatPanel + 角色区分气泡 + **Agent 流式输出**（WebSocket 推送 text delta，前端实时渲染 + 闪烁光标）
- [x] 项目选择：Tauri 原生目录选择 + init 骨架生成
- [x] 文件树：**递归树形**（目录可展开/折叠，文件夹在上文件在下，自动隐藏 `.aisync`/`.vectordb`）
- [x] 对话历史：`.aisync/conversations/` CRUD（列表可折叠，首条消息自动生成标题，启动时优先恢复上次激活对话）

**待改进（Phase 2 打磨）：**
- [x] 工具系统重构第一步：Tool Descriptor + 文件影响声明 + Tool Run 记录 + 工具中心最近运行
- [x] Agent 对话 Markdown 渲染：正文支持标题、列表、引用、代码块、链接、表格
- [x] Agent 流式回复去重：`stream` 与 `agent_final` 合并展示，避免同一回复出现两次
- [x] 对话历史完整加载：历史对话不再截断为最近 200 条，实时事件仅保留高上限保护
- [x] 启动对话恢复：有历史时优先加载上次激活对话，不再误清记忆后新建
- [x] Agent 会话记忆注入：同一 conversation 的近期历史会随当前请求传回 LLM，并按消息数/字符预算裁剪
- [x] 会话摘要记忆压缩：超出近期窗口的旧消息压缩到 `.aisync/conversations/{id}.summary.md` 并随请求注入
- [x] 工具结果 AI 渲染：`ui_hint` 支持编辑器预览、搜索结果、角色卡片和 JSON fallback
- [x] 文件树默认收起：当前选中文件所在目录自动展开
- [x] 前端 chunk 优化：CodeMirror 编辑器 lazy 加载，消除 Vite 大 chunk 警告
- [x] 设置面板字段过多，应分 tab：改为设置内部侧栏分页（2026-05-15）
- [ ] ToolDrawer 内嵌编辑器高度不足
- [ ] Markdown 编辑器无工具栏
- [ ] 无暗色模式
- [ ] 截图级大纲/组织/角色管理页仍需基于工具协议继续实现

### Phase 2.5 — 下一步作业（工具系统继续破旧立新）

> 目标：从“工具中心 + 执行台”推进到“小说对象管理工作台”。优先把已有工具协议沉淀成可扩展的管理页和更可靠的 Agent 运行体验。

0. **基础信息页（P0）**
   - [x] 读取和编辑 `project.yaml` 小说名
   - [x] 自动统计已完成章节数、正文字符数、角色档案数、世界观文档数、大纲节点数
   - [x] 展示章节进度列表，并可跳转打开章节文件
   - [x] 展示世界观文件列表，并可跳转打开设定文件
   - [x] 增加创作目标、章节目标数、目标字数、当前状态、简介等项目级字段
   - [x] 展示章节进度和字数进度
   - [ ] 增加计划日期、状态流转记录和阶段里程碑

1. **大纲管理页（P0）**
   - [x] 增加 `outline_generate` 工具描述与后端实现
   - [x] 兼容读取 `plot/outline.md`，并预留 `plot/outline.json`
   - [x] 前端新增大纲管理视图：章节卡片、刷新、AI 生成/续写入口
   - [x] 工具结果支持 `list:outline_chapters` 渲染
   - [x] 侧边栏工具页由 Tool Descriptor `workspace_view` 驱动，删除工具后入口自动消失
   - [x] 前端 Workspace View Registry：工具页组件集中注册，App 不再为每个工具页硬编码渲染分支
   - [x] 大纲节点编辑：新增、删除、编辑标题和摘要
   - [x] `plot/outline.json` 结构化写入，并同步生成可读 `plot/outline.md`
   - [x] 大纲搜索与节点统计
   - [x] 大纲节点上移/下移排序
   - [ ] 大纲拖拽排序
   - [ ] `plot/outline.json` 与 `plot/outline.md` 双向差异合并

2. **章节管理页（P0）**
   - [x] `write_chapter` 通过 `workspace_view` 驱动侧边栏章节管理入口
   - [x] 读取 `chapters/**/*.md` 并聚合标题、摘要、字数和正文
   - [x] 前端新增章节列表/详情/搜索/Markdown 预览
   - [x] 支持直接编辑保存章节 Markdown
   - [x] 写作/编辑工具执行后自动刷新章节视图、基础信息统计和文件树
   - [x] 章节元数据 `ch-meta.yaml`：状态、摘要、目标字数、修订轮次
   - [x] 章节页展示单章字数目标进度
   - [ ] 章节与大纲节点关联

3. **角色管理页（P0）**
   - [x] 读取 `characters/**/profile.yaml` 和 `profile.md`
   - [x] 前端新增角色列表/详情/搜索
   - [x] `create_character` 执行后自动刷新角色视图
   - [x] `create_character` 通过 `workspace_view` 驱动侧边栏角色管理入口
   - [ ] 增加角色重复检查和 slug 冲突提示

4. **世界观管理页（P1）**
   - [x] 读取 `world/**/*.md`
   - [x] 前端新增世界观文档列表和预览
   - [x] `update_worldview` 通过 `workspace_view` 驱动侧边栏世界观整理入口
   - [x] `update_worldview` 执行后自动刷新
   - [x] 支持已有世界观文档直接编辑保存
   - [x] 支持通过 `update_worldview` 按路径新建/追加/重写世界观文档
   - [ ] 世界观主题模板和常用分类入口
   - [ ] 世界观文档重命名/删除

5. **前端工具协议整理（P1）**
   - [x] `AiRender` 抽离 `rendererRegistry`，工具结果渲染类型不再堆在 if/else 中
   - [x] `workspaceViews` 明确为前端支持的工具工作区白名单 registry
   - [x] 工具默认方案语义修正：`default_preset_id` 指向设置里的 LLM 预设，`default_agent` 废弃兼容
   - [x] 工具结果 renderer 增加更严格的数据 schema 校验

6. **Tool Run 体验增强（P1）**
   - [ ] 后端 ToolRun 增加 `running/failed/cancelled` 之外的阶段字段
   - [x] 工具中心支持手动刷新工具描述，避免新增工具后前端列表陈旧
   - [x] 工具中心支持工具搜索，并将一致性校验归入分析校验分类
   - [x] 前端运行历史支持按工具、状态筛选
   - [x] 历史记录支持复用参数重新打开工具表单
   - [x] 工具执行前增加更明确的文件影响确认
   - [x] 工具 AI 生成支持选择 LLM 方案，默认优先工具声明方案，否则使用当前设置方案
   - [x] 工具默认 LLM 方案项目级配置：`.aisync/tool_settings.json` 持久化，ToolDrawer 可保存/清除默认方案
   - [x] 工具元信息中文化：工具描述、参数说明、结果提示尽量改为中文

8. **LLM 配置体验（P1）**
   - [x] 复制已有预设配置
   - [x] 重命名已有预设
   - [x] 自动获取模型列表并提供模型名补全
   - [x] 模型选择显式下拉：模型列表可直接选择，获取失败时保留当前模型并展示错误（2026-05-16）

7. **Agent 对话稳定性（P1）**
   - [x] 后端避免对发起 WebSocket 重复广播最终消息，或前端按 `conversation_id`/内容去重
   - [x] Agent 回复重复渲染二次加固：同一轮 `stream` 与 `agent_final` 回溯合并，避免中间夹工具/状态事件时重复显示
   - [x] Agent 循环增加最大迭代上限
   - [x] 工具调用事件增加 tool name、params 摘要和执行耗时
- [x] Agent 动态反馈：检索上下文、请求模型、工具调用、完成状态和报错前台可见
- [x] Agent 任务列表：按写作、设定、角色、大纲、检索、一致性检查等请求动态生成任务模板
- [x] 一致性检查降噪：过滤结构性编号，收紧否定词与状态词匹配
   - [x] Markdown 渲染继续补齐任务列表、分割线、转义边界
   - [x] Markdown 嵌套列表正修：有序列表内嵌无序列表不再导致后续序号重置为 1（2026-05-15）
   - [x] DeepSeek thinking 模式兼容：工具调用多轮时保留并回传 `reasoning_content`，避免 400 报错（2026-05-15）
   - [x] DeepSeek 思考模式可选：设置页开关会映射到 DeepSeek V4 `thinking enabled/disabled` 请求参数（2026-05-15）
   - [x] DeepSeek thinking+tools 加固：禁用该场景流式并多路径提取 `reasoning_content`，降低续轮 400 风险（2026-05-16）
   - [x] 聊天历史与设置页滚动修复：限制抽屉/设置页高度并启用内部滚动（2026-05-16）
   - [x] 工具结果默认折叠：`tool_result` 不再同时显示原始内容和 `ui_hint` 结构化列表，减少 Agent 对话噪音（2026-05-16）
   - [x] Agent 任务面板保留本轮任务列表：避免新会话/历史刷新后任务流程栏被 liveEvents 重置吞掉（2026-05-16）
   - [x] Agent 任务面板兜底流程：缺少 `agent_task_list` 时根据状态事件合成四步流程，避免安装版任务栏空白（2026-05-16）
   - [x] Markdown 流式标题容错：修复输出停在 `### ` 等半截标题时解析器死循环并触发 `RangeError: Invalid array length`（2026-05-16）
   - [x] `<think>` 思考块渲染：MarkdownView 识别完整/流式半截 `<think>`，默认折叠成一行，展开后显示内容（2026-05-18）
   - [x] 长对话历史分页/窗口渲染，避免超长 DOM 影响滚动性能
   - [x] 超长会话摘要记忆：旧消息压缩为 conversation summary，避免仅依赖近期窗口
   - [x] 用户可见记忆状态：显示本次注入了多少条历史、是否已有摘要、是否后台更新
   - [x] 摘要记忆后台化：摘要更新不阻塞用户主回复
   - [x] 摘要质量评估：为小说设定、用户偏好、未完成事项增加检查样例
   - [x] 摘要质量离线测试：覆盖关键设定、用户偏好、未完成事项和空摘要失败场景
   - [x] 对话消息选择：选择模式、全选可见、复制、引用到输入
   - [x] 单条消息快捷操作：复制、引用、加入/取消选择
   - [x] 引用输入轻量化：引用只插入短摘录，不再把整条长消息搬进输入框
   - [x] 引用上下文框：引用显示为输入框上方摘要，发送时附带上下文，不污染输入框
   - [x] AI 选项按钮：识别“可选方案/下一步选项”后的编号列表，渲染为可点击选择并继续对话
   - [x] 安装版后端端口动态分配：前端启动时读取真实 API 地址，不再固定 `8000`
   - [x] 安装版隐藏后端 Python 控制台窗口，关闭前端时只终止 Tauri 管理的后端进程
   - [x] 轻量版检查更新：设置页读取 GitHub Releases latest，提示新版并打开安装包/发布页
   - [x] 2026-05-13 安装器覆盖安装加固：NSIS 安装/卸载前清理旧版 AiSync 与随包 Python 进程，避免 runtime 文件锁定
   - [x] 2026-05-13 整合项目报告与规划到本地汇总文件 `项目.local.md`
   - [x] 2026-05-13 设置区重构为分区卡片，更新按钮切换为桌面外部打开，README 许可改为商用需授权

### Phase 3 — 智能增强（2-3 周）

- [x] 向量化引擎集成：本地分块索引、内容签名失效、`.aisync/vector_index.json` 持久化
- [x] 语义/文本混合检索基础版：`ProjectVectorStore.query` + `search_project` 真实检索
- [x] Agent 上下文自动注入：主 Agent 请求前从项目索引检索相关片段
- [x] 向量索引前端状态页：重建索引、查看文件数/片段数、分组分布和检索测试
- [x] 章节工作流接入向量辅助：当前章节相关设定检索、一致性检查工具预填、索引重建入口
- [x] 可选接入真实 embedding：OpenAI-compatible embeddings 写入本地索引，并与词面检索混合评分
- [x] 可选接入 ChromaDB 后端：配置切换到 `chroma` 时写入持久化集合，缺依赖自动回退本地索引
- [x] 一致性校验工具基础版：基于索引检索相关设定，输出数值/状态/否定表述风险
- [x] 一致性校验前端渲染：`list:issues` 问题清单
- [x] 一致性校验 LLM 二次判定与修复建议：工具 `AI 生成` 路径输出结构化问题和 suggestion
- [ ] 人物关系图可视化（前端从 `ui_hint` 渲染）

### Phase 4 — 体验打磨（2 周）

- [ ] 大纲可视化编辑
- [ ] 时间线工具
- [ ] 导出功能
- [ ] 文风分析 & 调整
## Phase 4.5 — 打包与客户机运行时

- [x] 开发态 `npm run tauri dev` 不再打包后端，直接启动源码后端
- [x] 发布态 `npm run tauri build` 不再默认跑 PyInstaller
- [x] 发布包改为携带干净 `backend-src` 源码资源
- [x] 增加轻量 Python runtime 准备脚本，自动下载/校验 Python runtime
- [x] 准备内置轻量 Python：`frontend/src-tauri/resources/runtime/python/python.exe`
- [x] 安装版优先使用随包 Python 直接启动后端
- [x] 安装版随包 Python 启动失败时，回退到自动 venv
- [x] venv 回退会自动创建 `%APPDATA%\com.aisync.app\runtime\.venv`
- [x] venv 回退会自动 `pip install <backend-src>` 并用 venv 启动后端
- [x] 诊断信息加入 Python 候选、源码候选、setup 日志
- [x] 安装包清单确认不再包含 `aisync-backend.exe`、`__pycache__`、`.pyc`
- [x] 增加离线 wheelhouse 准备脚本：`scripts/prepare_runtime_wheels.ps1`
- [x] 安装版发现 `aisync-backend` wheel 时优先从 wheelhouse 离线安装
- [~] 离线 wheelhouse 暂缓：当前默认交付不携带 `runtime/wheels/`，避免安装包膨胀；仅在需要无网 venv 回退时启用
- [x] 安装版资源清单包含内置 Python、后端源码和依赖
- [x] 安装版真实冷启动验收：端口清空后从已安装目录启动，确认随包 Python 后端可启动且 `/health` 通过
- [x] 移除旧 PyInstaller sidecar 启动回退逻辑，避免升级安装残留的旧 exe 被误启动
- [x] Windows 启动后端不弹空控制台：Python/venv 检查和后端进程使用隐藏窗口启动
- [x] 安装版前端快速显示：Tauri setup 不再同步等待后端启动，后端改为后台线程启动
- [x] 关闭前端清理后端：窗口关闭事件显式终止托管后端进程
- [x] 清理旧 PyInstaller 构建脚本 `scripts/build_backend.ps1`
- [x] 2026-05-14 安装器升级回归：版本升至 `0.1.7`，NSIS 安装前清理旧主程序/卸载器/backend-src/runtime，覆盖安装可替换旧 `aisync.exe`
- [x] 2026-05-14 安装版启动回归：已安装目录启动显示 `version=0.1.7`，随包 Python 后端健康，关闭主窗口后无安装目录下的 `aisync/pythonw` 残留进程
- [x] 2026-05-14 README 拆分：README 改为项目入口文档，新增 `技术细节.md` 承接目录结构、运行时、日志、API、工具和向量细节

---

## 八、连续作业队列（从 2026-05-14 起）

> 原则：之后用户说“继续”时，默认按本队列从上往下推进，不再每次停下来问下一步。除非用户插入 bug、打包故障或明确改方向。

### A. 发布链路闭环（P0）

目标：让项目从“本机能打包”走到“GitHub Release 可发版、客户端可发现、用户能安装/升级、出错能定位”。

- [x] A1. 建立发布检查清单：版本号、构建命令、产物路径、安装覆盖验证、启动验证、关闭进程验证、日志检查
- [x] A2. 增加 `发布流程.md`：从改版本号到 GitHub Release 上传安装包的完整步骤
- [x] A3. 检查更新 UI 再打磨：明确当前版本、最新版本、检查时间、下载资产名、失败原因
- [x] A4. GitHub Release asset 选择规则：优先 NSIS `*-setup.exe`，无匹配时退回发布页
- [x] A5. 下载/打开失败前台可见：Tauri `open_external_url` 报错要显示在设置页，而不是静默失败
- [x] A6. 生成安装包后自动输出诊断摘要：版本、安装包路径、文件大小、runtime 是否存在
- [x] A7. 安装版诊断页入口：在设置页增加“复制诊断信息”和“打开日志目录”
- [x] A8. 整理旧版清理说明：哪些可删、哪些是用户数据不能删
- [x] A9. 决定 MSI 是否继续作为发布产物；如 NSIS 为主，README/发布流程明确写 NSIS 优先
- [x] A10. 发布前本机端到端验收脚本化：覆盖安装、启动、健康检查、关闭进程检查
- [x] A11. 检查更新版本比较修复：从 release tag/name 中提取数字版本，避免同号或更低版本误报新版本（2026-05-15）

### B. Agent 运行可靠性（P0）

目标：Agent 不再像黑箱；每次任务能看见状态、失败原因、可恢复入口和历史记录。

- [x] B1. Agent run 记录持久化：每轮对话生成 run_id，记录开始/结束、状态、错误、工具调用摘要
- [x] B2. 前端显示当前 run 状态：准备上下文、请求模型、执行工具、等待工具、整理回复、完成/失败
- [x] B3. 失败恢复入口：失败消息提供“复制错误”“重试本轮”“带错误继续问”
- [x] B4. 中断状态收敛：中断后清理 pending 状态，避免旧会话打开仍显示等待响应
- [x] B5. WebSocket 重连后的状态恢复：重新拉取当前 conversation、active run、最近事件
- [x] B6. 空 assistant message 防御：发送给 LLM 前过滤或修复无 content/tool_calls 的 assistant 消息
- [x] B7. 工具调用超时与错误分级：用户取消、工具失败、模型失败、网络失败分开显示
- [x] B8. Agent 事件日志压缩：长期保留关键事件，流式 token 不无限写入历史
- [x] B9. 对话记忆状态更可操作：显示摘要更新时间、摘要长度、近期消息窗口
- [x] B10. 加一组后端回归测试覆盖重复回复、空 assistant、工具失败、恢复历史
- [x] B11. Agent run 报告位置修正：`agent_run` 不再显示在聊天气泡内，改在 Agent 任务区显示运行摘要和工具调用报告（2026-05-15）

### I. Agent 工作流与提示词管理（P0/P1）

目标：把当前“单主 Agent 一轮调用工具”的模式，升级为可见、可暂停、可换模型、可人工确认的写作工作流。主 Agent 负责理解用户目标和编排步骤，具体写作、检查、整理、特殊片段生成由受控的步骤/子 Agent 执行；工具继续作为稳定、可审计的文件和项目操作层。

问题判断（2026-05-19）：

- 当前架构仍能支撑普通对话和简单工具调用，但写章节这类长流程越来越不适合“一口气执行到底”。
- 用户希望在写章节时追加临时提示词、指定某一段用特定模型/预设、在中途确认或改方向；这些需求属于“步骤级控制”，不是单个工具参数能自然解决的。
- 不建议直接做完全自由的 Agent 集群；更稳妥的路线是先做 WorkflowRun/WorkflowStep，再把专业子 Agent 作为受控执行器接入。
- 当前提示词不够透明：默认 system prompt 在 `backend/app/agent.py`，预设里可覆盖 system prompt，运行时还会自动注入会话记忆、向量检索上下文、伏笔上下文和工具描述。用户很难看到本轮到底给模型喂了什么。

设计方向：

```text
用户请求
  ↓
主 Agent / 编排器
  ↓
WorkflowRun
  ↓
WorkflowStep[]
  ├─ 每步可指定 LLM 预设
  ├─ 每步可挂载提示词包
  ├─ 每步可声明输入上下文和输出目标
  ├─ 每步可要求人工确认
  └─ 每步输出优先进入 temp/drafts，确认后再写正式文件
```

提示词技术边界（2026-05-19，详见 `项目文档/提示词技术.md`）：

- 不采用“万物皆 prompt”的一锅管理。
- 人物、伏笔、世界观、大纲、章节是项目对象和结构化数据，不长期复制进提示词。
- Prompt Pack 只管理文风、写作规则、输出格式、禁忌、偏好、特殊模型片段规则等模型行为。
- Context Pack 管理本次任务需要注入哪些结构化上下文，如本章人物、相关伏笔、当前大纲节点、相关世界观。
- Workflow Step 管理执行顺序、步骤级模型、Prompt Pack、Context Pack、输出位置和人工确认点。
- 保留参考方案里的集中管理、模板语法、可启用/停用、可复用、可按阶段选择，但不接受“所有内容都写成提示词”。

执行拆分：

- [x] I1. 建立提示词来源审计：Agent run 记录本轮使用的 system prompt 来源、记忆摘要、近期消息数、向量上下文数量、伏笔上下文是否注入、可用工具列表（2026-05-19）
- [~] I2. 前端增加“提示词预览/上下文预览”：暂缓，不优先暴露完整 prompt；先做可管理、可保存、可引用的提示词包。
- [x] I3. 建立提示词包数据结构：全局/项目级 prompt packs，字段包含名称、分类、适用阶段、正文、变量、默认启用状态（2026-05-19）
- [x] I4. 提示词包管理页：新增、编辑、复制、禁用、删除；支持写作/规划/润色/检查/特殊风格等分类（2026-05-19）
- [x] I5. 对话阶段 Prompt Pack 自动注入：已启用且适用阶段包含 `chat` 的提示词包会自动进入主 Agent 对话，不要求每轮手动选择；临时追加入口后续再做（2026-05-20）
- [x] I6. 写章节/编辑章节工具接入 Prompt Pack：`chapter_draft` 阶段自动作用于 `write_chapter`，`revision` 阶段自动作用于 `edit_chapter`；ToolRun 记录提示词包阶段、数量和名称。临时额外提示词改留到 WorkflowStep 人工干预里实现，不放进每轮普通对话。（2026-05-20）
- [x] I6.1. 项目级默认启用关系：设置页提示词管理支持“沿用全局启用/只使用本项目勾选项”；项目设置写入 `.aisync/prompt_pack_settings.json`，主 Agent 对话和章节工具都会按项目设置过滤 Prompt Pack，避免全局文风影响所有项目。（2026-05-20）
- [x] I7. 定义 WorkflowRun/WorkflowStep 持久化结构：新增 `.aisync/workflows/{run_id}.json`、`/api/workflows`、后端 WorkflowRunStore 和前端“工作流”页；支持记录状态、步骤、模型预设、提示词包、上下文包、输出位置、错误和人工确认点。当前只完成骨架和可见状态，执行器进入 I8。（2026-05-20）
- [x] I8. 写章节工作流最小版：工作流页可手动推进下一步，当前支持检索上下文 → 生成章节计划 → 用户确认暂停 → 生成草稿并写入 `temp/drafts/{run_id}.md`。暂未做自动连续执行、步骤编辑、分段长文、润色检查和正式章节写入确认。（2026-05-20）
- [ ] I8.1. 工作流步骤编辑：用户确认前可编辑计划、补充临时提示词、调整输出路径或指定步骤模型。
- [ ] I8.2. 工作流分段写作：把正文草稿拆成多个场景/段落 step，降低长文输出失败率。
- [ ] I8.3. 草稿转正式章节：从 `temp/drafts` 写入 `chapters/**.md` 前展示 diff 和文件影响确认。
- [ ] I9. 步骤级 LLM 预设选择：允许某一步使用 Grok/Claude/DeepSeek 等不同预设，默认继承当前对话预设。
- [ ] I10. 人工干预机制：步骤暂停、编辑步骤提示词、重试当前步骤、跳过步骤、把某步输出改写后继续。
- [ ] I11. 专业子 Agent 受控接入：章节规划 Agent、正文写作 Agent、风格润色 Agent、一致性检查 Agent、特殊片段 Agent。
- [ ] I12. 工具层保持审计边界：正式文件写入仍走工具和确认；自由草稿优先写入 `temp/drafts`。

### C. 小说对象工作台（P0/P1）

目标：把“章节/角色/世界观/大纲”做成真正面向小说对象的工作台，而不是文件列表套表单。

- [x] C1. 章节与大纲节点关联：章节元数据记录 outline node id，大纲页可看到对应章节状态（2026-05-15）
- [x] C2. 大纲拖拽排序：前端拖拽重排后写回 `plot/outline.json` 并同步 markdown（2026-05-15）
- [x] C3. 大纲节点状态：未开始/草稿/修订/完成，联动基础信息统计（2026-05-15）
- [x] C3.5. 伏笔管理基础版：`plot/foreshadows.json`、伏笔工作区、状态/重要性/章节/大纲关联、基础信息统计（2026-05-15）
- [x] C3.6. 大纲原文/结构分离：Markdown 原文完整展示，结构视图只使用 `outline.json`，支持从原文严格导入章节节点（2026-05-16）
- [x] C4. 世界观文档重命名/删除：带文件影响确认，更新文件树和索引（2026-05-15）
- [x] C5. 世界观主题模板：地理、历史、势力、技术/魔法、规则、年表（2026-05-15）
- [ ] C6. 角色 slug 冲突提示：创建角色前检查目录/名称重复
- [ ] C7. 角色重复检查：按姓名、别名、设定关键词做轻量相似提示
- [ ] C8. 角色关系数据结构：定义 `characters/relationships.json` schema
- [ ] C9. 人物关系图可视化：从 `ui_hint=graph:relations` 渲染，支持节点/边查看
- [ ] C10. 基础信息页增加计划日期、状态流转记录和阶段里程碑
- [~] C11. 伏笔智能提醒：写章节前检索相关伏笔，提示未回收/可回收伏笔
  - [x] C11.1 章节页相关伏笔提示：按埋设章节、回收章节、关联大纲节点和相关文件分组展示（2026-05-15）
  - [x] C11.2 写作工具/Agent 上下文注入相关伏笔（2026-05-15）
  - [x] C11.3 伏笔提醒可解释化：显示命中原因和建议动作（2026-05-15）

### D. 工具系统继续破旧立新（P1）

目标：工具成为可发现、可配置、可复用、可审计的能力单元，前端呈现尽量由工具描述符驱动。

- [ ] D1. ToolRun 增加阶段字段：`preparing/running_model/running_tool/writing_files/indexing/completed`
- [ ] D2. ToolRun 支持取消标记：不能强杀时也能阻止后续步骤并标记 cancelled
- [ ] D3. 工具参数 schema 增强中文校验错误
- [ ] D4. 工具中心按“写作/设定/角色/检索/校验/导出”分组展示
- [ ] D5. 工具详情页显示：能力说明、读写范围、默认 LLM 方案、最近运行、前端页面入口
- [ ] D6. 工具执行结果支持“写入文件前预览 diff”模式
- [ ] D7. 工具描述符增加 examples，供前端展示和 Agent prompt 使用
- [ ] D8. 工具前端 renderer 缺失时显示可读 fallback，并提示缺少 renderer 类型
- [ ] D9. 工具工作区入口排序策略：基础功能在前，工具页单独分组
- [ ] D10. 工具权限按会话持久化：本次对话选择的工具集合可保存为偏好

### E. 向量化与一致性增强（P1）

目标：减少误报，提高检索可解释性，把向量索引用到真实写作流程里。

- [ ] E1. 索引详情页显示每个文件的 chunk 数、更新时间、签名、索引状态
- [ ] E2. 检索结果增加命中原因：关键词命中、语义命中、混合分数拆分
- [ ] E3. 一致性检查数字误报继续降噪：章节编号、标题编号、列表编号、百分比范围分开处理
- [ ] E4. 一致性检查按类型分组：数值、时间线、人物状态、地理位置、设定冲突、语气不确定
- [ ] E5. LLM 二次判定加入引用片段和置信度，避免只给泛泛建议
- [ ] E6. 写章节前自动拉取“本章相关设定包”：角色、地点、伏笔、世界规则
- [ ] E7. 大纲节点关联检索：选中大纲节点时显示相关章节/设定/角色
- [ ] E8. 索引后台刷新：文件保存后延迟增量重建，而不是全量手动
- [ ] E9. Chroma 缺依赖/损坏时的前台诊断和一键回退 local
- [ ] E10. 增加向量索引后端测试：分块、签名失效、混合排序、一致性降噪

### F. 编辑器与写作体验（P1/P2）

目标：把基础 Markdown 编辑器变成能支撑长篇写作的日常工具。

- [ ] F1. Markdown 编辑器工具栏：标题、粗体、斜体、引用、列表、链接
- [ ] F2. 编辑器保存状态：未保存、保存中、已保存、保存失败
- [ ] F3. 文件树搜索与过滤
- [ ] F4. 文件树新建/重命名/删除文件夹和 markdown 文件
- [ ] F5. 最近打开文件列表
- [ ] F6. 章节页增加字数趋势和修订轮次记录
- [ ] F7. 写作目标提醒：按章节目标/总字数目标显示差距
- [ ] F8. 导出功能基础版：导出全部章节为单个 Markdown
- [ ] F9. 导出 epub/pdf 方案评估与最小实现
- [ ] F10. 暗色模式与高对比模式

### H. 自由文件区（P1）

目标：在每个小说项目内建立 `temp/` 自由工作区，让 Agent 可以安全地读写临时资料、外部同步内容和中间输出，而不污染正式章节/设定/角色目录。

设计边界：

- `temp/` 是项目内目录：`<project>/temp/`
- Agent 对 `temp/**` 拥有完整读写能力
- `temp/` 默认不计入章节、世界观、角色等正式统计
- 从 `temp/` 移入正式目录必须经过用户确认
- 外部项目资料先复制/同步到 `temp/inbox/`，Agent 不直接任意读写外部路径
- 向量索引可读取 `temp/`，但必须带 `source=temp` 标记，检索和一致性判断要区分临时资料与正式资料

建议目录：

```text
temp/
  inbox/      # 外部项目或资料同步进来的原始副本
  drafts/     # Agent 生成的临时草稿、备选方案
  exports/    # 临时导出和整理结果
  notes/      # 自由笔记
  .aisync-temp.json
```

实施拆分：

- [x] H1. 项目初始化创建 `temp/inbox`、`temp/drafts`、`temp/exports`、`temp/notes`（2026-05-18）
- [x] H2. 文件树支持 `temp/` 自由区展示，并用“临时/自由”标记区分正式项目文件（2026-05-18）
- [x] H3. 复用现有文件 API 支持 `temp/**` 新建、编辑、重命名、删除（2026-05-18）
- [ ] H4. Agent 工具增加 `temp_file_list/read/write/delete`，限定根目录为当前项目 `temp/`
- [ ] H5. 主 Agent system prompt 明确：临时资料优先写入 `temp/`，正式目录写入仍按工具约束
- [ ] H6. 增加“归档到正式项目”动作：从 `temp/` 移动到 `chapters/world/characters/plot` 前展示确认和目标类型
- [ ] H7. 外部文件/文件夹同步到 `temp/inbox/`，保留来源记录到 `.aisync-temp.json`
- [ ] H8. 向量索引支持 `source=temp`，检索结果显示“临时资料”，一致性检查默认降低临时资料权重
- [ ] H9. 自由区清理工具：按时间/来源/未引用状态清理临时文件
- [ ] H10. 增加后端测试：路径越界防护、temp 读写、归档确认、索引 source 标记

当前决策（2026-05-18）：

1. 先做项目内 `temp/` 最小闭环，不开放任意外部路径读写。
2. 第一批只实现 H1-H3：初始化目录、文件树标记、前端可打开编辑常见文本文件。
3. 第二批再做 H4-H5：Agent 专用 temp 工具和 system prompt 约束。
4. H6-H8 暂缓到最小闭环稳定后再做，避免一开始把“临时区、正式归档、向量索引、外部同步”全混在一起。
5. Agent 后续写入自由区时默认使用 `temp/drafts/` 或 `temp/notes/`；外部导入材料默认进入 `temp/inbox/`。
6. 安全收紧（2026-05-19）：公开文件 API 和自由区操作只允许 `.md/.txt/.json/.yaml/.yml/.csv`，屏蔽 `.aisync`、`.vectordb`、点号内部路径和 `temp/.aisync-temp.json`。

### G. 配置、许可与项目治理（P1/P2）

目标：项目更像可发布产品，减少维护和授权上的含糊地带。

- [x] G1. 新增正式 `LICENSE` 文件：采用 PolyForm Noncommercial License 1.0.0，商用/分发/嵌入需书面授权（2026-05-19）
- [x] G2. README 许可说明链接到 `LICENSE`（2026-05-19）
- [x] G3. 第三方依赖许可说明：区分 AiSync 自身授权和依赖授权（2026-05-19）
- [ ] G4. `.gitignore` 再核对：runtime、target、local 文档、项目数据、日志全部排除
- [ ] G5. `.codexignore` 核对：避免 Codex 读写大体积 runtime/target/node_modules
- [ ] G6. 技术细节文档持续更新：每次打包策略变化后同步
- [x] G6.1. 新增 Agent 能力文档：`项目文档/Agent能力.md` 记录当前对话、记忆、工具、对象工作台、向量和边界（2026-05-15）
- [x] G6.2. 打包入口收敛：仓库根目录 `npm run build` 一键进入 Tauri 发布打包，`build:frontend` 保留为前端静态构建（2026-05-16）
- [x] G6.3. 发布包一致性校验：打包后自动确认 release exe 嵌入当前 dist 资产；发布产物收敛为 NSIS 并升至 `0.1.8` 避免同版本覆盖误判（2026-05-16）
- [x] G6.4. 新增打包链路文档：`项目文档/打包链路.md` 说明 dev、发布、安装态资源来源和一致性校验（2026-05-16）
- [x] G6.5. 安装版启动体验：release 主程序隐藏 Windows 空终端，前端脚本加载和后端等待阶段增加启动动画（2026-05-16）
- [x] G6.6. 构建产物清理脚本：`scripts/clean_build_artifacts.ps1` 支持 DryRun、保留最新 NSIS、清理旧安装包/MSI/wix/dist/log/runtime-cache（2026-05-18）
- [x] G6.7. 启动等待页动态反馈：轮换启动文案、显示已等待秒数，长等待时提供复制诊断和打开日志入口（2026-05-19）
- [ ] G7. 建立 `CHANGELOG.md`：按版本记录面向用户的变化
- [ ] G8. 建立 issue 模板：bug、功能请求、打包问题
- [ ] G9. 清理旧文档删除状态：确认 `规划.md`/`项目完成报告.md` 是否正式迁移为 `.local.md`
- [ ] G10. 发布前 repo 干净度检查：区分应提交文件和本地文件

### 默认执行顺序

1. 先做 A1-A3：发布链路清单、发布流程文档、更新 UI 反馈。
2. 然后做 B1-B4：Agent run 状态和失败恢复，解决“看起来卡住”的体验。
3. 插入做 I1/I3/I4/I5/I6：提示词来源审计、提示词包结构、管理页、对话和章节工具按阶段自动引用，先解决“想改文风就能在固定位置改并保存”。
4. 再做 I6.1/I7-I10：项目级 Prompt Pack 默认启用、WorkflowRun/WorkflowStep、写章节工作流最小版、步骤级模型选择和人工干预。
5. 再做 C6-C10：角色关系、基础信息里程碑等小说对象工作台补全。
6. 插入做 H4-H8：Agent 专用自由区工具、正式归档确认和 `temp` source 向量标记。
7. 接着做 E1-E4：向量索引可解释与一致性降噪，并把 `temp` source 纳入检索解释。
8. 穿插做 G4-G10：ignore、CHANGELOG、issue 模板和发布前 repo 干净度，保证发布不埋雷。
