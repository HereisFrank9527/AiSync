现在正在进行：Web-only 运行形态、Agent 稳定性与工作流编写

# AI 小说创作平台 — 项目规划

## 零、关键设计决策（已确认）

| 决策点 | 选择 | 技术影响 |
|--------|------|---------|
| **部署形态** | 本地 Web / 局域网 Web | React + Vite + FastAPI；Tauri 桌面壳与安装包链路已移除 |
| **本地 Web 模式** | 主线 | FastAPI 可托管构建后的 React 前端，浏览器访问本机 `127.0.0.1:27631` |
| **项目存储位置** | 项目库 | 默认 `~/.aisync/projects`；不使用安装目录保存项目，避免权限、升级和卸载风险 |
| **多模型支持** | 支持自定义 API | LLM Client 适配器模式（Anthropic / OpenAI / 自定义兼容端点） |
| **协作需求** | 单人使用 | 权限系统大幅简化，无需多角色设计 |
| **版本控制** | 暂不考虑 | 去掉 .history/ 复杂设计，保留简单操作日志 |
| **离线能力** | 部分考虑 | 不打包本地模型，LLM 仍依赖在线 API；后续如需非技术用户分发，再设计 Web runtime 包 |

---

## 一、核心设计理念

```
用户 <──> 前端UI(编辑器+可视化) <──> 主Agent <──> 工具系统 <──> AI模型(LLM)
                 |                        |
            WebSocket               asyncio.Lock
            (流式+事件)              (并发安全)
                 |                        |
            FastAPI Web 服务        项目文件系统 + 向量数据库
```

### 核心通信机制：双通道 + 事件总线

```
┌──────────────┐   WebSocket (流式)   ┌──────────────┐
│   前端 UI     │◄────────────────────►│   Python 后端  │
│              │   REST (CRUD)        │   FastAPI     │
│   编辑器      │◄────────────────────►│              │
│   可视化组件   │                      │   Agent 循环  │
│              │                      │   Tools      │
└──────────────┘                      └──────────────┘
```

1. **Agent → 前端（被动展示）**：工具调用结果通过 WebSocket 实时推送到前端，前端根据 `ui_hint` 渲染对应组件，不阻塞 Agent 循环。

2. **前端 → Agent（主动干预）**：用户在可视化组件上的操作，通过 WebSocket 发回后端，作为伪用户消息插入 Agent 消息队列，Agent 下一轮推理自然感知。

3. **并发安全**：Agent tool_use 循环和前端操作共享 `ProjectContext`，使用 `asyncio.Lock` 保护文件读写。

---

## 二、技术栈

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **运行壳** | 浏览器 + FastAPI | 启动链路更清晰，局域网访问更直接，不再维护桌面安装包 |
| **前端** | React + TypeScript | 组件化，生态丰富 |
| **后端** | Python (FastAPI) | AI 生态最强；开发态和 Web 运行态都直接启动源码后端 |
| **AI 调用** | 适配器模式 | 统一接口，支持 Anthropic / OpenAI / 自定义 OpenAI 兼容端点 |
| **向量数据库** | ChromaDB（本地嵌入） | 轻量，无需额外服务 |
| **项目存储** | 文件系统 (md + yaml + json) | 直观可读，可后续接入 Git |
| **通信** | WebSocket + REST | 流式输出 + 常规 CRUD |

---

## 三、模块化架构

### 3.1 工具系统（Tool Descriptor + Tool Run）

采用**注册制 + 基类继承 + 描述符协议**。插入新工具只需继承基类、放入 `tools/` 目录，自动注册；同一个工具既可被主 Agent 的 tool calling 调用，也可在前端工具中心以表单方式手动执行。

```python
# ===== 工具基类 =====
class BaseTool(ABC):
    name: str
    description: str
    has_frontend_ui: bool = True
    required_permissions: list = []
    default_preset_id: str | None = None  # 设置里的默认 LLM 方案；default_agent 仅保留旧兼容

    @abstractmethod
    def schema(self) -> dict:
        """返回工具参数的 JSON Schema，供 LLM 和前端 SchemaForm 共用"""
        pass

    @abstractmethod
    async def execute(self, params: dict, context: ProjectContext) -> ToolResult:
        """执行工具逻辑，读写项目文件并返回结果"""
        pass

    def file_access(self) -> ToolFileAccess:
        """声明可能读取、修改、生成的文件模式，执行前展示给用户"""

    def presentation(self) -> ToolPresentation | None:
        """声明前端结果呈现类型，如 stream:editor / card:character"""

    def frontend_descriptor(self) -> dict:
        """返回工具中心需要的完整描述符"""


# ===== 工具返回结果 =====
class ToolResult:
    content: str              # 纯文本结果（供 Agent 阅读）
    ui_hint: Optional[dict]   # 前端渲染指令
    metadata: dict            # 工具补充元数据


# ===== 工具运行记录 =====
class ToolRunRecord:
    run_id: str
    tool_name: str
    mode: "execute" | "invoke"
    status: "completed" | "failed"
    started_at: str
    finished_at: str
    file_access: dict
    params: dict
    result: ToolResult | None
    error: str | None
```

当前执行 API 先采用同步执行 + 持久化记录：每次 `execute` 或 `invoke` 都写入 `.aisync/tool_runs/{run_id}.json`。后续如需长任务，可把同一记录结构扩展为 `queued/running/cancelled`。

### 3.2 主Agent 系统

```python
class MasterAgent:
    """主Agent — 编排所有工具"""

    def __init__(self, llm_client, tool_registry, project):
        self.llm = llm_client
        self.tools = tool_registry
        self.project = project
        self.memory = ConversationMemory()
        self.vector_store = project.vector_store
        self._interrupted = False       # 用户中断信号

    async def run(self, user_input: str):
        """主循环：理解意图 → 检索上下文 → 调用工具 → 返回结果"""

        # 1. 从向量库检索相关上下文
        relevant_context = await self.vector_store.query(user_input)

        # 2. 构建 prompt（含工具 schemas + 项目上下文 + 模型配置）
        messages = self._build_messages(user_input, relevant_context)

        # 3. LLM 推理（tool_use 循环）
        while True:
            if self._interrupted:
                self._interrupted = False
                return "操作已中断，等待新指令。"

            response = await self.llm.chat(
                messages=messages,
                tools=self.tools.get_all_schemas()
            )

            if response.has_tool_calls:
                for call in response.tool_calls:
                    tool = self.tools.get_tool(call.name)
                    result = await tool.execute(call.params, self.project)
                    messages.append(tool_result(result))
                    # 通过 WebSocket 推送结果到前端
                    await self._push_to_frontend(result)
            else:
                return response.content

    def interrupt(self):
        """前端调用，设置中断信号"""
        self._interrupted = True

    async def _push_to_frontend(self, result: ToolResult):
        """通过 WebSocket 推送工具结果到前端"""
```


### 3.3 项目上下文（并发安全）

```python
class ProjectContext:
    _lock: asyncio.Lock

    async def read_file(self, path: str) -> str:
        async with self._lock:
            ...

    async def write_file(self, path: str, content: str):
        async with self._lock:
            ...
```

---

## 四、项目文件存储结构

```
my-novel-project/
├── project.yaml                    # 项目元数据 + 模型配置
├── world/                          # 世界观（直观阅读 → Markdown）
│   ├── overview.md
│   ├── magic-system.md
│   ├── geography.md
│   ├── history.md
│   └── rules.yaml                  # 世界规则（结构化）
│
├── characters/                     # 角色
│   ├── index.yaml                  # 角色索引
│   ├── protagonist/
│   │   ├── profile.md
│   │   └── profile.yaml
│   └── relationships.json          # 人物关系图（前端渲染）
│
├── plot/                           # 剧情
│   ├── outline.md                  # 大纲
│   ├── arcs/                       # 故事弧
│   │   ├── arc-01-起源.yaml
│   │   └── arc-02-冲突.yaml
│   └── timeline.json               # 时间线（前端可视化）
│
├── chapters/                       # 章节正文
│   ├── vol-01/
│   │   ├── ch-001.md
│   │   ├── ch-002.md
│   │   └── ch-meta.yaml            # 章节元数据（状态、字数、摘要）
│   └── vol-02/
│
├── assets/                         # 辅助资源
│   ├── name-dictionary.yaml
│   └── style-guide.md
│
├── .aisync/
│   └── conversations/               # Agent 对话历史（项目内存储）
│       └── {conversation_id}.json
│
├── .vectordb/                      # 向量数据库（本地）
│   └── chroma/
│
└── operation.log                   # 简单操作日志（代替 .history/）
```

### project.yaml 补充内容

```yaml
name: "我的小说"
model:
  provider: "anthropic"        # anthropic | openai | custom
  api_base: "https://api.deepseek.com/anthropic"
  api_key_env: "DEEPSEEK_KEY"
  model_name: "deepseek-v4-pro"
  parameters:
    temperature: 0.7
    max_tokens: 8192
```

### 三种格式的分工原则

| 格式 | 适用场景 | 示例 |
|------|---------|------|
| **Markdown** | 需要直接阅读/编辑的长文本 | 章节正文、世界观描述、角色传记 |
| **YAML** | 半结构化、人可读可编辑 | 大纲节点、角色属性、项目配置 |
| **JSON** | 复杂结构、前端渲染 | 人物关系图、时间线、统计数据 |

---

## 五、向量化方案

```python
class ProjectVectorStore:
    """项目级向量存储，用于语义检索"""

    COLLECTIONS = {
        "chapters":    {"chunk_size": 500, "overlap": 50},
        "characters":  {"chunk_size": 300},
        "world":       {"chunk_size": 400},
        "plot":        {"chunk_size": 200},
    }

    async def index_file(self, file_path: str):
        """文件变更时自动重新索引"""

    async def query(self, text: str, collections: list = None, top_k: int = 10):
        """语义检索：写作时自动拉取相关设定，保证前后一致"""

    async def check_consistency(self, new_content: str):
        """一致性检查：新写内容是否与已有设定矛盾"""
```

### 向量化的核心价值

- 写第 50 章时，自动检索前面提到的相关伏笔
- 创建新角色时，自动对比已有角色避免雷同
- 世界观一致性校验（新设定 vs 已有设定）

---

## 六、核心工具清单（首批）

### 工具双模式说明

每个工具返回 `ToolResult`，其中可选的 `ui_hint` 字段让前端决定如何渲染：

| 工具 | 纯 Agent 模式 | 前端模式 |
|------|:---:|:---:|
| `write_chapter` | 流式输出文本 | 编辑器实时显示 + token 级流式 |
| `relationship_graph` | 文本描述关系 | 可视化关系图（可拖拽编辑） |
| `timeline_manage` | 文本列表 | 时间线拖拽组件 |
| `plot_outline` | 树形文本 | 大纲树编辑器 |
| `create_character` | 文本描述 | 表单 + 角色卡片预览 |

### 工具列表

| 工具名 | 功能 | ui_hint | 优先级 |
|--------|------|---------|--------|
| `write_chapter` | 撰写/续写章节 | `stream:editor` | P0 |
| `edit_chapter` | 修改已有章节 | `stream:editor` | P0 |
| `create_character` | 创建角色档案 | `card:character` | P0 |
| `update_worldview` | 更新世界观设定 | — | P0 |
| `search_project` | 向量语义搜索 | `list:search_results` | P0 |
| `consistency_check` | 一致性校验 | `list:issues` | P1 |
| `plot_outline` | 生成/调整大纲 | `tree:outline` | P1 |
| `relationship_graph` | 管理人物关系 | `graph:relations` | P1 |
| `style_transfer` | 文风调整 | — | P2 |
| `timeline_manage` | 时间线管理 | `timeline:events` | P2 |
| `export_novel` | 导出 epub/pdf | — | P2 |
| `chapter_summary` | 自动生成摘要 | — | P2 |

---

## 七、开发阶段规划

### Phase 1 — 基座搭建（2-3 周）

- [x] 工具基类 & 注册系统（含 `ui_hint`）
- [x] 主 Agent 循环（含中断机制）
- [x] LLM Client 适配器（Anthropic / OpenAI / 自定义）
- [x] 项目文件系统（CRUD + asyncio.Lock）
- [x] FastAPI + WebSocket 通信层
- [x] Tauri 壳 + 基础前端框架
- [x] LLM 运行时配置 API（GET/PUT /api/config/llm）
- [x] 前端 LLM 设置面板（SettingsPanel 组件）
- [x] CSS 变量设计系统 + 前端目录结构重构
- [x] Sidebar 组件（项目选择、视图切换、连接状态）
- [x] ChatPanel 组件（事件流展示、消息输入、中断控制）
- [x] SettingsPanel 迁移 + App 重构（三视图布局）
- [x] 后端预设系统（Preset CRUD API + 内置/自定义预设管理）
- [x] 前端预设集成（usePresets hook + SettingsPanel 完整表单 + WebSocket preset_id 同步）
- [x] 主 Agent 可调用工具按预设配置：默认全部工具，可在设置页按工具勾选
- [x] 对话运行时工具选择：ChatPanel 可为本次主 Agent 请求选择可调用工具，默认全部
- [x] 对话工具选择体验打磨：已选摘要、全选/全不选/恢复默认、空选择禁发提示

### Phase 1.5 — 验收修复（基本完成，低优先级遗留）

> Phase 1 验收发现的问题。截至 2026-05-03。

**高优先级** — 全部已修复
- [x] WebSocket 健壮性：自动重连（指数退避）、错误处理、JSON.parse try/catch
- [x] 事件数组内存泄漏：保留最近 200 条
- [x] 预设更新后 Agent 缓存未失效
- [x] 预设加载/错误状态展示
- [x] 错误消息颜色区分成功/失败
- [x] 颜色对比度达标

**中优先级** — 全修
- [x] 删除预设确认对话框
- [x] presets.json 迁移到 `~/.aisync/`
- [x] 项目选择改为 Tauri 原生目录对话框
- [ ] 无 React Error Boundary（非阻塞）
- [ ] Agent 循环无迭代上限（非阻塞，Phase 3 加）

**低优先级**
- [x] OpenAI 客户端应用内 API Key
- [x] Anthropic 流式响应 await 修复
- [x] 聊天消息角色区分（用户蓝色右对齐，Agent 左对齐）
- [x] SettingsPanel 错误消息颜色
- [ ] 向量存储为空实现（Phase 3）
- [ ] 暗色模式 / 响应式布局
- [ ] 设置保存成功提示不自动消失
- [ ] vite 在 dependencies 而非 devDependencies
- [ ] 少量硬编码像素值

### Phase 2 — 核心写作（基础版完成，需 UI 打磨）

> 截至 2026-05-03，所有 Phase 2 功能已有**最低可用实现**，但 UI 质量粗陋，需持续打磨。

- [x] 工具 UI 框架：ToolDrawer + SchemaForm + `/api/tools`（基础版，表单可用但不美观）
- [x] 写作工具：write_chapter、edit_chapter、create_character、update_worldview（后端逻辑可运行，前端表单粗糙）
- [x] Markdown 编辑器：CodeMirror 6，lazy 加载（可用但无工具栏）
- [x] 对话交互：ChatPanel + 角色区分气泡 + **Agent 流式输出**（WebSocket 推送 text delta，前端实时渲染 + 闪烁光标）
- [x] 项目选择：Tauri 原生目录选择 + init 骨架生成
- [x] 文件树：**递归树形**（目录可展开/折叠，文件夹在上文件在下，自动隐藏 `.aisync`/`.vectordb`）
- [x] 对话历史：`.aisync/conversations/` CRUD（列表可折叠，首条消息自动生成标题，启动时优先恢复上次激活对话）

**待改进（Phase 2 打磨）：**
- [x] 工具系统重构第一步：Tool Descriptor + 文件影响声明 + Tool Run 记录 + 工具中心最近运行
- [x] Agent 对话 Markdown 渲染：正文支持标题、列表、引用、代码块、链接、表格
- [x] Agent 流式回复去重：`stream` 与 `agent_final` 合并展示，避免同一回复出现两次
- [x] 对话历史完整加载：历史对话不再截断为最近 200 条，实时事件仅保留高上限保护
- [x] 启动对话恢复：有历史时优先加载上次激活对话，不再误清记忆后新建
- [x] Agent 会话记忆注入：同一 conversation 的近期历史会随当前请求传回 LLM，并按消息数/字符预算裁剪
- [x] 会话摘要记忆压缩：超出近期窗口的旧消息压缩到 `.aisync/conversations/{id}.summary.md` 并随请求注入
- [x] 工具结果 AI 渲染：`ui_hint` 支持编辑器预览、搜索结果、角色卡片和 JSON fallback
- [x] 文件树默认收起：当前选中文件所在目录自动展开
- [x] 前端 chunk 优化：CodeMirror 编辑器 lazy 加载，消除 Vite 大 chunk 警告
- [x] 设置面板字段过多，应分 tab：改为设置内部侧栏分页（2026-05-15）
- [ ] ToolDrawer 内嵌编辑器高度不足
- [ ] Markdown 编辑器无工具栏
- [ ] 无暗色模式
- [ ] 截图级大纲/组织/角色管理页仍需基于工具协议继续实现

### Phase 2.5 — 下一步作业（工具系统继续破旧立新）

> 目标：从“工具中心 + 执行台”推进到“小说对象管理工作台”。优先把已有工具协议沉淀成可扩展的管理页和更可靠的 Agent 运行体验。

0. **基础信息页（P0）**
   - [x] 读取和编辑 `project.yaml` 小说名
   - [x] 自动统计已完成章节数、正文字符数、角色档案数、世界观文档数、大纲节点数
   - [x] 展示章节进度列表，并可跳转打开章节文件
   - [x] 展示世界观文件列表，并可跳转打开设定文件
   - [x] 增加创作目标、章节目标数、目标字数、当前状态、简介等项目级字段
   - [x] 展示章节进度和字数进度
   - [ ] 增加计划日期、状态流转记录和阶段里程碑

1. **大纲管理页（P0）**
   - [x] 增加 `outline_generate` 工具描述与后端实现
   - [x] 兼容读取 `plot/outline.md`，并预留 `plot/outline.json`
   - [x] 前端新增大纲管理视图：章节卡片、刷新、AI 生成/续写入口
   - [x] 工具结果支持 `list:outline_chapters` 渲染
   - [x] 侧边栏工具页由 Tool Descriptor `workspace_view` 驱动，删除工具后入口自动消失
   - [x] 前端 Workspace View Registry：工具页组件集中注册，App 不再为每个工具页硬编码渲染分支
   - [x] 大纲节点编辑：新增、删除、编辑标题和摘要
   - [x] `plot/outline.json` 结构化写入，并同步生成可读 `plot/outline.md`
   - [x] 大纲搜索与节点统计
   - [x] 大纲节点上移/下移排序
   - [ ] 大纲拖拽排序
   - [ ] `plot/outline.json` 与 `plot/outline.md` 双向差异合并

2. **章节管理页（P0）**
   - [x] `write_chapter` 通过 `workspace_view` 驱动侧边栏章节管理入口
   - [x] 读取 `chapters/**/*.md` 并聚合标题、摘要、字数和正文
   - [x] 前端新增章节列表/详情/搜索/Markdown 预览
   - [x] 支持直接编辑保存章节 Markdown
   - [x] 写作/编辑工具执行后自动刷新章节视图、基础信息统计和文件树
   - [x] 章节元数据 `ch-meta.yaml`：状态、摘要、目标字数、修订轮次
   - [x] 章节页展示单章字数目标进度
   - [ ] 章节与大纲节点关联

3. **角色管理页（P0）**
   - [x] 读取 `characters/**/profile.yaml` 和 `profile.md`
   - [x] 前端新增角色列表/详情/搜索
   - [x] `create_character` 执行后自动刷新角色视图
   - [x] `create_character` 通过 `workspace_view` 驱动侧边栏角色管理入口
   - [ ] 增加角色重复检查和 slug 冲突提示

4. **世界观管理页（P1）**
   - [x] 读取 `world/**/*.md`
   - [x] 前端新增世界观文档列表和预览
   - [x] `update_worldview` 通过 `workspace_view` 驱动侧边栏世界观整理入口
   - [x] `update_worldview` 执行后自动刷新
   - [x] 支持已有世界观文档直接编辑保存
   - [x] 支持通过 `update_worldview` 按路径新建/追加/重写世界观文档
   - [ ] 世界观主题模板和常用分类入口
   - [ ] 世界观文档重命名/删除

5. **前端工具协议整理（P1）**
   - [x] `AiRender` 抽离 `rendererRegistry`，工具结果渲染类型不再堆在 if/else 中
   - [x] `workspaceViews` 明确为前端支持的工具工作区白名单 registry
   - [x] 工具默认方案语义修正：`default_preset_id` 指向设置里的 LLM 预设，`default_agent` 废弃兼容
   - [x] 工具结果 renderer 增加更严格的数据 schema 校验

6. **Tool Run 体验增强（P1）**
   - [ ] 后端 ToolRun 增加 `running/failed/cancelled` 之外的阶段字段
   - [x] 工具中心支持手动刷新工具描述，避免新增工具后前端列表陈旧
   - [x] 工具中心支持工具搜索，并将一致性校验归入分析校验分类
   - [x] 前端运行历史支持按工具、状态筛选
   - [x] 历史记录支持复用参数重新打开工具表单
   - [x] 工具执行前增加更明确的文件影响确认
   - [x] 工具 AI 生成支持选择 LLM 方案，默认优先工具声明方案，否则使用当前设置方案
   - [x] 工具默认 LLM 方案项目级配置：`.aisync/tool_settings.json` 持久化，ToolDrawer 可保存/清除默认方案
   - [x] 工具元信息中文化：工具描述、参数说明、结果提示尽量改为中文

8. **LLM 配置体验（P1）**
   - [x] 复制已有预设配置
   - [x] 重命名已有预设
   - [x] 自动获取模型列表并提供模型名补全
   - [x] 模型选择显式下拉：模型列表可直接选择，获取失败时保留当前模型并展示错误（2026-05-16）

7. **Agent 对话稳定性（P1）**
   - [x] 后端避免对发起 WebSocket 重复广播最终消息，或前端按 `conversation_id`/内容去重
   - [x] Agent 回复重复渲染二次加固：同一轮 `stream` 与 `agent_final` 回溯合并，避免中间夹工具/状态事件时重复显示
   - [x] Agent 循环增加最大迭代上限
   - [x] 工具调用事件增加 tool name、params 摘要和执行耗时
- [x] Agent 动态反馈：检索上下文、请求模型、工具调用、完成状态和报错前台可见
- [x] Agent 任务列表：按写作、设定、角色、大纲、检索、一致性检查等请求动态生成任务模板
- [x] 一致性检查降噪：过滤结构性编号，收紧否定词与状态词匹配
   - [x] Markdown 渲染继续补齐任务列表、分割线、转义边界
   - [x] Markdown 嵌套列表正修：有序列表内嵌无序列表不再导致后续序号重置为 1（2026-05-15）
   - [x] DeepSeek thinking 模式兼容：工具调用多轮时保留并回传 `reasoning_content`，避免 400 报错（2026-05-15）
   - [x] DeepSeek 思考模式可选：设置页开关会映射到 DeepSeek V4 `thinking enabled/disabled` 请求参数（2026-05-15）
   - [x] DeepSeek thinking+tools 加固：禁用该场景流式并多路径提取 `reasoning_content`，降低续轮 400 风险（2026-05-16）
   - [x] 聊天历史与设置页滚动修复：限制抽屉/设置页高度并启用内部滚动（2026-05-16）
   - [x] 工具结果默认折叠：`tool_result` 不再同时显示原始内容和 `ui_hint` 结构化列表，减少 Agent 对话噪音（2026-05-16）
   - [x] Agent 任务面板保留本轮任务列表：避免新会话/历史刷新后任务流程栏被 liveEvents 重置吞掉（2026-05-16）
   - [x] Agent 任务面板兜底流程：缺少 `agent_task_list` 时根据状态事件合成四步流程，避免安装版任务栏空白（2026-05-16）
   - [x] Markdown 流式标题容错：修复输出停在 `### ` 等半截标题时解析器死循环并触发 `RangeError: Invalid array length`（2026-05-16）
   - [x] `<think>` 思考块渲染：MarkdownView 识别完整/流式半截 `<think>`，默认折叠成一行，展开后显示内容（2026-05-18）
   - [x] 长对话历史分页/窗口渲染，避免超长 DOM 影响滚动性能
   - [x] 超长会话摘要记忆：旧消息压缩为 conversation summary，避免仅依赖近期窗口
   - [x] 用户可见记忆状态：显示本次注入了多少条历史、是否已有摘要、是否后台更新
   - [x] 摘要记忆后台化：摘要更新不阻塞用户主回复
   - [x] 摘要质量评估：为小说设定、用户偏好、未完成事项增加检查样例
   - [x] 摘要质量离线测试：覆盖关键设定、用户偏好、未完成事项和空摘要失败场景
   - [x] 对话消息选择：选择模式、全选可见、复制、引用到输入
   - [x] 单条消息快捷操作：复制、引用、加入/取消选择
   - [x] 引用输入轻量化：引用只插入短摘录，不再把整条长消息搬进输入框
   - [x] 引用上下文框：引用显示为输入框上方摘要，发送时附带上下文，不污染输入框
   - [x] AI 选项按钮：识别“可选方案/下一步选项”后的编号列表，渲染为可点击选择并继续对话
   - [x] 安装版后端端口动态分配：前端启动时读取真实 API 地址，不再固定 `8000`
   - [x] 安装版隐藏后端 Python 控制台窗口，关闭前端时只终止 Tauri 管理的后端进程
   - [x] 轻量版检查更新：设置页读取 GitHub Releases latest，提示新版并打开安装包/发布页
   - [x] 2026-05-13 安装器覆盖安装加固：NSIS 安装/卸载前清理旧版 AiSync 与随包 Python 进程，避免 runtime 文件锁定
   - [x] 2026-05-13 整合项目报告与规划到本地汇总文件 `项目.local.md`
   - [x] 2026-05-13 设置区重构为分区卡片，更新按钮切换为桌面外部打开，README 许可改为商用需授权

### Phase 3 — 智能增强（2-3 周）

- [x] 向量化引擎集成：本地分块索引、内容签名失效、`.aisync/vector_index.json` 持久化
- [x] 语义/文本混合检索基础版：`ProjectVectorStore.query` + `search_project` 真实检索
- [x] Agent 上下文自动注入：主 Agent 请求前从项目索引检索相关片段
- [x] 向量索引前端状态页：重建索引、查看文件数/片段数、分组分布和检索测试
- [x] 章节工作流接入向量辅助：当前章节相关设定检索、一致性检查工具预填、索引重建入口
- [x] 可选接入真实 embedding：OpenAI-compatible embeddings 写入本地索引，并与词面检索混合评分
- [x] 可选接入 ChromaDB 后端：配置切换到 `chroma` 时写入持久化集合，缺依赖自动回退本地索引
- [x] 一致性校验工具基础版：基于索引检索相关设定，输出数值/状态/否定表述风险
- [x] 一致性校验前端渲染：`list:issues` 问题清单
- [x] 一致性校验 LLM 二次判定与修复建议：工具 `AI 生成` 路径输出结构化问题和 suggestion
- [ ] 人物关系图可视化（前端从 `ui_hint` 渲染）

### Phase 4 — 体验打磨（2 周）

- [ ] 大纲可视化编辑
- [ ] 时间线工具
- [ ] 导出功能
- [ ] 文风分析 & 调整
## Phase 4.5 — 打包与客户机运行时

- [x] 开发态 `npm run tauri dev` 不再打包后端，直接启动源码后端
- [x] 发布态 `npm run tauri build` 不再默认跑 PyInstaller
- [x] 发布包改为携带干净 `backend-src` 源码资源
- [x] 增加轻量 Python runtime 准备脚本，自动下载/校验 Python runtime
- [x] 准备内置轻量 Python：`frontend/src-tauri/resources/runtime/python/python.exe`
- [x] 安装版优先使用随包 Python 直接启动后端
- [x] 安装版随包 Python 启动失败时，回退到自动 venv
- [x] venv 回退会自动创建 `%APPDATA%\com.aisync.app\runtime\.venv`
- [x] venv 回退会自动 `pip install <backend-src>` 并用 venv 启动后端
- [x] 诊断信息加入 Python 候选、源码候选、setup 日志
- [x] 安装包清单确认不再包含 `aisync-backend.exe`、`__pycache__`、`.pyc`
- [x] 增加离线 wheelhouse 准备脚本：`scripts/prepare_runtime_wheels.ps1`
- [x] 安装版发现 `aisync-backend` wheel 时优先从 wheelhouse 离线安装
- [~] 离线 wheelhouse 暂缓：当前默认交付不携带 `runtime/wheels/`，避免安装包膨胀；仅在需要无网 venv 回退时启用
- [x] 安装版资源清单包含内置 Python、后端源码和依赖
- [x] 安装版真实冷启动验收：端口清空后从已安装目录启动，确认随包 Python 后端可启动且 `/health` 通过
- [x] 移除旧 PyInstaller sidecar 启动回退逻辑，避免升级安装残留的旧 exe 被误启动
- [x] Windows 启动后端不弹空控制台：Python/venv 检查和后端进程使用隐藏窗口启动
- [x] 安装版前端快速显示：Tauri setup 不再同步等待后端启动，后端改为后台线程启动
- [x] 关闭前端清理后端：窗口关闭事件显式终止托管后端进程
- [x] 清理旧 PyInstaller 构建脚本 `scripts/build_backend.ps1`
- [x] 2026-05-14 安装器升级回归：版本升至 `0.1.7`，NSIS 安装前清理旧主程序/卸载器/backend-src/runtime，覆盖安装可替换旧 `aisync.exe`
- [x] 2026-05-14 安装版启动回归：已安装目录启动显示 `version=0.1.7`，随包 Python 后端健康，关闭主窗口后无安装目录下的 `aisync/pythonw` 残留进程
- [x] 2026-05-14 README 拆分：README 改为项目入口文档，新增 `技术细节.md` 承接目录结构、运行时、日志、API、工具和向量细节

---

## 八、连续作业队列（从 2026-05-14 起）

> 原则：之后用户说“继续”时，默认按本队列从上往下推进，不再每次停下来问下一步。除非用户插入 bug、打包故障或明确改方向。

### A. 发布链路闭环（P0）

目标：让项目从“本机能打包”走到“GitHub Release 可发版、客户端可发现、用户能安装/升级、出错能定位”。

- [x] A1. 建立发布检查清单：版本号、构建命令、产物路径、安装覆盖验证、启动验证、关闭进程验证、日志检查
- [x] A2. 增加 `发布流程.md`：从改版本号到 GitHub Release 上传安装包的完整步骤
- [x] A3. 检查更新 UI 再打磨：明确当前版本、最新版本、检查时间、下载资产名、失败原因
- [x] A4. GitHub Release asset 选择规则：优先 NSIS `*-setup.exe`，无匹配时退回发布页
- [x] A5. 下载/打开失败前台可见：Tauri `open_external_url` 报错要显示在设置页，而不是静默失败
- [x] A6. 生成安装包后自动输出诊断摘要：版本、安装包路径、文件大小、runtime 是否存在
- [x] A7. 安装版诊断页入口：在设置页增加“复制诊断信息”和“打开日志目录”
- [x] A8. 整理旧版清理说明：哪些可删、哪些是用户数据不能删
- [x] A9. 决定 MSI 是否继续作为发布产物；如 NSIS 为主，README/发布流程明确写 NSIS 优先
- [x] A10. 发布前本机端到端验收脚本化：覆盖安装、启动、健康检查、关闭进程检查
- [x] A11. 检查更新版本比较修复：从 release tag/name 中提取数字版本，避免同号或更低版本误报新版本（2026-05-15）

### B. Agent 运行可靠性（P0）

目标：Agent 不再像黑箱；每次任务能看见状态、失败原因、可恢复入口和历史记录。

- [x] B1. Agent run 记录持久化：每轮对话生成 run_id，记录开始/结束、状态、错误、工具调用摘要
- [x] B2. 前端显示当前 run 状态：准备上下文、请求模型、执行工具、等待工具、整理回复、完成/失败
- [x] B3. 失败恢复入口：失败消息提供“复制错误”“重试本轮”“带错误继续问”
- [x] B4. 中断状态收敛：中断后清理 pending 状态，避免旧会话打开仍显示等待响应
- [x] B5. WebSocket 重连后的状态恢复：重新拉取当前 conversation、active run、最近事件
- [x] B6. 空 assistant message 防御：发送给 LLM 前过滤或修复无 content/tool_calls 的 assistant 消息
- [x] B7. 工具调用超时与错误分级：用户取消、工具失败、模型失败、网络失败分开显示
- [x] B8. Agent 事件日志压缩：长期保留关键事件，流式 token 不无限写入历史
- [x] B9. 对话记忆状态更可操作：显示摘要更新时间、摘要长度、近期消息窗口
- [x] B10. 加一组后端回归测试覆盖重复回复、空 assistant、工具失败、恢复历史
- [x] B11. Agent run 报告位置修正：`agent_run` 不再显示在聊天气泡内，改在 Agent 任务区显示运行摘要和工具调用报告（2026-05-15）

### I. Agent 工作流与提示词管理（P0/P1）

目标：把当前“单主 Agent 一轮调用工具”的模式，升级为可见、可暂停、可换模型、可人工确认的写作工作流。主 Agent 负责理解用户目标和编排步骤，具体写作、检查、整理、特殊片段生成由受控的步骤/子 Agent 执行；工具继续作为稳定、可审计的文件和项目操作层。

问题判断（2026-05-19）：

- 当前架构仍能支撑普通对话和简单工具调用，但写章节这类长流程越来越不适合“一口气执行到底”。
- 用户希望在写章节时追加临时提示词、指定某一段用特定模型/预设、在中途确认或改方向；这些需求属于“步骤级控制”，不是单个工具参数能自然解决的。
- 不建议直接做完全自由的 Agent 集群；更稳妥的路线是先做 WorkflowRun/WorkflowStep，再把专业子 Agent 作为受控执行器接入。
- 当前提示词不够透明：默认 system prompt 在 `backend/app/agent.py`，预设里可覆盖 system prompt，运行时还会自动注入会话记忆、向量检索上下文、伏笔上下文和工具描述。用户很难看到本轮到底给模型喂了什么。

设计方向：

```text
用户请求
  ↓
主 Agent / 编排器
  ↓
WorkflowRun
  ↓
WorkflowStep[]
  ├─ 每步可指定 LLM 预设
  ├─ 每步可挂载提示词包
  ├─ 每步可声明输入上下文和输出目标
  ├─ 每步可要求人工确认
  └─ 每步输出优先进入 temp/drafts，确认后再写正式文件
```

提示词技术边界（2026-05-19，详见 `项目文档/提示词技术.md`）：

- 不采用“万物皆 prompt”的一锅管理。
- 人物、伏笔、世界观、大纲、章节是项目对象和结构化数据，不长期复制进提示词。
- Prompt Pack 只管理文风、写作规则、输出格式、禁忌、偏好、特殊模型片段规则等模型行为。
- Context Pack 管理本次任务需要注入哪些结构化上下文，如本章人物、相关伏笔、当前大纲节点、相关世界观。
- Workflow Step 管理执行顺序、步骤级模型、Prompt Pack、Context Pack、输出位置和人工确认点。
- 保留参考方案里的集中管理、模板语法、可启用/停用、可复用、可按阶段选择，但不接受“所有内容都写成提示词”。

执行拆分：

- [x] I1. 建立提示词来源审计：Agent run 记录本轮使用的 system prompt 来源、记忆摘要、近期消息数、向量上下文数量、伏笔上下文是否注入、可用工具列表（2026-05-19）
- [~] I2. 前端增加“提示词预览/上下文预览”：暂缓，不优先暴露完整 prompt；先做可管理、可保存、可引用的提示词包。
- [x] I3. 建立提示词包数据结构：全局/项目级 prompt packs，字段包含名称、分类、适用阶段、正文、变量、默认启用状态（2026-05-19）
- [x] I4. 提示词包管理页：新增、编辑、复制、禁用、删除；支持写作/规划/润色/检查/特殊风格等分类（2026-05-19）
- [x] I5. 对话阶段 Prompt Pack 自动注入：已启用且适用阶段包含 `chat` 的提示词包会自动进入主 Agent 对话，不要求每轮手动选择；临时追加入口后续再做（2026-05-20）
- [x] I6. 写章节/编辑章节工具接入 Prompt Pack：`chapter_draft` 阶段自动作用于 `write_chapter`，`revision` 阶段自动作用于 `edit_chapter`；ToolRun 记录提示词包阶段、数量和名称。临时额外提示词改留到 WorkflowStep 人工干预里实现，不放进每轮普通对话。（2026-05-20）
- [x] I6.1. 项目级默认启用关系：设置页提示词管理支持“沿用全局启用/只使用本项目勾选项”；项目设置写入 `.aisync/prompt_pack_settings.json`，主 Agent 对话和章节工具都会按项目设置过滤 Prompt Pack，避免全局文风影响所有项目。（2026-05-20）
- [x] I7. 定义 WorkflowRun/WorkflowStep 持久化结构：新增 `.aisync/workflows/{run_id}.json`、`/api/workflows`、后端 WorkflowRunStore 和前端“工作流”页；支持记录状态、步骤、模型预设、提示词包、上下文包、输出位置、错误和人工确认点。当前只完成骨架和可见状态，执行器进入 I8。（2026-05-20）
- [x] I8. 写章节工作流最小版：工作流页可手动推进下一步，当前支持检索上下文 → 生成章节计划 → 用户确认暂停 → 生成草稿并写入 `temp/drafts/{run_id}.md`。暂未做自动连续执行、步骤编辑、分段长文、润色检查和正式章节写入确认。（2026-05-20）
- [x] I8.1. 工作流步骤编辑基础版：工作流步骤可编辑步骤模型、输出路径、额外提示词和步骤输出；计划/草稿执行器会读取 `step.input.extra_prompt`，用户可在确认前改计划并继续执行。提示词包勾选 UI 暂未做，后续纳入 I10 人工干预增强。（2026-06-27）
- [x] I8.1a. 本地 Web-first 运行模式：FastAPI 托管 `frontend/dist`，前端支持同源 `/api` 与 WebSocket，根目录新增 `npm run web`，浏览器模式可手动输入项目绝对路径；默认开发端口改为较少见的 `27631`。（2026-06-27）
- [x] I8.1b. 项目库导入/导出：后端新增项目库列表、新建、zip 导入/导出和 zip 路径安全校验；前端侧边栏支持项目选择、新建、导入、导出，Web 模式不再把手填路径作为主流程。项目库存放于用户数据目录，不放安装目录。（2026-06-27）
- [x] I8.1c. 项目库管理补完：支持项目重命名和删除；重命名更新 `project.yaml` 元数据，删除仅允许项目库内项目，外部文件夹不进入删除流程。（2026-06-27）
- [x] I8.1d. 首次启动项目引导：无当前项目时主界面展示新建、导入、打开外部文件夹和刷新项目库入口，避免 Web 模式打开后只有空工作区。（2026-06-27）
- [ ] I8.2. 工作流分段写作：把正文草稿拆成多个场景/段落 step，降低长文输出失败率。
  - 当前决策（2026-06-28）：不把分段工作流作为正文写作主入口，避免把简单写章流程复杂化。正文写作优先走主 Agent 对话或 `write_chapter` 工具；只有当长章节输出经常中断、模型上下文不足或需要多模型分工时，再启用“章节计划 -> 场景拆分 -> 分段草稿 -> 合并润色 -> 用户确认写入正式章节”的备用链路。
- [ ] I8.3. 草稿转正式章节：从 `temp/drafts` 写入 `chapters/**.md` 前展示 diff 和文件影响确认。
- [ ] I9. 步骤级 LLM 预设选择：允许某一步使用 Grok/Claude/DeepSeek 等不同预设，默认继承当前对话预设。
- [ ] I10. 人工干预机制：步骤暂停、编辑步骤提示词、重试当前步骤、跳过步骤、把某步输出改写后继续。
- [ ] I11. 专业子 Agent 受控接入：章节规划 Agent、正文写作 Agent、风格润色 Agent、一致性检查 Agent、特殊片段 Agent。
- [ ] I12. 工具层保持审计边界：正式文件写入仍走工具和确认；自由草稿优先写入 `temp/drafts`。

### C. 小说对象工作台（P0/P1）

目标：把“章节/角色/世界观/大纲”做成真正面向小说对象的工作台，而不是文件列表套表单。

- [x] C1. 章节与大纲节点关联：章节元数据记录 outline node id，大纲页可看到对应章节状态（2026-05-15）
- [x] C2. 大纲拖拽排序：前端拖拽重排后写回 `plot/outline.json` 并同步 markdown（2026-05-15）
- [x] C3. 大纲节点状态：未开始/草稿/修订/完成，联动基础信息统计（2026-05-15）
- [x] C3.5. 伏笔管理基础版：`plot/foreshadows.json`、伏笔工作区、状态/重要性/章节/大纲关联、基础信息统计（2026-05-15）
- [x] C3.6. 大纲原文/结构分离：Markdown 原文完整展示，结构视图只使用 `outline.json`，支持从原文严格导入章节节点（2026-05-16）
- [x] C4. 世界观文档重命名/删除：带文件影响确认，更新文件树和索引（2026-05-15）
- [x] C5. 世界观主题模板：地理、历史、势力、技术/魔法、规则、年表（2026-05-15）
- [ ] C6. 角色 slug 冲突提示：创建角色前检查目录/名称重复
- [ ] C7. 角色重复检查：按姓名、别名、设定关键词做轻量相似提示
- [ ] C8. 角色关系数据结构：定义 `characters/relationships.json` schema
- [ ] C9. 人物关系图可视化：从 `ui_hint=graph:relations` 渲染，支持节点/边查看
- [ ] C10. 基础信息页增加计划日期、状态流转记录和阶段里程碑
- [~] C11. 伏笔智能提醒：写章节前检索相关伏笔，提示未回收/可回收伏笔
  - [x] C11.1 章节页相关伏笔提示：按埋设章节、回收章节、关联大纲节点和相关文件分组展示（2026-05-15）
  - [x] C11.2 写作工具/Agent 上下文注入相关伏笔（2026-05-15）
  - [x] C11.3 伏笔提醒可解释化：显示命中原因和建议动作（2026-05-15）

### D. 工具系统继续破旧立新（P1）

目标：工具成为可发现、可配置、可复用、可审计的能力单元，前端呈现尽量由工具描述符驱动。

- [ ] D1. ToolRun 增加阶段字段：`preparing/running_model/running_tool/writing_files/indexing/completed`
- [ ] D2. ToolRun 支持取消标记：不能强杀时也能阻止后续步骤并标记 cancelled
- [ ] D3. 工具参数 schema 增强中文校验错误
- [ ] D4. 工具中心按“写作/设定/角色/检索/校验/导出”分组展示
- [ ] D5. 工具详情页显示：能力说明、读写范围、默认 LLM 方案、最近运行、前端页面入口
- [ ] D6. 工具执行结果支持“写入文件前预览 diff”模式
- [ ] D7. 工具描述符增加 examples，供前端展示和 Agent prompt 使用
- [ ] D8. 工具前端 renderer 缺失时显示可读 fallback，并提示缺少 renderer 类型
- [ ] D9. 工具工作区入口排序策略：基础功能在前，工具页单独分组
- [ ] D10. 工具权限按会话持久化：本次对话选择的工具集合可保存为偏好

### E. 向量化与一致性增强（P1）

目标：减少误报，提高检索可解释性，把向量索引用到真实写作流程里。

- [ ] E1. 索引详情页显示每个文件的 chunk 数、更新时间、签名、索引状态
- [ ] E2. 检索结果增加命中原因：关键词命中、语义命中、混合分数拆分
- [ ] E3. 一致性检查数字误报继续降噪：章节编号、标题编号、列表编号、百分比范围分开处理
- [ ] E4. 一致性检查按类型分组：数值、时间线、人物状态、地理位置、设定冲突、语气不确定
- [ ] E5. LLM 二次判定加入引用片段和置信度，避免只给泛泛建议
- [ ] E6. 写章节前自动拉取“本章相关设定包”：角色、地点、伏笔、世界规则
- [ ] E7. 大纲节点关联检索：选中大纲节点时显示相关章节/设定/角色
- [ ] E8. 索引后台刷新：文件保存后延迟增量重建，而不是全量手动
- [ ] E9. Chroma 缺依赖/损坏时的前台诊断和一键回退 local
- [ ] E10. 增加向量索引后端测试：分块、签名失效、混合排序、一致性降噪

### F. 编辑器与写作体验（P1/P2）

目标：把基础 Markdown 编辑器变成能支撑长篇写作的日常工具。

- [ ] F1. Markdown 编辑器工具栏：标题、粗体、斜体、引用、列表、链接
- [ ] F2. 编辑器保存状态：未保存、保存中、已保存、保存失败
- [ ] F3. 文件树搜索与过滤
- [ ] F4. 文件树新建/重命名/删除文件夹和 markdown 文件
- [ ] F5. 最近打开文件列表
- [ ] F6. 章节页增加字数趋势和修订轮次记录
- [ ] F7. 写作目标提醒：按章节目标/总字数目标显示差距
- [ ] F8. 导出功能基础版：导出全部章节为单个 Markdown
- [ ] F9. 导出 epub/pdf 方案评估与最小实现
- [ ] F10. 暗色模式与高对比模式

### H. 自由文件区（P1）

目标：在每个小说项目内建立 `temp/` 自由工作区，让 Agent 可以安全地读写临时资料、外部同步内容和中间输出，而不污染正式章节/设定/角色目录。

设计边界：

- `temp/` 是项目内目录：`<project>/temp/`
- Agent 对 `temp/**` 拥有完整读写能力
- `temp/` 默认不计入章节、世界观、角色等正式统计
- 从 `temp/` 移入正式目录必须经过用户确认
- 外部项目资料先复制/同步到 `temp/inbox/`，Agent 不直接任意读写外部路径
- 向量索引可读取 `temp/`，但必须带 `source=temp` 标记，检索和一致性判断要区分临时资料与正式资料

建议目录：

```text
temp/
  inbox/      # 外部项目或资料同步进来的原始副本
  drafts/     # Agent 生成的临时草稿、备选方案
  exports/    # 临时导出和整理结果
  notes/      # 自由笔记
  .aisync-temp.json
```

实施拆分：

- [x] H1. 项目初始化创建 `temp/inbox`、`temp/drafts`、`temp/exports`、`temp/notes`（2026-05-18）
- [x] H2. 文件树支持 `temp/` 自由区展示，并用“临时/自由”标记区分正式项目文件（2026-05-18）
- [x] H3. 复用现有文件 API 支持 `temp/**` 新建、编辑、重命名、删除（2026-05-18）
- [ ] H4. Agent 工具增加 `temp_file_list/read/write/delete`，限定根目录为当前项目 `temp/`
- [ ] H5. 主 Agent system prompt 明确：临时资料优先写入 `temp/`，正式目录写入仍按工具约束
- [ ] H6. 增加“归档到正式项目”动作：从 `temp/` 移动到 `chapters/world/characters/plot` 前展示确认和目标类型
- [ ] H7. 外部文件/文件夹同步到 `temp/inbox/`，保留来源记录到 `.aisync-temp.json`
- [ ] H8. 向量索引支持 `source=temp`，检索结果显示“临时资料”，一致性检查默认降低临时资料权重
- [ ] H9. 自由区清理工具：按时间/来源/未引用状态清理临时文件
- [ ] H10. 增加后端测试：路径越界防护、temp 读写、归档确认、索引 source 标记

当前决策（2026-05-18）：

1. 先做项目内 `temp/` 最小闭环，不开放任意外部路径读写。
2. 第一批只实现 H1-H3：初始化目录、文件树标记、前端可打开编辑常见文本文件。
3. 第二批再做 H4-H5：Agent 专用 temp 工具和 system prompt 约束。
4. H6-H8 暂缓到最小闭环稳定后再做，避免一开始把“临时区、正式归档、向量索引、外部同步”全混在一起。
5. Agent 后续写入自由区时默认使用 `temp/drafts/` 或 `temp/notes/`；外部导入材料默认进入 `temp/inbox/`。
6. 安全收紧（2026-05-19）：公开文件 API 和自由区操作只允许 `.md/.txt/.json/.yaml/.yml/.csv`，屏蔽 `.aisync`、`.vectordb`、点号内部路径和 `temp/.aisync-temp.json`。

### G. 配置、许可与项目治理（P1/P2）

目标：项目更像可发布产品，减少维护和授权上的含糊地带。

- [x] G1. 新增正式 `LICENSE` 文件：采用 PolyForm Noncommercial License 1.0.0，商用/分发/嵌入需书面授权（2026-05-19）
- [x] G2. README 许可说明链接到 `LICENSE`（2026-05-19）
- [x] G3. 第三方依赖许可说明：区分 AiSync 自身授权和依赖授权（2026-05-19）
- [ ] G4. `.gitignore` 再核对：runtime、target、local 文档、项目数据、日志全部排除
- [ ] G5. `.codexignore` 核对：避免 Codex 读写大体积 runtime/target/node_modules
- [ ] G6. 技术细节文档持续更新：每次打包策略变化后同步
- [x] G6.1. 新增 Agent 能力文档：`项目文档/Agent能力.md` 记录当前对话、记忆、工具、对象工作台、向量和边界（2026-05-15）
- [x] G6.2. 打包入口收敛：仓库根目录 `npm run build` 一键进入 Tauri 发布打包，`build:frontend` 保留为前端静态构建（2026-05-16）
- [x] G6.3. 发布包一致性校验：打包后自动确认 release exe 嵌入当前 dist 资产；发布产物收敛为 NSIS 并升至 `0.1.8` 避免同版本覆盖误判（2026-05-16）
- [x] G6.4. 新增打包链路文档：`项目文档/打包链路.md` 说明 dev、发布、安装态资源来源和一致性校验（2026-05-16）
- [x] G6.5. 安装版启动体验：release 主程序隐藏 Windows 空终端，前端脚本加载和后端等待阶段增加启动动画（2026-05-16）
- [x] G6.6. 构建产物清理脚本：`scripts/clean_build_artifacts.ps1` 支持 DryRun、保留最新 NSIS、清理旧安装包/MSI/wix/dist/log/runtime-cache（2026-05-18）
- [x] G6.7. 启动等待页动态反馈：轮换启动文案、显示已等待秒数，长等待时提供复制诊断和打开日志入口（2026-05-19）
- [ ] G7. 建立 `CHANGELOG.md`：按版本记录面向用户的变化
- [ ] G8. 建立 issue 模板：bug、功能请求、打包问题
- [ ] G9. 清理旧文档删除状态：确认 `规划.md`/`项目完成报告.md` 是否正式迁移为 `.local.md`
- [ ] G10. 发布前 repo 干净度检查：区分应提交文件和本地文件

### 默认执行顺序

1. 先做 A1-A3：发布链路清单、发布流程文档、更新 UI 反馈。
2. 然后做 B1-B4：Agent run 状态和失败恢复，解决“看起来卡住”的体验。
3. 插入做 I1/I3/I4/I5/I6：提示词来源审计、提示词包结构、管理页、对话和章节工具按阶段自动引用，先解决“想改文风就能在固定位置改并保存”。
4. 再做 I6.1/I7-I10：项目级 Prompt Pack 默认启用、WorkflowRun/WorkflowStep、写章节工作流最小版、步骤级模型选择和人工干预。
5. 再做 C6-C10：角色关系、基础信息里程碑等小说对象工作台补全。
6. 插入做 H4-H8：Agent 专用自由区工具、正式归档确认和 `temp` source 向量标记。
7. 接着做 E1-E4：向量索引可解释与一致性降噪，并把 `temp` source 纳入检索解释。
8. 穿插做 G4-G10：ignore、CHANGELOG、issue 模板和发布前 repo 干净度，保证发布不埋雷。

## 2026-06-27 Agent 对话噪声折叠

- [x] Agent 任务面板默认收起，仅保留当前状态摘要。
- [x] 运行状态、记忆注入、工具开始/完成等细节从主聊天流移入“运行细节”折叠块。
- [x] 旧的未收敛 Agent run 提示支持持久忽略，避免刷新后反复出现同一条确认提示。

## 2026-06-27 Agent 状态收敛

- [x] 后端维护进程内活跃 Agent run 集合，避免误把真正运行中的任务收敛。
- [x] `latest run` 接口会把已无活跃任务的旧 `running` 记录自动标记为 `interrupted`，并同步修正会话状态。
- [x] 增加测试覆盖旧运行记录收敛与活跃运行保护。

## 2026-06-27 Agent 失败诊断与 token 统计

- [x] 失败原因不再允许空白：异常没有文本时记录异常类名，前端可看到具体失败原因。
- [x] 同一 conversation 增加单飞保护：已有 Agent 正在运行时拒绝重复发送，避免重复请求和额外 token 消耗。
- [x] Agent 请求尊重 LLM 预设/设置里的最大输出 token，不再默认按过大的 `16000` 兜底。
- [x] Agent run 的 `prompt_audit.usage` 记录模型请求数、工具调用数、真实 token（接口提供时）和估算 token（流式/兼容接口兜底）。
- [x] 前端 Agent 任务栏显示 token/模型请求/工具调用摘要，详细工具与审计信息默认折叠。

## 2026-06-27 Agent 运行策略与中断修复

- [x] 对话区增加“一致性审查”开关：关闭后本轮 Agent 可用工具会排除 `consistency_check`，避免频繁误报和额外工具调用。
- [x] 修复中断命中错误 Agent 实例：WebSocket 临时 Agent run 会登记真实运行实例，REST/WebSocket 中断优先按 conversation/run 命中当前任务。
- [x] 增加测试覆盖运行中 Agent 实例登记、命中中断和结束清理。

## 2026-06-28 工具级 LLM 路由

- [x] 抽出工具默认预设读取模块，工具中心和主 Agent 共用 `.aisync/tool_settings.json`。
- [x] 主 Agent 自动调用工具时会优先读取工具默认 LLM 方案；存在独立方案且工具支持 `invoke` 时，工具内部 AI 审查/生成使用该方案。
- [x] 没有工具默认方案、默认方案等于主 Agent 当前方案、或工具未返回 invoke 结果时，自动回退原本的 `execute` 行为。
- [x] Agent run 工具调用记录补充 `mode` 和 `preset_id`，前端任务报告能看到工具是否使用独立 AI 方案。

## 2026-06-28 对话历史定位修复

- [x] 切换/加载对话历史后自动定位到最新消息，避免从顶部一路手动翻到底。
- [x] 历史加载后同步滚动并在下一帧兜底滚动，规避消息 DOM 高度尚未稳定导致的定位失败。

## 2026-06-28 一致性检查降噪

- [x] 一致性检查关键词重叠不再把 Markdown 编号、纯数字、`ai` 等泛词作为有效线索。
- [x] “存在高相关设定”提示需要真实中文设定词/实体重叠，避免只因列表编号相同就刷屏。
- [x] 中文 token 增加 2-4 字片段识别，保留“林铎/黑雨城”等真实实体相关提示。

## 2026-06-28 角色归档/删除基础版

- [x] 新增可恢复角色归档能力：角色档案从 `characters/{slug}` 移动到 `temp/archive/characters/{slug}-{timestamp}`，不做不可逆硬删。
- [x] 新增 `character_manage` 工具，当前支持 `archive` 动作，供 Agent 处理旧名残留、重复角色和废弃角色。
- [x] 角色管理页增加“归档角色”和“管理工具”入口，归档前要求输入原因确认。
- [x] 归档后刷新角色页、基础信息、文件树和向量索引。

## 2026-06-30 上下文窗口档位

- [x] LLM 预设新增“上下文窗口”选项：经济、标准、长上下文、最高上下文。
- [x] 主 Agent 按预设窗口调整同对话近期历史、历史字符预算、单条历史截断长度、向量检索数量和单片段长度。
- [x] Agent run 审计记录本轮上下文档位、注入历史数量、检索片段预算和估算 token，前端任务报告可见。
- [x] 最高上下文不会改变输出 `max_tokens`，也不保证模型真实支持 200k；它只是尽量扩大输入上下文注入预算。

## 2026-06-30 工作流基础能力与示例

- [x] 工作流模板 API：内置“安全章节草稿”“草稿转正式章节”“章节修订草稿”三个模板，前端工作流页可直接从模板创建。
- [x] 工作流增加 `write_file` 执行器：只允许从 `temp/drafts/*.md` 写入 `chapters/**/*.md`，用于草稿确认后转正式章节。
- [x] 提示词示例 API：内置“半文半白叙事”“废土冷峻文风”“章节草稿约束”“设定一致性复核”，设置页可一键从示例创建提示词包。
- [x] 示例数据增加后端测试，防止后续功能退化到“给不出示例”。
- [x] 提示词示例前端改为默认可见卡片，不再藏在折叠区。
- [x] 提示词示例交互改为“选择预览 -> 点击创建”，避免点击示例即创建，也修复切换示例后仍创建旧示例的混乱。
- [x] 工作流记录支持删除；工作流支持创建空白流程、添加步骤、删除步骤，补齐最基础的自定义能力。
- [x] 工作流支持编辑标题/摘要、重试单步、跳过单步，失败后可从指定步骤继续推进。
- [x] 系统提示词管理分层：不把系统提示词直接混入普通 Prompt Pack；新增“系统规则/Agent 行为规则”管理区，和文风/写作提示词分层展示、审计和启停。（2026-07-03）

## 2026-07-03 系统提示词管理分层

- [x] 后端新增项目级系统规则存储和 API：`/api/system-rules` 读取/保存 `.aisync/system_rules.json`。
- [x] 主 Agent 创建时合成 system prompt：保留默认/预设级 System Prompt 兼容，同时按项目规则开关追加“项目系统规则”。
- [x] Agent 缓存键纳入系统规则版本，避免保存后仍沿用旧 Agent 实例。
- [x] Agent run 审计记录 system prompt 来源、base source、项目规则是否注入和字符数。
- [x] 设置页新增“系统规则”分页，和“提示词管理”分离；普通 Prompt Pack 继续用于文风、写作、润色、检查、特殊片段。
- [ ] 后续可继续做系统规则示例模板、规则版本历史、运行前 prompt 预览与 diff。

## 2026-07-03 工作流步骤级提示词包

- [x] 工作流步骤编辑器新增提示词包勾选，保存到 `WorkflowStepRecord.prompt_pack_ids`。
- [x] 工作流执行器支持步骤级注入：有 `prompt_pack_ids` 时只使用所选 Prompt Pack；未指定时回退阶段默认。
- [x] 阶段默认映射：计划步骤 `chapter_plan`，草稿步骤 `chapter_draft`，修订步骤 `revision`。
- [x] 步骤输出记录实际注入的提示词包元数据，前端步骤卡片显示“已注入”摘要。
- [x] 轻量可用增强：步骤编辑器支持“套用推荐/清空”，按步骤类型推荐 Prompt Pack；执行后显示注入模式、阶段、数量、是否含额外要求和 prompt 字符数。
- [ ] 后续可做工作流模板内置推荐提示词包、按步骤类型自动建议提示词包、执行前预览最终 prompt。

## 2026-07-04 章节与世界设定 UI 轻改

- [x] 章节管理页前端重整：新增概览条、章节索引头、状态标签、详情指标和正文阅读区边界。
- [x] 章节列表强化创作对象信息：标题、状态、字数、修订轮次、大纲关联优先于文件路径。
- [x] 世界观整理页前端重整：新增概览条、设定资料库头、文档分类标签、详情指标和 Markdown 阅读区边界。
- [x] 本轮不改后端数据结构和工具逻辑，只调整信息架构与视觉层级。
- [x] 补充章节状态筛选和世界设定类型筛选，均为前端本地筛选，不改后端。
- [x] 补充章节/世界设定本地排序：章节支持顺序、字数、状态、修订轮次；世界设定支持路径、类型、字数、标题。
- [ ] 后续继续做对象关联摘要和更明确的保存状态；最近打开暂缓。

## 2026-07-04 Agent 中断与一致性审查默认策略

- [x] 对话区“一致性审查”默认关闭，本轮 Agent 默认不暴露 `consistency_check`，需要时再手动打开，减少误触发和额外 token 消耗。
- [x] 中断链路补强：前端中断时携带 conversation/run 信息，并立即把当前任务本地标记为“已请求中断”，避免界面长时间停在“请求模型”。
- [x] 后端为当前 Agent run 登记可取消任务，REST/WebSocket 中断会优先命中真实运行中的 agent/task，并调用 `task.cancel()`。
- [x] 中断请求会立即把 AgentRun/Conversation 持久状态收敛为 interrupted，并广播 run_event 给前端任务面板。
- [x] 后端测试覆盖 conversation 定位 run、取消当前任务、持久状态改为 interrupted。
- [x] 新增 LLM 请求超时配置：预设里可设置请求超时秒数，默认 120 秒；非流式请求使用总时长超时，流式请求使用空闲超时。
- [x] 模型错误分类：超时、限流/额度、鉴权、请求参数、网络、通用模型错误会写入 AgentRun 的 usage 审计。
- [x] 前端任务面板显示请求超时、错误类型和错误详情，减少“只知道失败不知道为什么”的情况。
- [x] 失败不会自动重发，仍保持用户手动点击重试，避免额外 token 消耗。
- [x] Agent 任务区增加轻量恢复入口：失败/中断后显示处理建议，支持重试本轮、复制错误、标记已处理。
- [x] 错误建议按类型给出：超时、限流/额度、鉴权、请求参数、网络、通用模型错误分别提示下一步处理方向。
- [ ] 后续可继续做 provider 级更细超时/重试策略：例如流式长时间无增量、可选一次性重试、按错误类型给修复建议。

## 2026-07-04 Agent 超时与流式稳定性

- [x] 排查 Agent 连接超时链路：主 Agent 本意请求流式输出，但旧实现把整次 LLM 调用包在总时长超时里，长回复即使持续输出也可能在 120 秒附近被误杀。
- [x] 主 Agent 流式请求改为“空闲超时”：只要模型持续输出正文、reasoning 增量或工具参数增量，就刷新超时计时；完全长时间无进展时仍会超时。
- [x] DeepSeek thinking + 工具调用不再把主 Agent 请求降成非流式；带工具的流式请求会临时关闭 thinking，优先保住流式输出、中断能力和前台可见反馈。
- [x] 中断链路补洞：流式守护任务在外层取消时会同步取消底层 LLM 请求，`AgentInterrupted` 不再被误包装成普通模型错误。
- [x] AgentRun usage 审计补充请求模式：空闲/总超时、是否请求流式、消息数、工具数、本轮输入估算 token；前端任务面板显示这些摘要。
- [x] 保留非流式路径：会话记忆压缩、一致性检查、工作流步骤等短任务仍使用非流式请求，避免为内部任务制造不必要的流式复杂度。
- [x] 补充测试：覆盖流式持续输出不被总时长误杀、流式无进展仍超时、DeepSeek 工具流式请求临时关闭 thinking。

## 2026-07-05 Agent 轻量对话与超时诊断

- [x] 排查 run `5409b48c004a4b30995d43b9d1c6d517`：上游第一轮已返回工具调用，真正超时的是工具结果后的第二轮模型请求。
- [x] AgentRun usage 新增 `model_request_attempts`，前端报告显示“请求尝试”和“成功返回”，让失败请求也可见。
- [x] 撤回纯问候轻量链路：`你好/在吗/测试` 仍正常注入历史记忆、向量上下文和工具列表，避免切断连续对话能力。
- [x] AgentRun usage 新增 `llm_calls` 逐次模型请求追踪：记录阶段、轮次、模型、流式开关、工具 schema 数、是否工具结果续轮、估算输入 token、完成状态和错误类别。
- [x] 工具调用后的续轮请求改为稳妥收尾：`tool_finalize` 阶段不再携带工具 schema，且关闭流式请求，降低 OpenAI 兼容接口在工具结果续轮上的超时/400 风险。
- [x] 前端 Agent 任务报告显示最后一次模型请求摘要，方便判断是首轮请求失败还是工具执行后的整理回复失败。
- [x] `search_project` 与 `consistency_check` 结果瘦身：完整列表继续放在 `ui_hint` 给前端渲染，回传给模型的 `content` 只保留数量和前 3 条短摘要，降低工具续轮 token 压力。
- [x] Prompt 缓存友好布局：不删主要发送内容，只把长期 Prompt Pack 前移到动态会话摘要、近期历史和检索上下文之前，让稳定前缀更容易命中缓存。
- [x] Anthropic prompt cache 补强：开启缓存时，除 system prompt 外，工具 schema 副本也会加 ephemeral cache 标记；不修改原始工具描述内容。
- [x] 前端 Agent 任务报告显示缓存前缀状态，便于观察本轮是否存在稳定提示词前缀。
- [x] 工具调用后从固定 `tool_finalize` 改为受控多轮续跑：`tool_continue` 阶段继续暴露工具 schema，但关闭流式，并受最大迭代上限保护。
- [x] 工具续轮使用瘦上下文：保留长期提示词、会话摘要、少量近期历史、原始用户请求和工具结果摘要；不重复塞入首轮向量检索全文。
- [x] 工具续轮失败自动降级：续轮模型请求如超时、参数错误或通用模型错误，会退回不带工具 schema 的 `tool_finalize` 安全收尾。
- [x] Agent loop 增强：记录每轮工具批次、工具失败数和重复工具调用数，前端任务报告显示“工具轮次/重复跳过/工具失败”。（2026-07-13）
- [x] Agent loop 增加重复调用防护：同一轮任务内，如果模型再次调用完全相同的工具名和参数，后端跳过重复执行并把“请换参数或收尾”的观察结果回传给模型，避免死循环和重复 token 消耗。（2026-07-13）
- [x] 验证通过：`pytest backend/tests/test_agent_prompt_packs.py -q`、`npm run build`、`compileall backend/app`。
- [ ] 后续如需降 token，应做显式用户可控策略，例如“本轮轻量回复/不检索项目”，不能由系统对短输入自动断上下文。
- [~] provider 能力矩阵暂不做：当前主流模型基本支持工具调用，先用 `tool_finalize` 稳妥收尾和请求追踪解决实际问题。
- [ ] 后续如仍需继续降 token，再做完整资源引用机制：模型拿摘要和引用 id，前端保留全文，必要时由 Agent 按 id 二次读取。
- [ ] 后续继续完善 loop：增加“连续工具失败后暂停并给用户选择”的人机接管点，以及按工具类型设置更细的迭代预算。

## 2026-07-04 局域网开发启动

- [x] 新增根目录命令 `npm run dev:lan`，一条命令启动局域网开发模式。
- [x] `scripts/dev_lan.ps1` 会启动后端 `0.0.0.0:27631` 和 Vite `0.0.0.0:1420`，并打印局域网访问地址。
- [x] Vite dev server 增加 `/api` WebSocket/HTTP 代理，局域网设备访问前端时 API 会回到开发机后端，而不是访问设备自己的 `127.0.0.1`。
- [x] README 记录 `npm run dev:lan` 用法和无鉴权安全提醒。

## 2026-07-04 手机端基础适配
- [x] 外层应用壳在小屏改为单列布局，避免固定桌面侧栏挤压主内容。
- [x] 侧边栏在手机端压缩为顶部导航区，主导航和工具导航支持横向滚动。
- [x] 手机端隐藏项目长路径和手动路径输入，保留项目选择、新建、导入、导出、重命名、删除等常用入口。
- [x] 对话历史栏在手机端改为顶部受限高度区域，避免占满聊天区域。
- [x] 文件管理在手机端改为文件树在上、编辑区在下，并限制文件树高度。
- [x] 聊天头部、任务面板、运行报告和错误提示补充小屏换行与宽度约束。
- [x] 已执行 `npm run build:frontend` 验证通过。
- [x] 二次修正手机顶部侧栏：项目库不再在手机端摊开全部操作，只保留项目名、刷新和项目下拉；新建/导入/导出/重命名/删除等桌面操作在手机端隐藏，避免首屏被项目区吃掉。（2026-07-04）
- [x] 二次修正手机导航视觉：主导航和工具导航改成更紧凑的横向按钮组，侧栏取消内部大滚动条，主内容区负责滚动。（2026-07-04）
- [x] 三次重做手机端应用壳：不再复用桌面侧栏的移动端样式，`Sidebar` 改为桌面侧栏 + 手机顶部项目栏 + 手机底部主导航三段结构；工具入口放到顶部横向工具栏，主内容区为底部导航预留空间。（2026-07-04）
- [x] 修复手机端聊天页布局：底部主导航从悬浮覆盖改为应用壳第三行，主内容区不再抢滚动；聊天页恢复为消息列表独立滚动、输入框固定在聊天面板底部。（2026-07-04）
- [x] 手机端对话页空间回收：隐藏顶部工具横条，压缩移动顶栏、聊天 header、任务摘要和输入区；失败详情/恢复建议限高滚动，避免任务面板霸占首屏。（2026-07-04）

## 2026-07-04 Web-only 去 Tauri

- [x] 删除 `frontend/src-tauri/`、根目录 `src-tauri/` 和 Tauri 打包/运行脚本。
- [x] 移除前端 Tauri 依赖与 Tauri API 调用，浏览器打开外部链接改为 `window.open`。
- [x] 根目录脚本收敛：`npm run dev` 进入局域网 Web 开发，`npm run build` 只构建前端静态资源，`npm run web` 构建后由 FastAPI 托管。
- [x] 清理 CORS 和构建清理脚本里的桌面安装包残留。
- [x] README、技术细节、发布流程、更新发布待办、运行链路文档全部改为 Web-only 口径。
- [x] 项目报告更新当前形态：不再生成 Tauri/NSIS 安装包，后续如需非技术用户分发再单独设计 Web runtime 包。
- [ ] 后续可做 Web runtime zip 或 Docker/compose 分发方案，替代旧安装包路线。

## 2026-07-05 Agent 文件改动确认闭环

- [x] 新增 `file_change_proposal` 工具：Agent 可提出一组跨文件改动，并生成差异预览，但不会直接写入正式项目文件。
- [x] 新增 `.aisync/change_sets/*.json` 改动包存储：记录旧内容 hash、新内容、diff、原因和状态。
- [x] 新增 `/api/change-sets` 接口：支持读取、应用和丢弃改动包。
- [x] 应用改动前校验目标文件 hash；如果 proposal 生成后文件被外部修改，拒绝覆盖并返回冲突。
- [x] 前端 AI 渲染器新增 `changeset:proposal`：在聊天里显示文件改动包、文件级折叠 diff、应用全部和丢弃按钮。
- [x] 新增后端测试覆盖：proposal 不立即写文件、确认后写入、过期改动拒绝覆盖、内部路径拦截、丢弃改动包、工具 UI hint。
- [x] 修复旧版“全选工具名单”不包含新工具的问题：前端在工具列表刷新时会把 `file_change_proposal` 纳入编辑类工具集合，后端也会对旧的全工具名单做兜底补齐。
- [x] 主 Agent 系统提示词明确：遇到正式文件清理/改写且专用工具不能覆盖时，应调用 `file_change_proposal`，不要再回复“只能检索/无法写文件”。
- [x] 修复差异预览操作入口不可见：`changeset:proposal` 不再藏在默认折叠的工具结果里，而是直接显示为“待确认文件改动”确认卡，前端可见“应用全部/丢弃”。（2026-07-13）
- [x] 对话区新增“自动应用改动”开关：默认仍人工确认；开启后本轮 `file_change_proposal` 生成改动包会立即走后端 apply 逻辑落盘，并保留改动包审计状态。（2026-07-13）
- [x] 工具边界收紧：`outline_generate` 不再允许把 `requirements`/清理说明当作正文写进 `plot/outline.md`；缺少正式 `content` 会失败且不改文件。（2026-07-15）
- [x] 文件改动包兜底增强：只要本轮启用任一编辑类工具，后端自动补入 `file_change_proposal`，避免 Agent 再说“没有改动包工具入口”。（2026-07-15）
- [x] 工具治理字段落地：每个工具声明 `category`、`write_policy`、`requires_confirmation` 和 `agent_boundary`，后端 descriptor 与 Agent tool schema 共用同一份边界信息。（2026-07-15）
- [x] 工具中心改为读取后端治理字段分组展示，不再按工具名猜分类；工具抽屉显示分类、写入策略和 Agent 边界说明。（2026-07-15）
- [x] 主 Agent 系统提示词补充工具选择边界：search/review 只读，generate 只能写正式正文，补丁式清理必须走 `file_change_proposal`。（2026-07-15）
- [x] 修复 search-only 工具集无法写入的问题：即使当前可用工具只有检索类，只要用户请求明显是清理/删除/修改项目文件，后端也会兜底补入 `file_change_proposal`。（2026-07-15）
- [x] 撤回文本式 Agent loop 纠偏：已删除根据模型回复文本判断“没调用 file_change_proposal 就再喂一轮”的逻辑；后续如需拦截，应改为结构化 required-tool 状态，不再看回复文案猜。（2026-07-16）
- [ ] 后续可增强为“单文件应用/单文件丢弃”、更细的行级说明、应用成功后自动刷新文件树和当前打开文件。

## 2026-07-17 Agent loop 与文本控制治理

- [x] 删除 search-only 工具集根据用户句子中的“修改/删除/文件”等关键词自动补入 `file_change_proposal` 的逻辑；工具权限只由预设、前端选择和工具治理关系决定。此项取代 2026-07-15 的关键词兜底方案。
- [x] 删除任务面板根据用户文字猜“章节/角色/大纲/世界观”流程的逻辑；初始显示通用阶段，模型实际调用工具后再按工具身份切换任务模板。
- [x] Agent 最大轮次现在会预留最后一次无工具收尾，避免最后一轮执行了写工具却没有机会整理结果。
- [x] 重复工具调用签名加入项目状态版本：项目未变化时拦截完全相同调用；成功写入后允许用相同检索参数重新验证。
- [x] 连续两轮工具批次全部失败或重复时结构化停止，记录 `termination_reason=tool_stalled`，避免死循环和额外 token 消耗。
- [x] `ToolResult` 增加结构化 `status` 与 `retryable` 字段，loop 不再依赖工具结果文案判断成功、失败或重复。
- [x] 自动安全收尾仅保留给明确的请求格式错误（`bad_request`），超时和普通模型错误不再自动补发；运行报告显示兼容收尾次数和结束原因。
- [x] 伏笔上下文改为只在章节路径、大纲关联或实际词项命中时注入，不再对无关请求兜底塞入所有关键伏笔。
- [x] 文件改动确认升级为 `waiting_approval -> applied/discarded -> verifying/finalizing`：差异卡出现后同一个 Agent run 暂停，用户应用或丢弃后恢复，不再提前结束本轮。（2026-07-17）
- [x] 改动包应用/丢弃 API 会唤醒对应等待任务；整包应用才恢复，局部应用仍保持等待，避免把部分写入误报成完整完成。
- [x] 改动差异卡写入对话历史，刷新页面后仍可操作；卡片挂载时会读取改动包真实状态，避免历史快照一直显示“等待确认”。
- [x] 应用后 Agent 获得结构化 `applied` 结果并继续验证，丢弃后获得结构化 `discarded` 结果且明确不得声称已修改；运行报告记录确认决定。
- [x] 服务重启导致原 Agent 消失时仍允许应用/丢弃改动包，但前端会明确提示“原 Agent 已结束，可发送新消息继续验证”。
- [x] 等待确认增加默认 30 分钟超时与“稍后处理”动作；两者都保留 pending 改动包并释放当前 Agent。（2026-07-18）
- [x] Prompt 压缩阶段改用结构化消息标签，不再依赖固定中文前缀。（2026-07-18）

## 2026-07-18 Agent 内部消息结构化

- [x] Agent 内部消息增加结构化标签：提示词包、记忆摘要、历史消息和当前请求分别标记 `prompt_pack`、`memory_summary`、`history`、`current_request`。
- [x] 工具续轮上下文压缩改为读取内部标签，不再通过固定中文前缀判断消息类型。
- [x] 当前请求瘦身改为读取内部保存的原始请求字段，不再搜索“用户请求：”文本定位内容。
- [x] 发给模型前统一剥离 `_aisync_*` 内部字段，模型实际收到的消息内容保持不变。
- [x] 增加回归测试，验证标签不外泄且提示词、记忆、历史和当前请求内容不变。
- [x] 等待确认默认上限为 30 分钟；超时后自动按“稍后处理”恢复并无工具收尾，正式文件不变。（2026-07-18）
- [x] 差异卡增加“稍后处理”：改动包继续保持 pending，当前 Agent 收到 `deferred` 决定后结束本轮。
- [x] 应用继续进入验证；丢弃、稍后处理和超时强制进入无工具收尾，避免再次生成改动包。
- [x] 等待器在差异卡展示前注册，消除用户快速点击和 Agent 尚未挂起之间的竞态。
- [x] 差异卡状态通过 `changeset_update` 实时同步并写回对话历史；刷新后仍以服务端状态为准。
- [x] 应用改动后通过结构化 React 回调刷新文件树、当前打开文件和向量索引状态，不使用全局浏览器事件。（2026-07-18）
- [x] 同步刷新基础信息、章节、大纲、伏笔、角色和世界观数据，避免切换页面后仍看到应用前的缓存。
- [x] 仅当当前打开文件位于改动列表时重新读取内容；文件被删除时关闭当前文件，但不强制把用户从对话页跳到文件页。
- [x] 自动应用和人工应用都触发刷新；按 change set id 去重，避免 WebSocket 更新与按钮回调造成重复请求。
- [x] 向量侧只刷新索引状态并显示 stale，不自动全量重建，避免一次文件修改触发昂贵索引任务。
- [ ] 下一步：评估并收敛 Agent 高层写入工具，减少 `write_chapter`、`outline_generate`、`update_worldview` 与通用文件补丁之间的职责重叠。

## 2026-07-18 Agent 输出截断与网页断连治理

- [x] 排查实际半截回复：持久化内容本身断在半句，运行却被标为 completed；确认不是 Markdown 或前端渲染裁剪。
- [x] OpenAI 兼容流式客户端保留最后一帧 `finish_reason`，并将缺少正常结束帧的响应标记为 `stream_incomplete`；旧代码曾固定丢弃流式停止原因，因此历史故障无法再精确区分输出上限和上游提前断流。
- [x] Agent 识别 `length`、`max_tokens`、`max_output_tokens` 和 `stream_incomplete`，把结束原因记录为 `output_truncated`，并在回复末尾明确提示内容未完整生成。
- [x] 截断的工具调用不再执行，避免使用不完整参数修改项目；改为返回明确错误并建议缩小任务或提高预设最大输出 Token。
- [x] WebSocket 推送失败与 Agent 执行解耦：切换页面、手机后台挂起或临时断网导致前端连接断开时，后端继续生成并持久化最终回复。
- [x] WebSocket 成功链路补写最终 prompt audit，使运行报告可看到最终 Token、停止原因和请求状态。
- [x] 截断状态归入折叠的运行细节，正文只保留一次清晰警告，避免额外占用聊天空间。
- [x] 增加流式停止原因、缺失结束帧和 Agent 截断处理回归测试；后端全量 `120 passed`，Python 编译和前端生产构建通过。
- [ ] 后续可增加结构化“继续生成”快捷按钮；默认不自动补发，避免意外重复消耗 Token。

## 2026-07-19 AGENT.md 项目自律文件

- [x] 将项目长期规则从隐藏的 `.aisync/system_rules.json` 收敛为项目根目录单文件 `AGENT.md`，不新增规则数据库和专用工具。
- [x] `AGENT.md` 同时容纳当前项目固定文风与 Agent 长期工作习惯，每轮创建 Agent 时自动读取并注入 system prompt。
- [x] 旧项目若存在已启用的 `.aisync/system_rules.json` 且尚无 `AGENT.md`，首次读取时自动迁移内容；旧文件保留为遗留备份，不再作为优先生效来源。
- [x] 设置页“系统规则”改为 `AGENT.md` 单页编辑器，移除启用模式选择；首次保存自动创建文件，并提供可恢复的基础模板。
- [x] 主 Agent 可通过现有 `file_change_proposal` 提议修改 `AGENT.md`；默认差异确认，开启自动应用后直接落盘，不另建自修改工具。
- [x] Agent 仅在用户明确要求长期记住、调整项目文风/工作习惯，或同类问题被反复纠正且可确认是长期偏好时沉淀规则；不记录单次任务和小说事实。
- [x] 程序级工具权限、安全边界和核心规则保持在代码中，`AGENT.md` 不得覆盖或扩大这些权限。
- [x] Prompt Pack 边界调整：当前项目固定文风放 `AGENT.md`，跨项目复用、按阶段启用或随时切换的文风继续使用 Prompt Pack。
- [x] README、项目报告、Agent 能力、技术细节和提示词技术文档已同步新口径。
- [x] 增加 `AGENT.md` 直接读取、旧规则迁移、system prompt 注入边界和差异提案测试；后端全量 `124 passed`，前端生产构建、Python 编译和 diff 检查通过。

## 2026-07-19 工具失败人工接管

- [x] 连续两轮工具批次全部失败或重复时，不再直接抛出 `AgentLoopError` 并将本轮标记为失败。
- [x] Agent 返回结构化 `intervention`：重试本轮、跳过工具并总结、补充说明；默认不自动重试，避免额外 Token 消耗。
- [x] `intervention` 写入运行审计和 `agent_final.metadata`，对话刷新后仍能恢复操作入口。
- [x] 前端将人工接管选项渲染为按钮；“跳过工具并总结”会以空工具集发送收尾请求，“补充说明”会把失败信息放回输入框供用户编辑。
- [x] 增加 loop 回归测试；定向测试 36 项通过，前端生产构建和 Python 编译通过。
- [ ] 下一步可评估按工具类型设置不同失败阈值；先观察人工接管是否足够，暂不继续增加复杂状态机。

## 2026-07-19 删除 temp 与检索循环治理

- [x] 核对真实运行记录：用户确认“对”后，Agent 连续调用 `search_project` 7 次，8 次模型请求累计约 7.8 万 Token，始终没有调用 `file_change_proposal`，最后只做无工具收尾并口头承诺，正式文件没有变化。
- [x] 增加只读工具预算：项目状态未变化时连续成功调用搜索/审查工具 3 次后，下一轮隐藏只读工具，保留文件改动和其他执行工具，避免检索循环吞掉轮次和 Token。
- [x] 增强 `file_change_proposal`：新增仅限 `temp/` 的 `delete_directory` 操作，服务端展开为逐文件删除差异，保留 `temp/.aisync-temp.json`，不支持删除正式目录或程序内部目录。
- [x] 主 Agent 明确删除确认语义：用户确认范围后应直接生成改动包，不再重复口头确认或声称已经删除。
- [x] 增加检索预算和 temp 目录展开测试；定向测试 40 项通过，Python 编译通过。
- [ ] 下一步继续观察真实 Agent 是否能在检索预算耗尽后调用补丁工具；若仍只收尾，再增加结构化执行意图检查，不回退到回复文本关键词纠偏。

## 2026-07-19 用户项目文件树目录操作

- [x] 文件树目录删除入口改为通用能力，正式目录和 `temp/` 子目录均可操作，不再只显示在自由区。
- [x] 保留 `temp/` 根目录及其运行元数据，避免用户误删应用管理目录。
- [x] 后端删除接口合并重复路由，校验路径穿越、隐藏目录、内部目录和不支持的文件类型，并在删除文件后移除实际目录。
- [x] 删除完成后刷新文件树、当前文件和向量索引状态；Agent 当前运行不再因旧对话状态误报“未收敛”。
- [x] Python 编译、前端生产构建和 diff 检查通过。

## 2026-07-20 章节写作与伏笔联合改动

- [x] `write_chapter` 和 `edit_chapter` 增加结构化 `foreshadow_actions`：支持新埋、推进、回收和本章不处理。
- [x] 新伏笔自动绑定当前章节；已有伏笔推进/回收必须使用已有 ID，并记录章节证据和关联文件。
- [x] 章节正文与 `plot/foreshadows.json` 生成同一个待确认改动包，应用前不修改正式文件。
- [x] Agent 对任意带 `changeset_id` 的工具结果统一等待用户确认，章节写作产生的联合改动包也支持应用、丢弃和自动应用。
- [x] 章节工作流草稿支持正文与伏笔动作结构化输出；正式写入沿用工作流确认并同时应用章节和伏笔清单。
- [x] 增加伏笔动作、联合改动包、编辑工具和工作流回归测试；后端全量 `132 passed`，前端生产构建和 Python 编译通过。
- [x] 增加应用后的轻量伏笔证据复核：校验章节存在、伏笔 ID、证据匹配、章节关联和埋设/回收状态；失败只标记“待复核”，不阻断已确认写入（2026-07-20）。前端差异预览与工作流步骤都会展示复核结果；暂不做全文自动发现。
- [x] 复核结果落地到伏笔记录的 `verification` 元数据；伏笔面板增加复核状态筛选、待复核计数、问题详情和“标记为已确认”操作，不修改章节正文或伏笔核心状态（2026-07-20）。
- [ ] 联网搜索完整独立工具暂不进入主线：当前项目核心是本地小说资料一致性，外部搜索会引入来源可信度、缓存、隐私、限流和联网配置成本；本次只先提供模型原生搜索的显式开关。

## 2026-07-26 Agent 多文件任务闭环

- [x] 新增 `read_project_files` 精确读取工具：跨文件修改前可一次读取最多 12 个明确文件，不再依赖向量检索片段猜测完整内容。
- [x] 同一模型轮次中的多个 `file_change_proposal` 自动结构化合并为一个改动包、一次确认；重复目标路径由改动包校验拒绝，避免静默覆盖。
- [x] 同轮出现多个不同写入工具时阻止整批写入，要求模型改用一个跨文件改动包，避免部分文件先落盘。
- [x] 一个改动包应用后，本轮工具集收敛为读取/审查工具，只允许验证和总结，不再发起第二次写入确认。
- [x] 增加改动包逐文件落盘核验；核验结果进入差异卡和 Agent 运行报告。
- [x] 增加精确读取安全边界、改动包合并、多写工具拦截、只读验证和落盘漂移检测测试；后端全量 `139 passed`，前端生产构建、Python 编译和 diff 检查通过。
- [x] 改动包已支持安全局部 patch：`replace_text`、`append_text`、`prepend_text`；现有文件无需由模型重发完整内容。（2026-07-26）

## 2026-07-26 Agent 安全局部补丁

- [x] `file_change_proposal` 增加 `replace_text`、`append_text`、`prepend_text`，仍只生成待确认改动包，不绕过现有人工确认或自动应用设置。
- [x] `replace_text` 要求旧文本在当前文件状态中恰好出现一次；零匹配或多匹配会拒绝整个提案，空 `new_text` 可用于精确删除目标段落。
- [x] 同一文件的多个局部操作按声明顺序执行并合并为一份最终差异；禁止与完整 `write` 或文件删除混用，避免操作语义含糊。
- [x] 改动包内部仍保存最终完整内容和旧文件 hash，因此差异预览、过期冲突检查、一次确认与应用后核验保持不变。
- [x] 前端差异卡显示“局部替换 / 追加内容 / 前置内容”等来源操作，便于判断模型实际采用的改法。
- [x] 增加顺序局部操作、精确删除、零匹配、多匹配和混合操作拒绝测试；后端全量 `143 passed`，前端生产构建、Python 编译和 diff 检查通过。
- [ ] 下一步优先做一次真实 Agent 多文件修改验收，观察模型是否稳定采用“精确读取 -> 单个局部改动包 -> 应用后验证”链路，再决定是否需要更复杂的行级 patch。

## 2026-07-26 模型原生联网搜索开关

- [x] LLM 预设增加“使用模型原生联网搜索”开关，默认关闭，不新增 AiSync 自己的联网搜索工具。
- [x] Anthropic 请求启用官方 `web_search_20250305` server tool；OpenAI 兼容请求启用 `web_search_options`，由供应商和模型自行判断是否支持。
- [x] 开关贯通全局配置、预设配置、Agent 请求和设置页；修改全局配置会重建缓存中的 Agent，避免旧客户端继续使用旧状态。
- [x] 设置页明确提示：原生搜索依赖具体模型/API，不能保证所有 OpenAI 兼容服务都支持；关闭时请求内容和工具列表保持原有行为。
- [x] 增加两家客户端的开关启用/关闭回归测试；联网搜索完整独立工具、来源管理和搜索结果结构化暂不进入本步。

## 2026-07-26 对话与设置页面 UI 收敛

- [x] 对话顶栏拆分为标题/状态/主操作与“本轮设置”两层，工具、一致性审查和自动应用默认收起，减少首屏拥挤。
- [x] Agent 失败信息默认只显示摘要，完整错误、复制、重试、带错误继续问和忽略提示保留在紧凑操作区。
- [x] 设置页提升禁用控件和说明文字的可读性，模型列表失败状态改为独立提示块，避免错误信息破坏设置布局。
- [x] 完成桌面端与 390x844 手机端检查：顶栏、设置折叠区、失败提示和底部输入框无明显重叠，手机端关键操作仍可用。
- [x] 执行前端生产构建与 `git diff --check`；清理临时浏览器验证页。
- [ ] 后续可继续统一中英文标签、优化侧栏折叠，并观察真实模型请求下失败操作区是否还需进一步压缩。

## 2026-07-26 Agent 多文件改动闭环验收

- [x] 使用临时项目运行“精确读取多个文件 -> 汇总一个 `file_change_proposal` -> 应用 -> 只读验证”的完整 Agent loop，确认两份文件只生成一个改动包。
- [x] 验证应用后 Agent 工具集收敛为只读验证工具，不会在同一轮继续生成第二个写入改动包。
- [x] 验证改动包默认保持人工确认、应用后逐文件核验、外部文件漂移拒绝和丢弃/超时状态均正常。
- [x] 相关回归测试 `48 passed`；后端全量测试 `146 passed`。
- [x] 本次验收未修改正式项目文件，也未发现需要追加修复的问题。
- [ ] 后续仅观察真实模型在复杂跨文件任务中是否稳定遵守“先读后改、一次汇总、应用后验证”；出现偏离时优先调整工具描述和结构化约束，不新增文本关键词纠偏。

## 2026-07-26 文件树目录操作布局修复

- [x] 修复目录操作按钮使用 `float` 和负边距导致的覆盖问题；目录行改为名称区与操作区明确分栏的 flex 布局。
- [x] 目录名和自由区标记允许收缩，操作按钮固定占位并保持文字不换行，避免窄侧栏和展开 `temp/` 后出现重叠。
- [x] 完成桌面端与 `390x844` 手机端视觉检查，根目录、自由区和自由区子目录操作按钮均显示正常。
- [x] 前端生产构建和 `git diff --check` 通过。

## 2026-07-26 Agent 工具续轮长请求修复

- [x] 根据项目“绝地潜兵：异界解放战争”最新运行记录定位问题：首轮请求流式正常，工具结果后的 `tool_continue` 被固定为非流式，导致中转站长时间处理时前端没有任何增量反馈。
- [x] 所有用户可见 Agent 阶段（首轮、`tool_continue` 和 `tool_finalize`）统一使用流式请求；只有内部短任务继续按调用场景使用非流式。
- [x] 流式工具续轮继续沿用现有 DeepSeek thinking 兼容策略：带工具时临时关闭 thinking，避免 `reasoning_content` 回传格式错误。
- [x] 请求审计现在能明确区分首轮、工具续轮和最终收尾；各阶段产生增量后前端会及时更新状态，中断和空闲超时也能命中流式任务。
- [x] 后端全量测试 `146 passed`，前端生产构建通过。
- [x] 已用真实中转站完成“工具调用后长时间生成”验收：工具续轮能够流式返回，前端不再长时间无反馈；中断和超时链路保持可用。（2026-07-26）
- [x] 已使用真实模型完成“精确读取 -> 跨文件改动预览 -> 用户应用 -> 应用后验证”完整链路验收，确认只生成一个改动包且应用后能进入验证。（2026-07-26）
- [ ] 多文件应用失败回滚暂缓：磁盘写入失败概率低，当前先保持预校验、一次应用和落盘核验，不为低概率故障引入额外复杂度。

## 2026-07-26 伏笔梳理工具补全

- [x] `foreshadow_manage` 改为真正的只读 `manage` 工具：读取 `plot/foreshadows.json` 和章节元数据，不再声明或执行任何写入。
- [x] 普通“梳理未回收/推进中/规划”请求优先按伏笔状态筛选；指定章节路径或大纲节点时，返回相关伏笔、命中原因和建议动作，避免“回收”等通用词误命中已回收记录。
- [x] 工具结果同时提供模型简短摘要、结果数量、模式和结构化前端数据；空账本会明确提示如何建立第一条伏笔，不再只打开侧栏。
- [x] 前端新增伏笔梳理结果渲染，显示标题、状态、重要性、关联章节、标签、命中原因和建议；聊天区会正确显示结果标题与数量。
- [x] 新增空账本、未回收筛选、章节关联匹配、结果限制和只读不写回归测试；后端全量 `150 passed`，前端生产构建、Python 编译和 `git diff --check` 通过。
- [ ] 下一步观察真实 Agent 是否能在写章节时稳定声明 `foreshadow_actions`；若仍无法主动埋设/推进/回收，再针对工具参数和章节提示词做小幅结构化引导，不增加文本关键词纠偏。

## 2026-07-27 模型原生联网来源展示

- [x] 新增供应商无关的 `WebSource` 结构，统一保存 URL、标题、短引用和供应商；只接受 HTTP/HTTPS，单轮最多保留 12 条。
- [x] OpenAI 兼容客户端解析标准 URL citation 和部分中转站常见的 `citations` URL；流式与非流式响应均可收集来源。
- [x] Anthropic 客户端解析 `web_search_tool_result` 搜索结果和文本 citation；同一 URL 的标题与引用片段自动合并。
- [x] Agent 在首轮、工具续轮和最终收尾之间累积并去重来源，最终写入 `agent_final.metadata.web_sources`；对话历史刷新后仍可恢复。
- [x] 对话回复下新增默认收起的“参考来源”区，显示标题、域名和短引用，可直接打开网页；来源不回注模型，不额外占用后续上下文。
- [x] 增加来源格式、无效 URL、去重合并和跨模型轮次累积测试；后端全量 `155 passed`，前端生产构建、Python 编译和 `git diff --check` 通过。
- [ ] 后续先用一个确实支持原生搜索引用的真实预设验收；若供应商只回普通文本，则保持无来源状态，不通过猜测 URL 伪造引用。

## 2026-07-27 显式联网搜索工具

- [x] 参考 Codex 的工具编排，将联网从主模型请求中的隐式参数提升为 Agent 可见的 `web_search` 只读工具；只有模型明确调用后才发起独立搜索请求。
- [x] 主 Agent 请求不再同时开启供应商原生搜索，避免 Anthropic 同名工具冲突和 OpenAI 线路重复搜索；`web_search` 内部请求才启用 Anthropic web search 或 OpenAI 兼容 `web_search_options`。
- [x] `web_search` 支持单独选择工具 LLM 预设；未配置时使用主 Agent 当前 LLM。搜索查询不注入项目向量、完整历史或其他工具列表，限制为短查询和最多 8 个来源。
- [x] 供应商必须返回结构化 HTTP/HTTPS 来源才判定搜索成功；只有普通文本或中转站静默忽略搜索参数时，工具明确返回“未获得可验证来源”，不允许模型声称已经实时核验。
- [x] 搜索开始、完成和失败复用结构化工具事件，前端显示专用状态和折叠来源卡；最终来源继续写入 `agent_final.metadata.web_sources`，刷新对话后仍可查看。
- [x] 搜索内部 LLM 的输入、输出和总 Token 并入本轮运行统计，同时单独记录 `tool_llm_calls`，避免搜索成本成为不可见消耗。
- [x] 增加显式工具启用/隐藏、主请求与搜索请求隔离、零来源失败、来源数量和 Token 计量测试；后端全量 `159 passed`，前端生产构建、Python 编译和 `git diff --check` 通过。
- [ ] 使用一个确定支持结构化引用的真实预设完成端到端验收；如果常用中转站长期不返回引用，再评估增加独立搜索服务适配器，不通过供应商名称硬编码能力矩阵。

## 2026-07-27 原生联网搜索中转站兼容修复

- [x] 根据最新 Agent run 定位真实失败：主 Agent 正常调用了两次 `web_search`，搜索子请求均获得文本和 Token 用量，但常用 OpenAI 兼容中转站没有按标准 `message.annotations` 返回 URL，解析结果为 0 条来源，随后触发连续工具失败保护。
- [x] OpenAI 兼容来源解析扩展到响应根节点及 `citations`、`sources`、`references`、`search_results`、`web_sources`、`results` 等常见嵌套字段，并兼容 `url`、`link`、`href`、`source_url` 字段名。
- [x] 搜索内部短提示现在要求供应商在正文末尾逐行返回真实来源的完整 HTTP/HTTPS 地址；结构化引用缺失时，只从该次原生搜索响应中的明确链接降级生成来源，不根据标题或描述猜测 URL。
- [x] 仍未返回任何来源地址时继续按失败处理，保留“不可声称已经联网核验”的边界；主 Agent 的历史、项目上下文、工具列表和普通请求缓存布局不受影响。
- [x] 增加响应根节点兼容、常见中转字段、Markdown/裸链接提取和工具降级成功测试；后端全量 `162 passed`，Python 编译通过，开发后端健康检查返回 200。
- [ ] 使用当前 `qianye / gpt-5.6-sol` 预设重新发起一次真实联网查询，确认该中转站至少返回一个明确 URL；如果仍只有无链接文本，则该线路实际上没有透传可用来源，下一步改接独立搜索服务而不是继续放宽可信边界。

## 2026-07-27 独立公开搜索适配器

- [x] 第二次真实验收确认 `qianye / gpt-5.6-sol` 仍只返回无 URL 文本：本轮 `web_search` 子请求消耗 581 Token，`source_count` 仍为 0，证明该中转线路没有透传可用的原生搜索来源，停止继续放宽文本解析。
- [x] 新增供应商无关的公开搜索适配器，固定访问 Bing RSS 搜索端点，解析标题、摘要和原始 HTTP/HTTPS URL；响应限制为 1 MB，并对重复和非法链接进行过滤。
- [x] `web_search` 改为独立公开搜索优先，成功时不调用内部 LLM、不产生 `tool_llm_calls` Token；公开搜索失败或无结果时才回退供应商原生搜索。
- [x] 设置页文案改为“启用联网搜索”，明确独立搜索优先、供应商原生搜索兜底；内部字段名暂保留 `native_web_search` 以兼容已有预设数据。
- [x] 使用与用户问题对应的 Helldivers 查询完成真实直连验证，成功获得 5 个带标题、摘要和 URL 的结果，包括 Helldivers Wiki 与 Steam 页面；测试过程未调用 LLM，也未写入对话历史。
- [x] 新增 RSS 解析、去重、非法 URL、结果上限、公开搜索优先和零 Token 回归测试；相关测试 `23 passed`。
- [ ] 在当前对话中重新发起一次需要实时资料的问题，确认 Agent 调用 `web_search` 后来源卡显示 `bing-rss` 结果，最终回复下保留参考来源。

## 2026-07-27 Tavily 付费搜索接入

- [x] 按 Tavily 官方 Search API 当前文档接入 `POST https://api.tavily.com/search`，使用 Bearer API Key；请求关闭 Tavily 生成式答案，仅接收搜索结果正文片段和来源 URL，由主 Agent 负责整理回答。
- [x] LLM 预设新增联网策略、Tavily API Key/环境变量、`basic|advanced` 搜索深度、1～20 条结果上限和清洗正文开关；旧预设缺失字段时自动使用默认值，无需迁移脚本。
- [x] 自动策略按 `Tavily -> Bing RSS -> 模型原生` 降级；可强制使用 Tavily 优先、仅免费 Bing RSS 或仅模型原生。Tavily 未配置、无结果、超时或请求失败均不会阻断后续兜底。
- [x] Tavily `basic` 固定 1 credit，`advanced` 固定 2 credits 并请求每个来源最多 3 个相关片段；可选清洗正文单来源限制约 4000 字符，避免工具结果无限膨胀。
- [x] Agent run 新增 `search_calls` 和 `search_credits` 审计，记录搜索服务、credits、请求 ID、响应时间和搜索深度；聊天任务摘要显示本轮联网额度，搜索服务成本不再与 LLM Token 混在一起。
- [x] 设置新增独立“联网搜索”页面，包含启用开关、策略、Key、环境变量、深度、结果数和正文开关，并说明工具默认预设对联网配置的优先级。
- [x] 桌面端和 `390x844` 手机端视觉检查通过：字段完整、页面可纵向滚动、无横向溢出；未填写或发送任何真实 Tavily Key。
- [ ] 用户在 Tavily 控制台创建 Key 后，将其填入 `web_search` 实际使用的预设并保存，再用一个详细游戏数据问题完成首次付费搜索验收。

## 2026-07-31 Tavily 结果上限放宽

- [x] 将联网搜索结果上限从 8 统一放宽到 20：前端设置、预设校验、运行时配置、Agent 工具 schema 和 Tavily 请求体保持一致；默认值仍为 5，避免旧预设和普通搜索突然扩大上下文。
- [x] 明确 Basic/Advanced 的使用差异：Basic 约 1 credit；Advanced 约 2 credits，并请求每个来源最多 3 个相关片段。两者都不改变结果条数上限，条数由“最多结果数”单独控制。（完成时间：2026-07-31）

## 2026-07-31 思考强度治理

- [x] 设置页将已有 `effort` 选项改为中文“思考强度”，明确说明它是预设级别，不同供应商会自动映射，普通模型可能忽略。
- [x] Anthropic 使用 `output_config.effort`；DeepSeek 的低、中、高不再全部压成高，`xhigh`/`max` 会安全降为高；OpenAI/兼容接口只对明确识别的推理模型发送 `reasoning_effort`，避免普通模型因未知参数 400。
- [x] 增加思考强度映射与普通模型隔离测试；专项测试 `18 passed`，后端全量 `244 passed`，前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-31）

## 2026-07-28 Agent 结构化选择交互

- [x] 删除基于“可选方案/请选择”等正文关键词和编号列表的前端猜测逻辑；普通 Markdown 编号列表不再被误判为交互选项。
- [x] 新增 Agent 内部协议工具 `present_choices`，由工具参数显式声明选择组、单选/多选、必选/可选、最少/最多数量和选项说明；工具自声明为内部工具，不出现在工具中心和本轮工具选择器中。
- [x] Agent 调用选择工具后直接以 `awaiting_choice` 收敛并等待用户，不再追加一次模型收尾请求，也不在聊天区显示原始工具结果。
- [x] 一条 Agent 回复支持 1～4 个独立选择组，每组支持 2～8 个选项；前端统一提交多个组，达到多选上限后禁用剩余选项，可选单选组支持清空。
- [x] 选择结果同时保存为可读用户消息和 `choice_response` 结构化元数据；下一轮模型请求会收到结构化结果，对话刷新后可恢复已选项并锁定，旧的未回答选择会标记为已跳过。
- [x] 完成后端参数边界、重复 id、Agent 单轮暂停、内部工具隐藏和选择结果注入测试；后端全量 `173 passed`，前端生产构建通过。
- [x] 完成桌面端和 `390x844` 手机端实际页面回归：单选、多选、多组、数量限制、历史恢复和消息区滚动正常，无横向溢出或控制台错误。
- [ ] 后续观察真实模型是否会在普通方案分析中滥用 `present_choices`；如需收紧，优先调整工具描述和结构化调用规则，不恢复正文关键词纠偏。

## 2026-07-28 一致性审查降噪与召回治理

- [x] 一致性审查默认继续关闭，只有用户本轮主动开启或模型明确调用时才消耗审查 Token。
- [x] 删除“高相关内容就是问题”、全局否定词差异和跨对象数字比较；规则只检查同一对象、同一属性、同一时间范围内的明确数值或对立状态。
- [x] 数值规则按语义维度区分年龄、比例、人口、距离、时长、年份、等级、容量、温度和速度；章节编号、列表编号及普通共享数字不再触发冲突。
- [x] 支持阿拉伯数字与常见中文数字归一化，例如 `20岁` 与 `二十岁` 视为相同，`21岁` 与 `二十岁` 才提示冲突。
- [x] 向量索引新增精确词召回能力，对 Markdown 标题、`name/title/姓名/名称` 字段和文件开头给予额外权重；一致性工具将精确召回与现有语义检索合并，避免长角色档案因余弦归一化分数过低而漏检。
- [x] 结构化角色档案会把文档标题或姓名字段作为对象锚点传给年龄、状态等字段，支持“标题写角色名、下一行写年龄”的常见 Markdown/YAML 格式。
- [x] 配置独立工具预设时使用便宜审查模型；未配置或与主模型相同时复用主 Agent LLM。合法 `{"issues":[]}` 会被接受为无冲突，只有 JSON 无法解析时才降级到保守规则。
- [x] LLM 审查 Token 纳入本轮 Agent 总统计；候选上限收紧为 12，审查输出上限降为 2000 Token，完整问题留给前端，回传模型只保留前三条摘要。
- [x] 使用当前小说副本完成真实验收：地理比例与普通相关剧情均为 0 误报；“林铎 21 岁”优先命中角色档案并识别与“二十岁左右”的冲突；“林铎 20 岁”不误报。
- [x] 新增相关内容、同/不同对象数字、人物生死、时间变化、中文数字、精确召回、LLM 空结果、非法输出和无候选测试；后端全量 `184 passed`，前端生产构建通过。
- [ ] 后续优先观察真实写章场景的漏检率；如需增强，应增加时间线和实体属性结构化数据，而不是重新扩大关键词规则或默认扫描全部章节。

## 2026-07-29 一致性审查可解释性

- [x] 一致性结果卡增加审查模式、实际模型、供应商、工具预设、输入/输出/总 Token、候选片段数和审查文件数，不再需要到运行日志里猜测审查成本。
- [x] 审查文件清单默认折叠；工具结果本身继续沿用聊天区自动折叠，不把候选全文和原始 JSON 直接铺进对话。
- [x] 规则冲突保存“新内容”和“已有设定”两侧短证据；LLM 输出协议同步增加 `new_snippet` 与 `existing_snippet`，旧 `snippet` 返回仍可兼容。
- [x] 严重级别前端改为“明确冲突 / 可能冲突 / 建议复核”，规则降级会显示独立警告，不再暴露 `critical/potential/notice` 英文内部值。
- [x] `list:issues` 工具结果与完整审查元数据写入对话历史，刷新或重新打开对话后仍可查看；其他普通检索结果仍不持久化，避免历史再次臃肿。
- [x] 完成桌面端与 `390x844` 手机端真实 CSS 检查：桌面四列摘要、手机单列摘要和证据区均无横向溢出。
- [x] 后端全量 `184 passed`，前端生产构建和 `git diff --check` 通过。
- [ ] 下一阶段如继续增强一致性，优先做写章完成后的可选增量事实提取；暂不做全书全文重审或默认自动审查。

## 2026-07-29 章节增量事实快照

- [x] 新增按章节存储的派生事实快照：`chapters/vol-01/ch-001.md` 对应 `plot/facts/vol-01/ch-001.json`，避免维护一个无限增长、容易冲突的全书事实总账。
- [x] 单章最多记录 12 条长期事实，类别覆盖身份、状态、关系、位置、持有物、时间点、世界规则和其他；每条保存主体、属性、值、确定性、来源章节和正文短证据。
- [x] `write_chapter`、`edit_chapter` 和章节工作流已接入事实快照；事实由主模型在原本那次写章输出中同时声明，不增加第二次模型调用和额外审查 Token。
- [x] 章节正文、伏笔动作和事实快照可合并为同一个待确认改动包，应用前统一查看差异；没有伏笔和事实时继续沿用直接写章，不增加无意义确认。
- [x] 编辑章节不传 `fact_records` 时保留旧快照，显式传空数组时清空该章快照；事实 ID 确定生成，重复事实自动去除，字段缺失和超过数量上限会拒绝写入。
- [x] 改动包新增默认折叠的“本章长期事实”预览，显示类别、主体、属性和值、确定性、证据、时间和标签；桌面与 `390x844` 手机宽度无横向溢出。
- [x] 事实 JSON 自动进入现有向量/混合索引，可被项目检索与一致性候选召回；它只是派生索引，章节正文仍是最终证据。
- [x] 新增事实路径、校验、去重、确定 ID、联合应用、显式清空、旧行为兼容、向量召回和工作流落盘测试；后端全量 `198 passed`，前端生产构建通过。（完成时间：2026-07-29）
- [ ] 下一步让一致性审查优先读取相关章节事实，按实体属性和故事时间比较状态变化；先做候选缩小与证据回链，不默认开启审查、不扫描全书全文。

## 2026-07-29 长章节草稿缓冲与分段阅读

- [x] 定位“工具调用达到输出上限”的根因：`write_chapter` 将整章正文放入一次工具 JSON，模型达到最大输出 Token 后会留下不完整参数；后端拒绝执行是必要的数据保护，不应尝试写入半章。
- [x] 新增 Agent 内部 `chapter_draft` 工具，使用 `begin -> append -> finalize` 协议；正文按 sequence 追加到 `.aisync/chapter_drafts/`，单块最多 5000 字符、单章最多 20 万字符。
- [x] 最终提交只携带 `draft_id`、伏笔动作和章节事实，不重复携带整章正文；后端从缓冲生成统一改动包后清理内部草稿，正式章节仍需差异确认后才写入。
- [x] 保留旧 `write_chapter` 兼容路径，但模型 schema 将单次正文限制为 6000 字符；系统提示和截断错误都会明确要求长章节改走草稿缓冲。
- [x] 内部草稿工具不出现在工具中心或本轮工具选择器；只要用户允许 `write_chapter`，Agent 会自动获得 `chapter_draft`。开始和追加结果不铺进聊天区，只显示累计字符状态，最终差异包正常展示。
- [x] Agent 默认最大迭代次数从 8 提高到 12，为多块长章保留足够的开始、追加、提交和收尾轮次；sequence 校验防止重复块和乱序写入。
- [x] `read_project_files` 新增 `inspect` 模式，返回文件总行数、字符数和最多 60 个 Markdown 标题行；模型可据此通过 `selections` 自选起止行读取局部，每段最多 800 行。
- [x] 分段正文带稳定行号，返回实际行范围、总行数和截断状态；原 `paths` 全文读取保持原始内容格式，跨文件整体改写链路不受影响。
- [x] 新增长章节缓冲、顺序保护、分块上限、丢弃、最终事实同步、内部工具自动暴露、完整 Agent loop、标题检查、行范围读取和全文兼容测试。
- [x] Ruff 检查通过，后端全量 `207 passed`，前端生产构建通过，后端独立导入正常。（完成时间：2026-07-29）
- [ ] 后续真实模型验收时重点观察：是否主动选择长章协议、分块衔接是否重复、是否在局部任务中先 inspect 再选行；若模型不稳定，优先收紧工具描述，不增加正文关键词纠偏。

## 2026-07-29 大纲原文恢复与防覆盖

- [x] 定位真实事故原因：`/story/outline/import-markdown` 将自由格式 `plot/outline.md` 提取为少量章节节点后，又用扁平结构化结果覆盖原文件，导致卷结构、作品定位、主线说明等非章节内容从正式文件消失。
- [x] 从已应用改动包恢复 `绝地潜兵-异界解放2/plot/outline.md` 覆盖前完整版本，共 5721 字符；被压扁的 Markdown 和 JSON 保存在项目 `.aisync/recovery/2026-07-29-171437/`，未直接删除。
- [x] 将当前压扁的 `plot/outline.json` 移入恢复目录，使仍在运行的旧后端立即回退到 `markdown_only` 并显示完整原文；接口确认可导入 3 个明确章节节点。
- [x] Markdown 导入改为只生成 `plot/outline.json`，不再重写 `plot/outline.md`；结构化大纲响应同时返回完整 Markdown，前端“完整 Markdown 原文”页始终可查看源文件。
- [x] 人工结构化保存和 Agent `outline_generate` 覆盖前自动在 `.aisync/outline_history/` 保存带时间、原因和内容哈希的大纲快照。
- [x] 新增导入原文不变、结构化模式返回原文、覆盖前快照和 Agent 快照测试；后端全量 `209 passed`，Ruff 与前端生产构建通过。（完成时间：2026-07-29）
- [ ] 下一步统一大纲真源：明确 `outline.json` 为结构化对象、`outline.md` 为自由格式源稿还是可读投影；在决策完成前，不再让两个入口静默互相覆盖。

## 2026-07-29 混合大纲模型

- [x] 明确唯一创作真源为 `plot/outline.md`；模型、用户和大纲生成工具只编辑 Markdown，`plot/outline.json` 改为后端自动维护的派生索引，不再要求模型同时写两个文件。
- [x] 新增混合大纲解析器，识别文档标题、卷、明确章节、普通标题区块和无标题 Markdown 内容；保留原始正文、标题层级、父子关系、稳定 ID 与源文件行号，并忽略代码块内的伪章节文本。
- [x] 只有明确章节节点显示“未开始/进行中/已完成”等状态；`第4—10章` 这类范围说明、作品定位、人物关系、创作原则和自由排版内容均作为普通区块展示，不再误报未完成。
- [x] `GET /story/outline` 返回 `hybrid` 结构、完整 Markdown 与章节兼容列表；源文件哈希未变化时不重复改写索引，Markdown 保存后自动快照并重建 JSON。
- [x] 旧扁平保存接口遇到自由格式大纲时返回 409，阻止再次压平覆盖；`outline_generate` 只写一次 Markdown，再由后端生成索引。
- [x] 前端新增结构视图、原文视图、区块折叠、类型标记、源行范围、Markdown 正文渲染和完整原文编辑器；旧简单项目仍保留原扁平编辑能力。
- [x] 使用恢复后的真实大纲验收：5721 字符原文保持不变，生成 35 个区块，其中 22 个普通区块、10 个分卷、3 个章节；特殊说明和布局均可见且不会被当成章节。
- [x] 完成桌面端与 `390x844` 手机端检查：无横向溢出，编辑器可完整载入并取消，控制台无错误；Ruff、`git diff --check`、前端生产构建通过，大纲相关测试 `25 passed`，此前后端全量测试 `214 passed`。（完成时间：2026-07-29）
- [ ] 下一步让 Agent 的大纲修改优先提交针对 `outline.md` 的局部补丁，而不是整文件重写；利用节点源行范围缩小读取和修改范围，并继续保留改动确认与历史快照。

## 2026-07-29 Agent 大纲局部补丁

- [x] 不新增独立大纲编辑工具，复用现有 `read_project_files -> file_change_proposal -> 差异确认` 链路，避免工具中心和 Agent 决策继续膨胀。
- [x] `read_project_files` 检查 `plot/outline.md` 时返回混合大纲区块 ID、类型、父区块和精确源行范围；模型可先看结构，再只读取需要修改的局部行段。
- [x] `file_change_proposal` 新增 `replace_outline_node` 操作：按区块 ID 替换完整 Markdown 区块，传空内容可删除区块；只允许作用于 `plot/outline.md`，未知或已变化的 ID 会拒绝执行。
- [x] 同一个改动包最多处理 24 个大纲区块；多个区块按原始行号倒序应用，前面区块的增删不会导致后续目标错位，也不能把同一区块重复修改两次。
- [x] 大纲区块补丁不能和同文件的完整覆盖、普通文本替换或删除混用；前端继续显示现有文件差异卡，不增加新的确认入口。
- [x] 改动包真正应用前自动保存旧 `outline.md` 到 `.aisync/outline_history/`，应用后自动重建 `outline.json`；项目概览、章节状态和大纲页面不会继续读取旧索引。
- [x] Agent 系统规则明确：已有大纲局部修改必须先 inspect、再读取目标行、最后提交 `replace_outline_node`；`outline_generate` 仅用于新建、较大段续写或用户明确要求的整体重写。
- [x] 新增区块索引暴露、多区块替换与删除、未知 ID 拒绝、快照和索引刷新测试；Ruff 通过，相关回归 `33 passed`，后端全量 `216 passed`。（完成时间：2026-07-29）
- [x] 使用隔离临时项目完成真实模型端到端验收：模型依次调用 `read_project_files.inspect -> 局部行读取 -> file_change_proposal`，准确传递“第二章 山门旧事”节点 ID，并通过单个改动包在该节点前局部插入说明区块；原大纲其余内容未被覆盖。（完成时间：2026-07-30）

## 2026-07-30 Agent 大纲节点前后插入验收

- [x] `file_change_proposal` 在原有节点替换基础上新增 `insert_before_outline_node` 与 `insert_after_outline_node`；同边界多次插入保持请求顺序，仍沿用最多 24 个大纲局部操作、倒序应用、差异确认、历史快照和索引重建。
- [x] 真实模型在隔离项目中完成“第二章前插入卷内节奏说明”：只开放 `read_project_files` 与 `file_change_proposal`，共两次读取和一次局部提交，生成并自动应用一个仅含 `plot/outline.md` 的改动包；`.aisync/outline_history/` 快照和派生索引均成功生成。
- [x] 最终模型收尾请求在文件已应用后返回 `503 auth_unavailable`；系统将运行识别为 `finalize` 而不是重新发送。验收未重复请求，避免同一区块重复插入，也再次验证了安全重试的副作用判定。
- [x] 验收发现正文“第一章负责引出密信……”被宽松章节正则误判为新章节；裸文本章节标题现在必须在“章/节/回”后结束或出现空白、冒号、句点、连字符等分隔，显式 Markdown 章节标题继续兼容紧凑写法。
- [x] 大纲派生索引版本提升到 3，现有项目下次加载会自动重建 `outline.json`，无需用户手动清缓存；修复后临时项目只保留“雨夜来客、山门旧事”两章，说明正文正确归入普通区块。
- [x] 改动包持久化保存 `source_operations` 与 `outline_node_ids`，后续即使刷新或查看历史，也能区分节点局部插入、节点替换与整文件写入，不再只看到物化后的 `write`。
- [x] 新增章节前缀正文误判、严格导入和局部操作审计回归测试；大纲及改动包相关 `47 passed`，后端全量 `227 passed`。（完成时间：2026-07-30）
- [ ] 下一步优化“工具已成功应用、最终回复失败”的前端状态：明确显示文件已经修改以及失败仅发生在回复收尾，并把“继续收尾”放在改动结果旁，减少用户看到红色失败后误以为文件未写入。

## 2026-07-30 最新 Agent 对话失败诊断

- [x] 核对失败运行 `8ba964cc422540c396eb180cc77ea8ff`：6 次模型请求均正常完成，不是上游超时；失败发生在 `chapter_draft.finalize` 的伏笔动作校验。
- [x] 第一次提交把 `advance` 与不存在的 `foreshadow_id` 组合，第二次又把 `plant` 与已有 `foreshadow_id` 组合；连续两轮工具失败后触发 Agent 防空转保护并转为人工介入。
- [x] 确认章节正文草稿仍保存在 `.aisync/chapter_drafts/`，本次失败没有覆盖正式章节，但 6 轮循环累计消耗 140851 Token。
- [x] 发现更晚运行 `1b1031207243411ca24ae51c1e32c6e0` 已在对话历史中产生完整回复，运行记录却仍停留在 `running`；这是独立的收尾状态持久化问题，会导致“未收敛”误报。
- [x] 章节改动包与可选伏笔元数据已解耦：伏笔动作逐条校验，错误项只警告并跳过，合法项与章节正文、长期事实继续生成改动包；长章节草稿不会再因伏笔 ID 或动作组合错误而丢失。（完成时间：2026-07-30）
- [x] 差异预览新增默认折叠的“已跳过无效伏笔动作”区域，可查看具体错误；针对本次 `advance` 无效 ID、`plant` 错带 ID 及合法/非法动作混合场景补充回归测试。
- [x] 后端完整回归 `220 passed`，相关 Ruff 检查与前端生产构建通过；按本次要求未修改运行记录收尾或“未收敛”判定逻辑。

## 2026-07-30 Agent 安全重试治理

- [x] 删除前端“把上次输入重新调用 send”的草率实现，新增后端 `retry_run` 恢复入口；重试不再向对话历史重复插入同一条用户消息。
- [x] Agent 运行记录保存完整原输入、原 LLM 预设、原工具列表、来源运行 ID 和恢复模式；重新发送会严格复用原配置，不再从 11 个已选工具意外退回全部工具。
- [x] 将恢复分成两种语义：首次模型请求失败且没有写入副作用时显示“重新发送”；已有文件或项目改动时显示“继续收尾”。
- [x] “继续收尾”强制禁用全部工具，只把已完成工具、已应用改动包、核验文件和失败原因整理给模型生成最终结论，禁止再次修改文件。
- [x] 只能恢复当前对话最后一轮；旧历史中的重试按钮不能越过新消息重放旧任务。连续空重试会回溯同输入的上一条有副作用运行，避免已写入后再次从头执行。
- [x] 使用真实运行接口确认最新 `502 overloaded` 失败被判定为 `restart`；此前已应用两个文件后失败的运行会判定为 `finalize`。
- [x] 新增原配置保存、恢复关系、副作用识别、收尾提示、历史去重和连续空重试回溯测试；后端全量 `225 passed`，相关 Ruff 与前端生产构建通过。（完成时间：2026-07-30）

## 2026-07-30 人物工作台 V1

- [x] 建立统一人物对象层：`profile.yaml` 管理姓名、定位、简介、别名、状态、阵营、标签、首次出场和 schema 版本，`profile.md` 继续保存自由人物档案；保存时保留既有未知 YAML 字段，兼容旧项目自定义数据。
- [x] 统一角色标识校验与姓名/别名冲突检查；损坏 YAML、非对象元数据和无效 schema 版本不再拖垮整个人物页，而是返回可见警告并尽量兼容读取。
- [x] 补全人物归档闭环：人物可附原因移入 `temp/archive/characters/`，归档列表可查看并安全恢复；恢复时检查归档路径、目标目录和重名冲突，避免覆盖正式角色。
- [x] 重构人物前端为工作台：增加概览统计、姓名/别名/阵营/标签搜索、定位与状态筛选、紧凑人物目录、结构化详情、独立 Markdown 档案编辑、未保存切换提醒、归档确认和归档恢复。
- [x] `create_character` Agent 工具接入统一对象层，支持新增结构化字段并复用冲突检查；新档案不再强制在 Markdown 中重复写入定位和简介。
- [x] 修复手机端创作工具入口被隐藏的问题；角色、伏笔、大纲、世界观和章节工作区现在均可从手机顶部工具栏进入。人物工作台在 `390x844` 下无横向溢出，目录与页面可滚动，编辑表单完整可用；概览统计压缩为 2×2，减少首屏占用。
- [x] 人物专项测试 `6 passed`，后端全量 `232 passed`，人物相关 Ruff、前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-30）
- [ ] 后续再评估人物关系与跨文件影响视图；V1 先保持轻量，不引入关系图谱或自动全项目改名，避免把基础档案管理复杂化。

## 2026-07-30 大纲工作台 UI 重构

- [x] 定位旧结构视图的核心问题：混合大纲会将全部区块正文同时展开，真实项目 83 个区块生成约 12961px 高的长页面；缩放或窗口变窄时，层级缩进、类型、标题、状态和正文共同争抢宽度。
- [x] 混合大纲改为“紧凑目录 + 单区块阅读器”：左侧目录只承担层级定位和搜索结果导航，右侧仅渲染当前区块正文，目录与正文独立滚动，不再堆叠全书卡片。
- [x] 阅读器增加父级提示、原文行号、直接子区块数量、关联章节、上一个/下一个切换和结构标题空状态；搜索后只在命中区块之间切换。
- [x] 大纲面板高度改为继承工作区可用高度，不再固定占用 `100vh`；宽桌面保持双栏，窄桌面自动收紧栏宽，手机端改为上下布局，避免缩放和底部导航造成尺寸错位。
- [x] 原文视图和 Markdown 编辑器继续保留完整自由排版；手机端编辑框、操作区和长文阅读均无横向溢出，不改变 `outline.md` 真源、派生索引或保存链路。
- [x] 完成 `1280x720`、`900x720` 与 `390x844` 验收：结构视图、节点切换、原文视图和编辑器均可用，页面无控制台错误；前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-30）
- [ ] 后续根据真实使用反馈再决定是否增加卷级折叠或目录快速跳转；当前先不加入缩放滑杆、关系图或画布模式，避免重新引入复杂布局。

## 2026-07-30 手机端章节阅读重构

- [x] 修正章节页的信息优先级：正文移动到章节标题和操作区之后，摘要、伏笔、向量辅助与生产信息不再挡在正文前面；桌面端继续保留左侧目录、右侧正文的工作台结构。
- [x] 手机端新增“正文 / 目录 / 信息”三段切换，默认进入正文；目录保留搜索、排序和状态筛选，选中章节后自动返回正文。
- [x] “信息”页集中承载摘要、相关伏笔、向量检索、一致性检查和生产信息，并保留当前章节名称与状态，避免辅助功能挤占阅读空间。
- [x] 手机端隐藏四块章节概览统计，移除详情区的嵌套卡片外观，正文使用完整可用宽度和更适合长篇阅读的行高；面板高度改为继承工作区，不再固定 `100vh`。
- [x] 完成 `390x844` 验收：正文首屏可见、无横向溢出，目录搜索可见、选章回正文、信息页与编辑/预览均正常；`1280x720` 桌面布局正常，前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-30）

## 2026-07-31 人物实体与旧项目迁移

- [x] 人物 schema 升级到 v3，每个活跃或归档人物拥有永久 `character_id`；姓名、别名、旧名和称号只用于解析，不再承担跨模块引用键。
- [x] `characters/index.yaml` 改为自动维护的派生人物注册表，记录活跃与归档人物的 ID、显示名称、别名、状态和归档位置；新项目初始化时直接创建有效空注册表。
- [x] 旧项目首次读取人物列表时自动迁移：给旧 YAML 补 ID；只有 `profile.md` 的人物生成最小元数据；保留未知自定义字段；人物 Markdown、章节正文和大纲原文均不改写。
- [x] 迁移前将原始 YAML 内容保存到 `.aisync/migration_backups/`，迁移结果保存到 `.aisync/character_migration.json`；重复 ID 会安全重分配，损坏 YAML 只报告不覆盖，重复执行不会重复生成 ID 或快照。
- [x] 新增统一姓名/别名解析能力和 `POST /api/story/characters/resolve`：无歧义名称返回永久 ID，未知名称进入 `unresolved`，重名或共享别名进入 `ambiguous`，不进行猜测式绑定。
- [x] 人物工作台显示永久 ID 和最近迁移摘要，明确提示迁移未修改自由文本；人物实体专项 `10 passed`，后端全量 `236 passed`，Ruff、前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-31）
- [x] 大纲节点、伏笔和章节元数据统一支持 `character_ids`；章节与伏笔写入原有元数据文件，大纲人物关联旁路保存到 `plot/outline-meta.yaml`，不污染 `plot/outline.md`。（完成时间：2026-07-31）
- [x] 前端增加通用人物引用控件：可按永久 ID 添加、移除、点击跳转人物档案；归档人物保留可见引用，未知 ID 不静默丢失；章节、伏笔、大纲工作台均已接入。（完成时间：2026-07-31）
- [x] 旧版 JSON 大纲兼容人物关联读取；补充移动端人物引用布局约束。后端全量 `239 passed`，前端生产构建、`git diff --check` 和 `1280x720` / `390x844` 页面验收通过。（完成时间：2026-07-31）
- [ ] 后续再评估人物关系与跨文件影响视图；当前不做正文人名自动扫描、不改写自由 Markdown、不引入关系图谱。

## 2026-07-31 AGENT.md 即时生效链路修复

- [x] 修复设置页首次加载与保存并发时，旧读取结果可能覆盖刚保存的 AGENT.md 内容的问题；所有读取和保存均按请求版本提交状态。
- [x] 保存 AGENT.md 后立即清除同一项目的闲置 Agent 缓存，避免后续辅助调用持有过期系统提示词。
- [x] HTTP Agent 运行入口改为按轮新建 Agent，与 WebSocket 主链路一致；当前生成中的一轮保持原提示词，下一条消息必定重新读取最新 AGENT.md。
- [ ] 后续在真实项目中确认：保存 AGENT.md 后直接发送下一条对话，运行审计应显示 `System: default+AGENT.md` 并使用最新内容。

## 2026-07-31 对话实时事件会话隔离

- [x] 修复切换对话后，旧会话的流式回复、工具状态和运行状态被追加到当前聊天面板的问题；WebSocket 实时事件现在统一携带 `conversation_id`。
- [x] 前端仅渲染当前会话的实时事件；其他会话完成后仍正常写入各自历史，切回原会话即可查看完整结果。
- [x] 防止迟到的会话初始化事件把用户从手动切换后的对话拉回旧会话；运行状态事件也按会话过滤。
- [x] 补充运行事件会话 ID 回归测试；相关后端测试 `22 passed`、Ruff、前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-31）

## 2026-07-31 Agent 写入工具链治理

- [x] 根据真实失败运行 `c36fc6f12b73425898b568cdb4cc7569` 定位根因：Agent 同时调用世界观专用写入与通用改动包，后端将两者全部拦截；后续精确文本替换未命中，最终在没有落盘的情况下消耗约 15 万 Token 和 6 次联网额度。
- [x] Agent 的普通已有文件修改统一收口到 `read_project_files -> file_change_proposal -> 差异确认`；`edit_chapter`、`update_worldview`、`outline_generate` 仍保留在工具中心供直接使用，但不再与通用改动包一起暴露给主 Agent。短章节写作、长章节草稿、人物创建等有独立结构化语义的能力继续保留。
- [x] 同批多写入冲突仍坚持整批不执行，避免部分成功；同一冲突批次现在只记录和展示一个真实错误，其余调用合并为同批重复，不再把一次决策错误放大成多个失败。
- [x] `read_project_files` 的全文、分段和 inspect 结果均返回文件 SHA-256；`file_change_proposal` 新增 `replace_lines`，按哈希和包含式行范围替换局部内容。文件已变化、哈希过期或行号越界时拒绝生成改动包，解决 `replace_text` 因标点或空白漂移零命中的问题。
- [x] 工具续轮仅压缩可重新获取的超长 `web_search`、`search_project`、`consistency_check` 结果；`read_project_files` 返回的正文和精确行段保持完整，避免为了省 Token 让模型失去待修改原文。
- [x] `human_intervention` 和 `awaiting_choice` 不再保存为 `completed`，统一落为 `waiting_user`；对话历史、运行条和聊天状态显示“等待选择”，用户仍可继续发送指令或处理结构化选择。
- [x] 新增写入入口收口、搜索结果压缩、哈希行补丁、旧哈希拒绝、冲突批次合并和等待人工状态测试；后端全量 `251 passed`，Ruff、前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-31）
- [ ] 下一步在真实小说项目中验收“一次请求同时修改章节正文与世界观”：应只生成一个多文件差异改动包，确认后逐文件核验，联网结果不应在每个续轮重复占满上下文。
- [ ] 验收稳定后评估是否将已从主 Agent 隐藏的三套旧写入工具标记为“工具中心专用”，并清理系统提示词中已经不再需要的兼容说明，继续降低模型选择成本。

## 2026-07-31 OpenAI 思考强度映射修正

- [x] 修复 OpenAI 兼容链路只允许 `low / medium / high`，导致设置页“超高 (`xhigh`)”与“最高 (`max`)”均静默降级为 `high` 的问题。
- [x] GPT-5 系列及其他支持 `reasoning_effort` 的 OpenAI 兼容模型现在按 `low / medium / high / xhigh` 原样发送；AiSync 的 `max` 在 OpenAI 链路映射为其最高合法值 `xhigh`。
- [x] DeepSeek 继续限制为 `low / medium / high`，Anthropic 继续使用自身的 `low / medium / high / max`，不改变其他供应商行为。
- [x] 补充 GPT-5.6 五档映射回归测试，专项 `18 passed`。（完成时间：2026-07-31）

## 2026-07-31 连续章节工作流

- [x] 新增“连续写章”创建入口，可设置起止章节、卷目录、每章目标字数、通用要求、写作模型和是否覆盖已有章节；单批限制为 20 章，默认不覆盖正式正文。
- [x] 连续任务按章节动态展开为独立 `chapter` 步骤；每章是一个持久化检查点，成功章节不会因后续失败或重试再次执行。
- [x] 每章由一次模型请求生成正文、伏笔动作和长期事实快照，并复用章节联合改动链路写入 `chapters/`、`plot/foreshadows.json` 与 `plot/facts/`。
- [x] 下一章只注入相关向量片段、活跃伏笔、前一章正文尾部和前一章事实，不累计携带此前所有章节全文；工作流记录也不重复保存整章正文。
- [x] 前端支持连续执行、只运行下一步、完成当前步后暂停、失败后重试当前章和跳过当前章；批次显示当前章节、完成章数和进度。
- [x] 每个章节步骤保留独立 LLM 预设和提示词包，可在执行前单独改用其他模型；为以后特殊章节使用 Grok 等模型保留入口，不引入 Agent 集群或场景分段。
- [x] 卷目录与章节路径校验下沉到执行层，手工新增步骤也不能绕过 `chapters/<卷>/ch-NNN.md` 写入边界。
- [x] 新增动态范围、接口路由、步骤模型继承、逐章落盘、有限上下文、事实同步和默认防覆盖测试；后端全量 `260 passed`，专项回归 `16 passed`，相关 Ruff、前端生产构建与 `git diff --check` 通过。（完成时间：2026-07-31）
- [ ] 下一步用真实项目连续生成 3 章，重点验收章节衔接、模型 JSON 稳定性、暂停生效时机与失败重试；通过后再增加“指定章节覆盖批次默认模型”的紧凑配置，不扩展成分段写作系统。

## 2026-08-01 Agent 检索与补丁恢复治理

- [x] 根据真实失败运行 `18b7283fd9e24b839721ed1a4681214e` 定位根因：首轮并行执行 1 次精读和 5 次搜索，旧版全局 3 次只读额度被一次耗尽，后续精读工具被隐藏；模型只能根据 120 字搜索摘要猜测精确原文，两次 `replace_text` 零匹配后触发人工介入。该轮未修改正式文件，但消耗约 9.7 万 Token。
- [x] 删除“全部只读工具共用固定 3 次额度”的粗粒度限制，改为动态治理：同一工具批次最多执行 3 个模糊搜索；额外并行搜索只作为已跳过结果返回，不算真实失败，也不影响 `read_project_files`。
- [x] 连续两批只有模糊检索、没有精确读取或文件状态变化时，下一轮暂停搜索工具；精确文件读取、修改工具和最终回答始终保留。成功精读或文件状态变化会解除暂停，不对精读设置低次数硬上限，复杂跨文件任务继续由 12 轮总上限、重复调用检测和防空转机制兜底。
- [x] `file_change_proposal` 遇到 `replace_text` 非唯一匹配、文件哈希变化、行号失效或大纲区块漂移时进入定向恢复：下一轮只暴露 `read_project_files`，要求读取报错路径的正文或最新大纲索引；成功读取后恢复补丁工具，并提示优先使用 `source_hash + replace_lines` 或最新大纲区块 ID。
- [x] 初始向量检索扩大候选池后按 chunk ID、同路径重复内容和单路径最多 3 段去重；运行审计记录去重前后数量，避免最高上下文模式被同一章节的重叠片段占满。
- [x] 失败工具运行记录新增脱敏参数摘要：保存改动路径、操作类型、截断后的 `old_text`、行号、哈希和新内容字符数，不保存完整新正文；后续可直接诊断模型实际提交了什么。
- [x] 新增并行搜索限流、连续探索暂停、精读保留、补丁零匹配恢复、哈希行补丁落盘、向量去重和失败参数审计测试；后端全量 `264 passed`，相关 Ruff 与 `git diff --check` 通过。（完成时间：2026-08-01）
- [ ] 下一步在真实小说项目中重试一次“多处章节局部修改”：运行审计应显示每批搜索不超过 3 次；若补丁原文漂移，应自动读取报错文件并生成一个可应用的多文件差异包，不再直接进入人工介入。

## 2026-08-01 进行中对话刷新恢复

- [x] 定位刷新后工具调用记录消失的双重根因：工具开始/结束事件只保存在前端内存；后端运行记录只在工具结束后追加一条摘要，无法表示刷新瞬间仍在执行的工具。
- [x] Agent 工具事件新增稳定 `call_id`；运行记录按同一 ID 保存 `running -> completed/failed` 生命周期、开始/结束时间、耗时和脱敏参数摘要。工具执行中刷新页面时，最新 run 接口可以恢复当前正在调用的工具。
- [x] 前端从 `activeRun.tool_calls` 重建刷新前的工具轨迹，并与刷新后的实时事件按 `call_id` 去重；记录继续放在默认折叠的“运行细节”区域，展开后完整显示并在固定高度内滚动，不重新挤占聊天正文。
- [x] 运行期间的状态、工具、流式文本、改动包和最终回复由“仅发送到发起请求的旧 WebSocket”改为项目连接广播，并保留 `conversation_id` 隔离；页面刷新建立新连接后可以继续接收原任务后续输出。
- [x] WebSocket 断开后的 `receive_json()` 同时处理 `RuntimeError/OSError`，避免正常刷新产生“WebSocket is not connected”日志噪声。
- [x] 新增工具运行中状态落盘与同 ID 完成更新测试；后端全量 `264 passed`，相关 Ruff、前端生产构建和 `git diff --check` 通过。（完成时间：2026-08-01）
- [ ] 真实验收：发送一个会调用多个工具的请求，在任一工具执行中刷新页面；运行细节应立即恢复已完成及正在执行的工具，后续流式回复与最终消息应继续出现在原对话。

## 2026-08-01 局域网开发启动时序修复

- [x] 定位 Vite `ws proxy error: ECONNREFUSED 127.0.0.1:27631`：后端重载子进程约 7 秒后才就绪，原脚本未等待健康检查便启动前端，浏览器因此在启动窗口内连接失败。
- [x] `scripts/dev_lan.ps1` 改为后端返回 `{"status":"ok"}` 后才启动 Vite；启动进程提前退出或 60 秒超时时直接中止，并显示最近 30 行后端日志。
- [x] 健康检查不再把任意 HTTP 200 服务误判为 AiSync 后端；修复仅有一个可用地址时局域网 IP 被截成首字符 `1` 的问题。
- [x] PowerShell 语法、后端提前退出诊断、局域网地址选择与现有 `/health` 接口验证通过。（完成时间：2026-08-01）

## 2026-08-01 Vite 公网隧道 Host 放行

- [x] 精确放行当前 Cpolar 域名 `480fccdf.r9.cpolar.top`，解决 Vite `Blocked request: host is not allowed`。
- [x] 新增 `AISYNC_VITE_ALLOWED_HOSTS` 环境变量，可用英文逗号追加后续隧道域名；不使用 `allowedHosts: true`，也不默认放行整个公共域名后缀。
- [x] 前端生产构建和 `git diff --check` 验证通过。（完成时间：2026-08-01）
- [x] Web-first 公开发布前移除个人 Cpolar 临时域名的默认硬编码；公网隧道域名只通过 `AISYNC_VITE_ALLOWED_HOSTS` 显式配置，避免把本机部署信息带入 GitHub 发布快照。（完成时间：2026-08-01）
- [x] 提交清单审计移除旧 npm 误生成并被 Git 跟踪的空文件 `frontend/aisync-frontend@0.1.0`；确认 `.release/`、前端构建产物、虚拟环境、本机 `.env`、日志和项目数据均由忽略规则排除。（完成时间：2026-08-01）

## 2026-08-01 WebSocket 隧道稳定性治理

- [x] 定位频繁断连由多类事件叠加：Cpolar 公网连接抖动、Vite 热更新/页面切换的正常重连，以及旧后端将断线后的读取打印为 ASGI 异常。
- [x] Agent WebSocket 新增 15 秒应用层心跳；后端仅返回 `pong`，心跳不进入聊天事件、对话历史或 Agent 运行记录。
- [x] 前端重连退避改为连接持续稳定 30 秒后才归零；短连接连续断开时按 1/2/4/8/10 秒退避，避免每秒连接放大隧道抖动。
- [x] 增加连接实例隔离，旧连接的迟到关闭事件不会清空新连接状态或重复安排重连。
- [x] 定位开发后端未更新的进程根因：旧 `--reload` 父进程退出后遗留 Python 子进程占用 `27631`，后续 `npm run dev` 因健康检查成功而持续复用旧代码。
- [x] LAN 启动脚本现在会清理自身启动的完整后端进程树，并识别“监听所有者已消失、同环境 Python 子进程仍存活”的遗留后端，避免旧代码继续占用端口。
- [x] 心跳专项测试、Ruff、前端生产构建、`git diff --check` 与后端全量测试通过（`265 passed`）。（完成时间：2026-08-01）
- [x] 清理旧后端并重启实际开发服务；本机 WebSocket 与 `wss://480fccdf.r9.cpolar.top` 公网链路均成功返回 `pong`，HTTPS 返回 200，当前日志无新增 ASGI 断线堆栈。
- [ ] 验证：通过 Cpolar 保持页面空闲 5 分钟，再发送一次 Agent 请求；连接状态应保持稳定，若隧道主动断开应自动恢复且不丢失后台运行结果。

## 2026-08-01 Agent 刷新不中断与流式草稿恢复

- [x] 根据真实失败运行 `5ecd8a58d02e43a88bc6fc90fccc769b` 定位根因：页面刷新关闭旧 WebSocket 后，广播仍向旧连接发送；`WebSocketDisconnect` 未被连接管理器隔离，异常沿流式回调冒泡并被误报为模型请求失败。
- [x] WebSocket 广播改为连接快照遍历，统一隔离 `WebSocketDisconnect / RuntimeError / OSError`；失效连接会被移出连接池，其他页面连接和后台 Agent 继续运行。
- [x] Agent run 新增带版本号的流式草稿；每个文本增量先更新内存草稿，约每 350ms 或 512 字符落盘，工具阶段、失败、中断和最终收尾时强制保存。失败或中断保留草稿，正常完成后以正式对话历史为准并清除副本。
- [x] 前端在页面刷新和 WebSocket 重连后主动读取当前对话最新 run，恢复已输出正文和工具生命周期；实时增量按 `run_id + stream_version` 去重，并处理历史加载、run 快照和新流片段的先后竞态。
- [x] 新增失效连接隔离、跨 Store 草稿恢复、阶段更新不覆盖草稿、失败保留与完成清理测试；后端全量 `268 passed`，Ruff 与前端生产构建通过。（完成时间：2026-08-01）
- [ ] 真实验收：发起会先输出文字再调用工具的请求，在文字和工具均可见后刷新；已有内容与工具状态应立即恢复，后台继续生成，最终回复不重复，运行记录不得再出现 `WebSocketDisconnect`。

## 2026-08-01 DeepSeek Thinking 工具续轮修复

- [x] 根据真实失败运行 `83b9a78b33394a0fa299d7f4c48de4a3` 定位根因：前 11 次流式工具请求临时关闭 thinking，第 12 次无工具收尾重新开启 thinking；历史 assistant 工具调用因此没有 `reasoning_content`，DeepSeek 在收尾请求中返回 400。
- [x] DeepSeek 新工具运行不再因为“流式 + 工具”自动关闭 thinking；流式响应持续提取 reasoning 增量，Agent 将其与 assistant 工具调用一起保存并在下一次请求原样回传。
- [x] 增加旧链路兼容：若请求历史中已经存在带工具调用但缺失 `reasoning_content` 的 assistant 消息，则该次及后续收尾自动使用非 thinking，并剔除历史中混入的 reasoning 字段，避免再次触发协议错误。
- [x] 新增 thinking 工具流启用、完整 reasoning 历史续传、缺失历史降级、流式 reasoning 提取与工具调用回传测试；专项 `22 passed`，后端全量 `272 passed`，Ruff 与 `git diff --check` 通过。（完成时间：2026-08-01）
- [ ] 真实验收：使用 `deepseek-v4-flash` 开启 thinking，执行至少两轮工具调用后正常生成最终回复；运行审计中的各次模型请求均应完成，不再出现 `reasoning_content` 400。

## 2026-08-01 文件补丁多行段治理

- [x] 根据最新运行 `74945ede57cc4162bfd0021c894f178a` 定位停滞原因：DeepSeek thinking 的 6 次模型请求均成功，但补丁先误用 `new_content`，随后因同一文件包含 3 个 `replace_lines` 被工具硬限制拒绝；本轮未生成或应用任何改动包，却消耗约 15.2 万 Token。
- [x] `file_change_proposal` 允许同一文件一次提交最多 24 个不重叠的 `replace_lines`；所有行段必须基于同一个原始 `source_hash`，收集完成后按行号倒序应用，避免前序替换导致后续行号漂移。
- [x] `replace_lines` 兼容模型误传的 `new_content` 字段并归一化为局部替换文本；空文本仍表示删除目标行段，因此无需再增加独立的“删除文本”工具。
- [x] 明确禁止同一文件内混用 `replace_lines` 与 `replace_text / append_text / prepend_text`；重叠行段、过期哈希、越界行号和超过数量上限继续拒绝，差异预览与人工确认链路不变。
- [x] 新增多行段乱序替换、空文本删除、`new_content` 兼容、重叠范围拒绝和混合补丁拒绝测试；专项 `27 passed`，后端全量 `275 passed`，Ruff 与 `git diff --check` 通过。（完成时间：2026-08-01）
- [ ] 真实验收：在刚才的角色档案对话中选择“重试本轮”，Agent 应生成一个包含 3 个行段修改的差异包，不再因字段名或同文件多段限制进入人工介入。

## 2026-08-01 Web-first GitHub 发布治理

- [x] 评估当前仓库发布状态：后端全量 `275 passed`、前端生产构建通过；确认主要阻塞来自 122 文件的大迁移尚未形成受控 Git 快照、版本号陈旧和发布资产链路缺失。
- [x] 发布基线统一为 `0.2.0`：根目录 `package.json`、前端 `package.json/package-lock.json` 和后端 `pyproject.toml` 使用同一版本；移除已废弃的 PyInstaller 可选依赖。（完成时间：2026-08-01）
- [x] 新增 `scripts/check_release.ps1`：检查三处版本一致、tag 版本匹配、SemVer 合法，并阻止旧 Tauri 目录或 PyInstaller 依赖回流。
- [x] 新增预构建 Web 发布包：`npm run release:web` 构建前端后输出 `.release/AiSync-web-v0.2.0.zip` 和 SHA256；发布包仅包含运行所需后端源码、静态前端、启动脚本、许可证和版本说明。
- [x] 发布包用户链路不再要求 npm：首次运行 `start.ps1` 会通过 `setup.ps1` 检查 Python 3.11、创建包内 `.venv` 并安装后端依赖；开发者源码构建仍使用 npm。
- [x] 新增 `.github/workflows/release-web.yml`：推送 `v*` tag 时在干净 Windows Runner 上校验版本、安装依赖、运行后端测试、重新构建 ZIP并创建或更新 GitHub Release；普通代码推送不会自动发版。
- [x] 新增 `CHANGELOG.md`，并同步 README、技术细节、发布流程、打包链路和项目报告，明确 Web-first、无桌面安装器、Python/npm 边界与局域网无鉴权限制。
- [x] 完成真实发布包验收：修复版本检查退出码误判、空 ZIP、Windows Store Python 占位程序和 Python 探测问题；发布 ZIP 解压后无缓存、日志、项目数据或 npm 依赖，SHA256 一致，使用全新 `.venv` 完成依赖安装，并在不调用 npm 的情况下启动后端、返回 `/health=ok` 和首页 200。（完成时间：2026-08-01）
- [x] 发布前回归：后端全量 `275 passed`、Ruff、前端 `0.2.0` 生产构建、PowerShell 语法、发布版本检查和 `git diff --check` 通过。
- [x] 首次 tag 工作流在发布包 SHA256 阶段失败：GitHub Windows Runner 的 `powershell.exe` 未提供 `Get-FileHash`；构建脚本改用 .NET `SHA256.ComputeHash`，兼容 Windows PowerShell 与 pwsh，本地发布包重新验证通过。（完成时间：2026-08-01）
- [x] 第二次 tag 工作流已通过 275 条测试和 Web ZIP 构建，但 `upload-artifact` 默认忽略点号目录，未发现 `.release/` 资产；工作流限定发布路径并启用 `include-hidden-files`，不扩大上传范围。（完成时间：2026-08-01）
- [x] 完成发布提交清单审计并形成受控 Git 快照：移除个人隧道域名与旧 npm 空文件，183 个源码、测试、文档和删除项进入 `feat: establish web-first v0.2.0 release` 提交；没有提交本地配置、运行数据或构建产物。（完成时间：2026-08-01）
- [x] 完成 GitHub 发布：整合远端仅有的 Apache LICENSE 历史提交但保留当前 PolyForm Noncommercial 许可，将 Web-first 主线推送到 `main`；`v0.2.0` tag 工作流最终成功，Release 已公开并包含 Web ZIP 与 SHA256。（完成时间：2026-08-01）

## 2026-08-02 README 社区入口

- [x] README 新增 QQ 交流群 `370872213`，不调整项目版本号。（完成时间：2026-08-02）
