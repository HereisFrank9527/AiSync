# AiSync 项目汇总

更新时间：2026-05-19
仓库：`https://github.com/HereisFrank9527/AiSync`
状态：进行中

## 项目报告

AiSync 是一个面向长篇小说创作的本地桌面工具，当前主线已经完成了从“工具中心”到“Agent 协作工作台”的一轮重构。

### 当前形态

- 桌面端：Tauri 2 + React + TypeScript
- 后端：Python FastAPI
- 执行方式：主 Agent 通过工具系统读写项目文件，前端通过 WebSocket 接收流式事件
- 打包方式：安装包内置 Python runtime，不要求用户自己装 Python

### 已完成的关键工作

- 工具系统从固定入口重构为 descriptor 驱动。
- 前端支持 Markdown / AI 渲染、任务列表、工具中心、章节/角色/世界观/大纲工作区。
- Agent 对话增加动态反馈、重复消息抑制、可选方案按钮、任务计划模板。
- 安装版改为动态端口，不再死锁 `8000`。
- 安装版可从 GitHub Releases 做轻量更新检查。
- NSIS 安装前会清理旧版 AiSync 和随包 Python 进程，减少覆盖安装锁文件问题。

### 当前已知问题

- 安装版黑框问题暂时搁置，优先级降低。
- 轻量更新只做到“检查并打开下载页”，还没接自动下载/签名更新。
- 设置页已改为内部侧栏分页；后续可继续微调单页表单密度。

### 当前打包规则

1. 修改 `frontend/src-tauri/tauri.conf.json` 的版本号。
2. 运行 `cd frontend && npm run tauri build`。
3. 到 GitHub Releases 发版，上传安装包。
4. 前端设置页可以用“检查更新”查看最新 release。

## 规划

### 默认连续作业队列

以后用户说“继续”时，默认按 `规划.local.md` 的“连续作业队列”推进，不再每次停下来问下一步。

1. 发布链路闭环：发布检查清单、发布流程文档、更新 UI 反馈、GitHub Release asset 选择、日志目录入口。
2. Agent 运行可靠性：run 记录持久化、前端状态恢复、失败重试、中断后状态收敛。
3. Agent 工作流与提示词管理：提示词来源审计、提示词包、步骤级模型选择、写章节工作流。
4. 小说对象工作台：章节关联大纲、大纲拖拽排序、角色关系图、世界观文档管理。
5. 工具系统增强：ToolRun 阶段、工具详情页、参数校验中文化、执行前 diff 预览。
6. 向量化增强：索引详情、检索命中原因、一致性检查降噪、写章节相关设定包。
7. 项目治理：正式 LICENSE、CHANGELOG、ignore 核对、旧文档迁移确认。

## 任务记录

- 2026-05-13：整理 `规划.md` 与项目现状，生成本地汇总文件 `项目.local.md`。
- 2026-05-14：补充连续作业队列，后续默认按队列推进。
- 2026-05-14：完成发布链路闭环第一轮：新增 `发布流程.md`、发布摘要脚本、安装版冒烟脚本，设置页增加更新 asset 信息、诊断复制和日志目录入口。
- 2026-05-15：推进 Agent 运行可靠性：新增 Agent run 持久化、前端 run 状态恢复、失败恢复入口和空 assistant message 防御。
- 2026-05-15：完成 Agent 运行可靠性 B 队列：实时事件压缩、记忆状态展示增强，后端测试扩展到 18 条。
- 2026-05-15：完成小说对象工作台 C1-C3：章节可关联大纲节点，大纲支持节点状态和拖拽排序，基础信息页显示大纲完成度。
- 2026-05-15：新增伏笔管理基础版：`plot/foreshadows.json` 持久化、侧边栏伏笔工作区、状态/重要性/章节/大纲关联、基础信息页伏笔回收统计。
- 2026-05-15：修正 Agent run 报告展示位置：运行报告不再进入聊天框，改由 Agent 任务区显示状态、run_id、输入摘要和工具调用结果。
- 2026-05-15：修复检查更新误报：release tag/name 会先提取数字版本再比较，同号或低版本不再提示发现新版本。
- 2026-05-15：新增 `项目文档/Agent能力.md`，记录当前 Agent 对话、记忆、工具、对象工作台、向量检索和能力边界。
- 2026-05-15：设置页重构为内部侧栏分页，预设管理、LLM 配置、Agent 行为、应用更新、诊断日志分屏显示。
- 2026-05-15：伏笔智能提醒推进第一步：章节页根据当前章节、回收章节、关联大纲节点和相关文件展示相关伏笔分组。
- 2026-05-15：伏笔智能提醒接入 Agent/写作工具：写作相关请求会注入 `plot/foreshadows.json` 压缩上下文，写作/编辑工具 AI 生成会提醒模型处理埋设、推进和回收。
- 2026-05-15：伏笔提醒可解释化：章节页显示“优先回收/埋设/推进”等建议动作，Agent 注入上下文附带命中原因。
- 2026-05-15：完成世界观文档重命名/删除：后端增加安全移动/删除接口，前端世界观工作区提供文件影响确认，并在操作后刷新文件树、概览和向量索引。
- 2026-05-15：新增世界观主题模板：世界观侧栏可创建或打开地理、历史、势力、技术/魔法、规则、年表等常用设定文档。
- 2026-05-15：正修 Markdown 列表渲染：前端 MarkdownView 支持递归嵌套列表，并兼容 AI 常见的“冒号后无缩进子弹列表”，避免后续有序列表全显示为 1。
- 2026-05-15：修复 DeepSeek thinking 模式工具调用续轮报错：OpenAI-compatible 客户端会收集并回传 `reasoning_content`，避免 400 invalid_request_error。
- 2026-05-15：继续加固 Markdown 列表渲染：相邻有序列表自动续接，冒号后的相邻无序列表会归入上一条有序列表项。
- 2026-05-15：DeepSeek 思考模式改为可控：OpenAI-compatible 客户端对 DeepSeek V4 发送 `thinking enabled/disabled`，设置页提示可关闭思考模式规避 reasoning_content 工具调用错误；旧 `deepseek-reasoner` 保持不回传 reasoning_content。
- 2026-05-16：继续加固 DeepSeek thinking 工具调用：thinking+tools 场景暂时禁用流式以确保拿到完整 `reasoning_content`，并从 SDK 对象、`model_extra`、`model_dump()` 多路径提取推理字段。
- 2026-05-16：修复聊天历史和设置页滚动布局：应用壳、聊天工作区、历史抽屉和设置内容区改为受控高度与内部滚动，避免面板撑爆后无法向下翻。
- 2026-05-16：大纲工作台拆分结构视图/原文视图：`plot/outline.md` 完整保留展示但不再自动污染结构节点；新增从 Markdown 严格导入章节节点接口与前端入口。
- 2026-05-16：优化 LLM 模型选择：设置页模型名增加显式下拉选择，刷新失败时显示真实错误；后端模型列表失败但已有当前模型时会返回当前模型作为 fallback。
- 2026-05-16：精简 Agent 工具结果展示：`tool_result` 默认折叠，展开后只显示结构化渲染或 Markdown fallback，避免 `list:search_results` 等内容重复刷屏。
- 2026-05-16：修复 Agent 任务流程栏稳定性：前端保留本轮最新任务列表，减少新会话或历史刷新导致任务流程消失的情况。
- 2026-05-16：收敛打包入口：新增仓库根目录 `package.json`，以后 `npm run build` 直接走 Tauri 发布打包，`npm run build:frontend` 仅做前端静态构建。
- 2026-05-16：修复安装版流式渲染崩溃：MarkdownView 支持 `### ` 这类半截标题，避免 `RangeError: Invalid array length`；Agent 任务面板增加状态事件兜底四步流程。
- 2026-05-16：修复 dev/安装包不一致的发布链路：确认旧安装版嵌入旧 JS 资产，发布摘要新增 dist 资产嵌入校验；Tauri bundle 目标收敛为 NSIS，版本升至 `0.1.8` 并已生成新安装包。
- 2026-05-16：新增 `项目文档/打包链路.md`，系统记录开发态、发布态、安装态的资源来源、命令入口、诊断路径和发布前检查清单。
- 2026-05-16：优化安装版启动体验：Windows release 主程序改为 GUI 子系统以消除空终端；前端 HTML fallback 和 React 后端等待页增加启动动画。
- 2026-05-18：Agent/Markdown 渲染支持 `<think>` 思考块：完整或流式未闭合内容都会默认折叠为一行，展开后再显示思考内容，避免占用大面积聊天空间。
- 2026-05-18：完善打包链路说明并新增 `scripts/clean_build_artifacts.ps1`：默认保留最新 NSIS，清理旧安装包、MSI、wix、dist、日志和 runtime 缓存，支持 `-DryRun` 预览。
- 2026-05-18：规划自由文件区：新增 `H. 自由文件区`，目标是在每个项目内提供 `temp/` 临时工作区，Agent 可完整读写，正式归档和外部同步走确认流程。
- 2026-05-18：确认自由文件区先手顺序：优先做项目内 `temp/` 最小闭环，先完成目录初始化、文件树标记和常见文本文件编辑，再进入 Agent 专用 temp 工具。
- 2026-05-18：完成自由文件区 H1-H3：项目初始化会创建 `temp/inbox|drafts|exports|notes` 和 `.aisync-temp.json`；文件树显示“自由区”标记；`temp/**` 支持常见文本文件新建、编辑、重命名和删除，删除/改名接口限定在 `temp/` 内。
- 2026-05-19：收紧自由区和公开文件 API 安全边界：只允许 `.md/.txt/.json/.yaml/.yml/.csv`，过滤 `.aisync`、`.vectordb`、点号内部路径和 `temp/.aisync-temp.json`，避免自由区变成任意文件入口。
- 2026-05-19：新增正式 `LICENSE`：采用 PolyForm Noncommercial License 1.0.0；README 已链接协议并明确商用、付费分发、二次发布、转售、嵌入商业产品或托管服务均需书面授权，第三方依赖仍按各自许可证。
- 2026-05-19：规划 Agent 大改方向：不直接做自由 Agent 集群，先建立可见的 WorkflowRun/WorkflowStep；主 Agent 负责编排，步骤可选择 LLM 预设、挂载提示词包、设置人工确认点，输出优先进入 `temp/drafts`。
- 2026-05-19：确认提示词透明度问题：当前提示词来源分散在默认 system prompt、LLM 预设覆盖、会话记忆、向量上下文、伏笔上下文和工具描述中；规划新增提示词来源审计、提示词预览和提示词包管理。
- 2026-05-19：优化启动等待页：不做虚假进度条，改为轮换启动文案、显示已等待秒数，超过 8 秒提供复制诊断和打开日志入口。
- 2026-05-19：发布打包至 0.1.9：执行 `npm run build` 成功，生成 NSIS 安装包 `frontend/src-tauri/target/release/bundle/nsis/AiSync_0.1.9_x64-setup.exe`，release summary 确认当前前端 JS/CSS 已嵌入 release exe。
- 2026-05-19：完成提示词来源审计 I1：AgentRun 新增 `prompt_audit` 摘要级元数据，记录 system prompt 来源、记忆摘要/近期消息、向量上下文、伏笔上下文和可用工具数量；前端 Agent 任务区展示审计标签，但暂不暴露完整 prompt 正文。
- 2026-05-19：修正提示词方向：用户要的是“提示词管理”而不是单纯暴露运行 prompt；新增全局提示词包存储 `~/.aisync/prompt_packs.json`、`/api/prompt-packs` CRUD、设置页“提示词管理”分页，支持文风/写作/规划/润色/检查/特殊片段等分类的新增、编辑、复制、启停和删除。
- 2026-05-19：新增 `项目文档/提示词技术.md`：确定提示词系统三分法，Prompt Pack 管理模型行为规则，Context Pack 管理结构化上下文注入，Workflow Step 管理步骤级模型、提示词包、上下文包、输出位置和人工确认；明确不采用“人物/伏笔/大纲/世界观全塞进 prompt”的方案。
- 2026-05-20：Prompt Pack 接入主 Agent 对话：已启用且适用阶段包含 `chat` 的提示词包会自动作为长期行为规则注入，不需要每轮手动选择；AgentRun 审计记录本轮使用的提示词包数量和名称。
- 2026-05-20：Prompt Pack 接入章节工具：`write_chapter` 自动使用 `chapter_draft` 阶段提示词包，`edit_chapter` 自动使用 `revision` 阶段提示词包；ToolRun 结果元数据记录本次使用的提示词包阶段、数量和名称。新增 `backend/app/core/prompt_pack_rendering.py` 统一渲染/审计逻辑，并补充后端回归测试。
- 2026-05-20：完成项目级 Prompt Pack 默认启用关系：每个项目可保存 `.aisync/prompt_pack_settings.json`，在设置页选择“沿用全局启用”或“只使用本项目勾选项”；主 Agent 对话、写章节和编辑章节都会按项目设置过滤提示词包，防止不同小说项目串文风。
- 2026-05-20：完成 WorkflowRun/WorkflowStep 持久化骨架：新增后端 `WorkflowRunStore` 与 `/api/workflows`，项目内记录写入 `.aisync/workflows/{run_id}.json`；前端新增“工作流”侧栏页，可创建章节草稿流程模板并查看步骤状态。当前尚未自动执行步骤，下一步进入写章节工作流最小执行器。
- 2026-05-20：完成章节草稿工作流最小执行器：`/api/workflows/{run_id}/run-next` 可推进上下文检索、章节计划、用户确认和草稿生成；`/confirm` 可确认等待中的人工步骤；草稿输出到 `temp/drafts/{run_id}.md`。前端工作流页增加“运行下一步”和“确认并继续”。
