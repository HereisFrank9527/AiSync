# AiSync 项目汇总

更新时间：2026-07-04
仓库：`https://github.com/HereisFrank9527/AiSync`
状态：进行中

## 项目报告

AiSync 是一个面向长篇小说创作的本地 Web 工具，当前主线已经完成了从“工具中心”到“Agent 协作工作台”的一轮重构，并在 2026-07-04 正式从 Tauri 桌面壳切换为 Web-only 形态。

### 当前形态

- 前端：React + Vite
- 后端：Python FastAPI
- 执行方式：主 Agent 通过工具系统读写项目文件，前端通过 WebSocket 接收流式事件
- 运行形态：本地 Web / 局域网 Web 优先，FastAPI 可直接托管构建后的 React 前端
- 打包方式：当前只构建 `frontend/dist/`，不再生成 Tauri/NSIS 安装包
- 项目管理：默认使用程序管理的项目库 `~/.aisync/projects`，支持新建、导入 `.aisync.zip`、导出 `.aisync.zip`；不把项目放进安装目录

### 已完成的关键工作

- 工具系统从固定入口重构为 descriptor 驱动。
- 前端支持 Markdown / AI 渲染、任务列表、工具中心、章节/角色/世界观/大纲工作区。
- Agent 对话增加动态反馈、重复消息抑制、可选方案按钮、任务计划模板。
- Web 开发模式改为少见端口 `27631`，局域网访问由 `npm run dev` / `npm run dev:lan` 提供。
- 前端设置页可从 GitHub Releases 做轻量更新检查。
- 已移除 Tauri 桌面壳、安装包脚本和随包 Python runtime 路线。

### 当前已知问题

- 轻量更新只做到“检查并打开下载页”，还没接自动下载/解压/替换。
- 设置页已改为内部侧栏分页；后续可继续微调单页表单密度。
- LAN dev 当前没有登录/鉴权，只适合可信网络临时使用。

### 当前构建规则

1. 运行 `npm run build` 构建 `frontend/dist/`。
2. 运行 `npm run web` 或 `npm run web:start` 由 FastAPI 托管 Web 页面。
3. 到 GitHub Releases 发版，上传源码包、文档包或后续专门设计的 Web runtime 包。
4. 前端设置页可以用“检查更新”查看最新 release。

## 规划

### 默认连续作业队列

以后用户说“继续”时，默认按 `规划.local.md` 的“连续作业队列”推进，不再每次停下来问下一步。

1. 发布链路闭环：发布检查清单、发布流程文档、更新 UI 反馈、GitHub Release asset 选择、日志目录入口。
2. Agent 运行可靠性：run 记录持久化、前端状态恢复、失败重试、中断后状态收敛。
3. Agent 工作流与提示词管理：提示词来源审计、提示词包、步骤级模型选择、写章节工作流。
4. 小说对象工作台：章节关联大纲、大纲拖拽排序、角色关系图、世界观文档管理。
5. 工具系统增强：ToolRun 阶段、工具详情页、参数校验中文化、执行前 diff 预览。
6. 向量化增强：索引详情、检索命中原因、一致性检查降噪、写章节相关设定包。
7. 项目治理：正式 LICENSE、CHANGELOG、ignore 核对、旧文档迁移确认。

## 任务记录

- 2026-05-13：整理 `规划.md` 与项目现状，生成本地汇总文件 `项目.local.md`。
- 2026-05-14：补充连续作业队列，后续默认按队列推进。
- 2026-05-14：完成发布链路闭环第一轮：新增 `发布流程.md`、发布摘要脚本、安装版冒烟脚本，设置页增加更新 asset 信息、诊断复制和日志目录入口。
- 2026-05-15：推进 Agent 运行可靠性：新增 Agent run 持久化、前端 run 状态恢复、失败恢复入口和空 assistant message 防御。
- 2026-05-15：完成 Agent 运行可靠性 B 队列：实时事件压缩、记忆状态展示增强，后端测试扩展到 18 条。
- 2026-05-15：完成小说对象工作台 C1-C3：章节可关联大纲节点，大纲支持节点状态和拖拽排序，基础信息页显示大纲完成度。
- 2026-05-15：新增伏笔管理基础版：`plot/foreshadows.json` 持久化、侧边栏伏笔工作区、状态/重要性/章节/大纲关联、基础信息页伏笔回收统计。
- 2026-05-15：修正 Agent run 报告展示位置：运行报告不再进入聊天框，改由 Agent 任务区显示状态、run_id、输入摘要和工具调用结果。
- 2026-05-15：修复检查更新误报：release tag/name 会先提取数字版本再比较，同号或低版本不再提示发现新版本。
- 2026-05-15：新增 `项目文档/Agent能力.md`，记录当前 Agent 对话、记忆、工具、对象工作台、向量检索和能力边界。
- 2026-05-15：设置页重构为内部侧栏分页，预设管理、LLM 配置、Agent 行为、应用更新、诊断日志分屏显示。
- 2026-05-15：伏笔智能提醒推进第一步：章节页根据当前章节、回收章节、关联大纲节点和相关文件展示相关伏笔分组。
- 2026-05-15：伏笔智能提醒接入 Agent/写作工具：写作相关请求会注入 `plot/foreshadows.json` 压缩上下文，写作/编辑工具 AI 生成会提醒模型处理埋设、推进和回收。
- 2026-05-15：伏笔提醒可解释化：章节页显示“优先回收/埋设/推进”等建议动作，Agent 注入上下文附带命中原因。
- 2026-05-15：完成世界观文档重命名/删除：后端增加安全移动/删除接口，前端世界观工作区提供文件影响确认，并在操作后刷新文件树、概览和向量索引。
- 2026-05-15：新增世界观主题模板：世界观侧栏可创建或打开地理、历史、势力、技术/魔法、规则、年表等常用设定文档。
- 2026-05-15：正修 Markdown 列表渲染：前端 MarkdownView 支持递归嵌套列表，并兼容 AI 常见的“冒号后无缩进子弹列表”，避免后续有序列表全显示为 1。
- 2026-05-15：修复 DeepSeek thinking 模式工具调用续轮报错：OpenAI-compatible 客户端会收集并回传 `reasoning_content`，避免 400 invalid_request_error。
- 2026-05-15：继续加固 Markdown 列表渲染：相邻有序列表自动续接，冒号后的相邻无序列表会归入上一条有序列表项。
- 2026-05-15：DeepSeek 思考模式改为可控：OpenAI-compatible 客户端对 DeepSeek V4 发送 `thinking enabled/disabled`，设置页提示可关闭思考模式规避 reasoning_content 工具调用错误；旧 `deepseek-reasoner` 保持不回传 reasoning_content。
- 2026-05-16：继续加固 DeepSeek thinking 工具调用：thinking+tools 场景暂时禁用流式以确保拿到完整 `reasoning_content`，并从 SDK 对象、`model_extra`、`model_dump()` 多路径提取推理字段。
- 2026-05-16：修复聊天历史和设置页滚动布局：应用壳、聊天工作区、历史抽屉和设置内容区改为受控高度与内部滚动，避免面板撑爆后无法向下翻。
- 2026-05-16：大纲工作台拆分结构视图/原文视图：`plot/outline.md` 完整保留展示但不再自动污染结构节点；新增从 Markdown 严格导入章节节点接口与前端入口。
- 2026-05-16：优化 LLM 模型选择：设置页模型名增加显式下拉选择，刷新失败时显示真实错误；后端模型列表失败但已有当前模型时会返回当前模型作为 fallback。
- 2026-05-16：精简 Agent 工具结果展示：`tool_result` 默认折叠，展开后只显示结构化渲染或 Markdown fallback，避免 `list:search_results` 等内容重复刷屏。
- 2026-05-16：修复 Agent 任务流程栏稳定性：前端保留本轮最新任务列表，减少新会话或历史刷新导致任务流程消失的情况。
- 2026-05-16：收敛打包入口：新增仓库根目录 `package.json`，以后 `npm run build` 直接走 Tauri 发布打包，`npm run build:frontend` 仅做前端静态构建。
- 2026-05-16：修复安装版流式渲染崩溃：MarkdownView 支持 `### ` 这类半截标题，避免 `RangeError: Invalid array length`；Agent 任务面板增加状态事件兜底四步流程。
- 2026-05-16：修复 dev/安装包不一致的发布链路：确认旧安装版嵌入旧 JS 资产，发布摘要新增 dist 资产嵌入校验；Tauri bundle 目标收敛为 NSIS，版本升至 `0.1.8` 并已生成新安装包。
- 2026-05-16：新增 `项目文档/打包链路.md`，系统记录开发态、发布态、安装态的资源来源、命令入口、诊断路径和发布前检查清单。
- 2026-05-16：优化安装版启动体验：Windows release 主程序改为 GUI 子系统以消除空终端；前端 HTML fallback 和 React 后端等待页增加启动动画。
- 2026-05-18：Agent/Markdown 渲染支持 `<think>` 思考块：完整或流式未闭合内容都会默认折叠为一行，展开后再显示思考内容，避免占用大面积聊天空间。
- 2026-05-18：完善打包链路说明并新增 `scripts/clean_build_artifacts.ps1`：默认保留最新 NSIS，清理旧安装包、MSI、wix、dist、日志和 runtime 缓存，支持 `-DryRun` 预览。
- 2026-05-18：规划自由文件区：新增 `H. 自由文件区`，目标是在每个项目内提供 `temp/` 临时工作区，Agent 可完整读写，正式归档和外部同步走确认流程。
- 2026-05-18：确认自由文件区先手顺序：优先做项目内 `temp/` 最小闭环，先完成目录初始化、文件树标记和常见文本文件编辑，再进入 Agent 专用 temp 工具。
- 2026-05-18：完成自由文件区 H1-H3：项目初始化会创建 `temp/inbox|drafts|exports|notes` 和 `.aisync-temp.json`；文件树显示“自由区”标记；`temp/**` 支持常见文本文件新建、编辑、重命名和删除，删除/改名接口限定在 `temp/` 内。
- 2026-05-19：收紧自由区和公开文件 API 安全边界：只允许 `.md/.txt/.json/.yaml/.yml/.csv`，过滤 `.aisync`、`.vectordb`、点号内部路径和 `temp/.aisync-temp.json`，避免自由区变成任意文件入口。
- 2026-05-19：新增正式 `LICENSE`：采用 PolyForm Noncommercial License 1.0.0；README 已链接协议并明确商用、付费分发、二次发布、转售、嵌入商业产品或托管服务均需书面授权，第三方依赖仍按各自许可证。
- 2026-05-19：规划 Agent 大改方向：不直接做自由 Agent 集群，先建立可见的 WorkflowRun/WorkflowStep；主 Agent 负责编排，步骤可选择 LLM 预设、挂载提示词包、设置人工确认点，输出优先进入 `temp/drafts`。
- 2026-05-19：确认提示词透明度问题：当前提示词来源分散在默认 system prompt、LLM 预设覆盖、会话记忆、向量上下文、伏笔上下文和工具描述中；规划新增提示词来源审计、提示词预览和提示词包管理。
- 2026-05-19：优化启动等待页：不做虚假进度条，改为轮换启动文案、显示已等待秒数，超过 8 秒提供复制诊断和打开日志入口。
- 2026-05-19：发布打包至 0.1.9：执行 `npm run build` 成功，生成 NSIS 安装包 `frontend/src-tauri/target/release/bundle/nsis/AiSync_0.1.9_x64-setup.exe`，release summary 确认当前前端 JS/CSS 已嵌入 release exe。
- 2026-05-19：完成提示词来源审计 I1：AgentRun 新增 `prompt_audit` 摘要级元数据，记录 system prompt 来源、记忆摘要/近期消息、向量上下文、伏笔上下文和可用工具数量；前端 Agent 任务区展示审计标签，但暂不暴露完整 prompt 正文。
- 2026-05-19：修正提示词方向：用户要的是“提示词管理”而不是单纯暴露运行 prompt；新增全局提示词包存储 `~/.aisync/prompt_packs.json`、`/api/prompt-packs` CRUD、设置页“提示词管理”分页，支持文风/写作/规划/润色/检查/特殊片段等分类的新增、编辑、复制、启停和删除。
- 2026-05-19：新增 `项目文档/提示词技术.md`：确定提示词系统三分法，Prompt Pack 管理模型行为规则，Context Pack 管理结构化上下文注入，Workflow Step 管理步骤级模型、提示词包、上下文包、输出位置和人工确认；明确不采用“人物/伏笔/大纲/世界观全塞进 prompt”的方案。
- 2026-05-20：Prompt Pack 接入主 Agent 对话：已启用且适用阶段包含 `chat` 的提示词包会自动作为长期行为规则注入，不需要每轮手动选择；AgentRun 审计记录本轮使用的提示词包数量和名称。
- 2026-05-20：Prompt Pack 接入章节工具：`write_chapter` 自动使用 `chapter_draft` 阶段提示词包，`edit_chapter` 自动使用 `revision` 阶段提示词包；ToolRun 结果元数据记录本次使用的提示词包阶段、数量和名称。新增 `backend/app/core/prompt_pack_rendering.py` 统一渲染/审计逻辑，并补充后端回归测试。
- 2026-05-20：完成项目级 Prompt Pack 默认启用关系：每个项目可保存 `.aisync/prompt_pack_settings.json`，在设置页选择“沿用全局启用”或“只使用本项目勾选项”；主 Agent 对话、写章节和编辑章节都会按项目设置过滤提示词包，防止不同小说项目串文风。
- 2026-05-20：完成 WorkflowRun/WorkflowStep 持久化骨架：新增后端 `WorkflowRunStore` 与 `/api/workflows`，项目内记录写入 `.aisync/workflows/{run_id}.json`；前端新增“工作流”侧栏页，可创建章节草稿流程模板并查看步骤状态。当前尚未自动执行步骤，下一步进入写章节工作流最小执行器。
- 2026-05-20：完成章节草稿工作流最小执行器：`/api/workflows/{run_id}/run-next` 可推进上下文检索、章节计划、用户确认和草稿生成；`/confirm` 可确认等待中的人工步骤；草稿输出到 `temp/drafts/{run_id}.md`。前端工作流页增加“运行下一步”和“确认并继续”。
- 2026-06-27：完成工作流步骤编辑基础版：前端工作流页每个步骤可编辑模型预设、输出路径、额外提示词和步骤输出；后端执行器会把 `step.input.extra_prompt` 注入章节计划/草稿 prompt；后端 step update 支持清空可选字段，避免 UI 清空后保存不生效。
- 2026-06-27：新增本地 Web 模式：FastAPI 可托管 `frontend/dist`，根目录新增 `npm run web/web:start/web:build`；前端同源 API/WebSocket 适配完成，浏览器模式可在侧边栏手动输入项目绝对路径；开发默认端口改为 `27631`，桌面安装版仍自动选端口。
- 2026-06-27：新增项目库导入/导出基础版：后端提供项目列表、新建、zip 导入、安全解压、zip 导出；前端侧边栏支持项目选择、新建、导入和导出，Web 模式不再依赖手填路径。明确项目库使用用户数据目录，不写入安装目录。
- 2026-06-27：补完项目库管理基础体验：支持项目重命名和删除；重命名只更新元数据，删除限定在项目库内，避免误删用户外部文件夹。
- 2026-06-27：新增首次启动项目引导：无当前项目时主界面显示新建、导入、打开外部文件夹和刷新项目库入口，Web 模式不再停在空工作区。
- 2026-07-04：Web-only 迁移第一轮：删除 Tauri 桌面壳目录、桌面打包脚本和 Tauri 前端依赖；根目录 `npm run build` 收敛为前端静态构建，`npm run dev` 收敛为局域网 Web 开发入口；README、技术细节、发布流程、更新待办和运行链路文档改为 Web-only 口径。

## 2026-06-27 Agent UI 更新

- Agent 任务面板默认折叠，减少对话区占用。
- memory/status/tool call 等运行过程信息改为集中进入“运行细节”折叠块。
- 同一条旧的未收敛运行提示点击忽略后会写入浏览器本地存储，刷新后不再反复弹出；新的失败或中断仍会正常提示。

## 2026-06-27 Agent 状态收敛

- 后端新增活跃 run 追踪：真实运行中的 Agent run 会登记在进程内活跃集合中。
- `latest run` 接口发现最近记录仍是 `running` 但当前进程没有对应活跃任务时，会自动标记为 `interrupted`，并同步收敛会话状态。
- 补充 Agent run store 测试，覆盖旧 running 自动收敛与真实活跃 run 不被误收敛。

## 2026-06-27 Agent 失败诊断与 token 统计

- 本次截图里的失败记录保存了空错误，前端只能显示“本轮 Agent 执行失败”；已改为异常文本为空时记录异常类名，避免黑箱失败。
- 发现同一用户输入在历史中重复出现，说明之前同一对话可并发/重复发送；已增加前后端保护，运行中禁发并在后端拒绝重复 Agent run。
- Agent 请求现在尊重 LLM 配置里的最大 token；此前未显式传入时会落到偏大的默认值，可能造成 token 消耗偏高。
- Agent run 报告新增模型请求数、工具调用数、真实 token/估算 token；任务面板标题显示摘要，详细工具事件和提示词审计默认折叠。

## 2026-06-27 Agent 运行策略与中断修复

- 对话区新增“一致性审查”开关；关闭后本轮不会把 `consistency_check` 暴露给主 Agent。
- 中断按钮现在会携带当前 conversation/run 信息，后端优先中断真实运行中的临时 Agent，而不是误打缓存 Agent。
- 后端测试新增运行中 Agent 中断命中覆盖。

## 2026-06-28 工具级 LLM 路由

- 工具中心设置的“工具默认方案”现在也会被主 Agent 自动工具调用读取。
- 当工具有独立默认 LLM 且支持 `invoke` 时，工具内部 AI 流程使用该模型；例如一致性审查可以设置为便宜模型。
- 没配置独立方案时保持旧行为，继续使用主 Agent 当前模型或直接执行工具。
- Agent 任务报告会显示工具调用的执行模式和使用的预设 id。

## 2026-06-28 对话历史定位修复

- 选择历史对话后，聊天区会自动跳到最新消息。
- 滚动定位增加下一帧兜底，减少历史内容渲染后又回到顶部的问题。

## 2026-06-28 一致性检查降噪

- 修复一致性检查过敏：Markdown 编号、纯数字和 `ai` 这类泛词不再触发“高相关设定”。
- 高相关复核现在需要真实中文设定词/实体重叠；角色名、地点名等仍会保留提示。

## 2026-06-28 角色归档/删除基础版

- 新增可恢复角色归档：角色从 `characters/{slug}` 移动到 `temp/archive/characters/{slug}-{timestamp}`。
- 新增 Agent 工具 `character_manage`，第一版只支持 archive，避免不可逆删除。
- 角色管理页增加归档入口，归档后刷新相关工作区和索引。

## 2026-06-28 正文写作入口决策

- 当前正文写作优先走主 Agent 直接对话或 `write_chapter` 工具，不强制用户进入工作流。
- 工作流分段写作继续保留在规划中，但只作为长章节输出不稳定、多模型分工或逐段人工确认时的备用方案。
- 已同步更新 `项目文档/Agent能力.md`，明确 Agent 可以直接写入正式章节，也可以通过工作流先输出到 `temp/drafts`。

## 2026-06-30 上下文窗口档位

- LLM 预设新增“上下文窗口”选项：经济、标准、长上下文、最高上下文。
- 该选项控制主 Agent 注入的同对话历史、向量检索片段数量和片段长度；最高上下文用于大窗口模型和重写/长章任务。
- 任务报告会显示本轮上下文档位、注入历史和 token 估算，方便判断 token 消耗来源。

## 2026-06-30 工作流基础能力与示例

- 工作流新增模板列表，当前内置“安全章节草稿”“草稿转正式章节”“章节修订草稿”。
- 新增 `write_file` 步骤执行器，支持草稿确认后从 `temp/drafts/*.md` 写入 `chapters/**/*.md`，并限制写入范围避免误伤设定文件。
- 提示词管理新增示例入口，当前可一键创建“半文半白叙事”“废土冷峻文风”“章节草稿约束”“设定一致性复核”。
- 已补测试覆盖提示词示例、工作流模板和草稿转正式章节写入。

## 2026-06-30 工作流与提示词二次修正

- 提示词示例改为默认可见卡片，不再藏在折叠区。
- 提示词示例改为先选择预览，再点击“从示例创建”才真正创建，避免误点和切换示例后创建错对象。
- 工作流补齐基础管理：可删除记录、创建空白流程、添加步骤、删除步骤。
- 工作流继续补基础恢复能力：可编辑标题/摘要、重试单步、跳过单步，失败后可从指定步骤继续推进。
- 系统提示词暂不混入普通提示词包；后续规划为单独的“系统规则/Agent 行为规则”管理层，和文风、写作、检查提示词分层。

## 2026-07-03 系统提示词管理分层

- 新增项目级系统规则配置：`.aisync/system_rules.json`。
- 新增 `/api/system-rules`，支持读取和保存当前项目系统规则。
- 设置页新增“系统规则”分页，和普通“提示词管理”分离；文风、写作、润色、检查仍由 Prompt Pack 管理。
- 主 Agent 创建时会把项目系统规则追加进 system prompt，并保留预设级 System Prompt 兼容。
- Agent run 审计新增 system prompt 来源、base source、项目规则是否注入和字符数，任务区显示“默认/预设+项目规则”等摘要。

## 2026-07-19 AGENT.md 单文件规则

- 项目长期规则从隐藏的 `.aisync/system_rules.json` 收敛为项目根目录 `AGENT.md`；旧自定义规则首次读取时自动迁移，旧文件仅保留为遗留备份。
- `AGENT.md` 同时维护当前项目固定使用的文风和 Agent 工作习惯，每轮对话自动读取并注入。
- Agent 可通过现有 `file_change_proposal` 提议修改 `AGENT.md`；默认展示差异并等待确认，开启自动应用时直接落盘。
- `AGENT.md` 不能改变工具权限、安全边界和程序级规则，也不应保存人物、世界观、大纲等小说事实或单次任务内容。
- Prompt Pack 继续用于需要跨项目复用、按阶段启用或随时切换的文风与任务提示词。

## 2026-07-03 工作流步骤级提示词包

- 工作流步骤编辑器新增提示词包勾选，步骤可指定本步使用哪些 Prompt Pack。
- 执行器规则：步骤未指定提示词包时沿用阶段默认；步骤指定后只注入所选提示词包。
- 计划步骤使用 `chapter_plan` 默认阶段，草稿步骤使用 `chapter_draft`，修订步骤使用 `revision`。
- 步骤输出会记录本次实际注入的提示词包 mode、stage、ids、names 和分类，前端步骤卡片可见。
- 轻量补充：步骤编辑器新增“套用推荐/清空”，按步骤类型推荐对应阶段和分类的 Prompt Pack；执行后显示模式、阶段、数量、额外要求和 prompt 字符数，不展示完整 prompt 正文。

## 2026-07-04 章节与世界设定 UI 轻改

- 章节管理页先做低风险前端重整：新增章节概览条、章节索引头、状态标签、详情指标和正文阅读区边界。
- 章节列表现在更突出标题、状态、字数、修订轮次和大纲关联，减少“文件路径列表”的观感。
- 世界观整理页新增设定概览条、设定资料库头、文档分类标签、详情指标和 Markdown 阅读区边界。
- 本轮只改前端信息层级和样式，不改后端数据结构、不牵连工具执行逻辑。
- 继续补低风险筛选：章节管理支持按状态筛选，世界观整理支持按设定类型筛选，筛选都在前端基于现有数据完成。
- 继续补本地排序：章节列表支持章节顺序、字数、状态、修订轮次排序；世界观列表支持路径、类型、字数、标题排序。最近打开暂不做。

## 2026-07-04 Agent 中断与一致性审查默认策略

- 对话区“一致性审查”现在默认关闭，默认不会把 `consistency_check` 暴露给本轮主 Agent；需要复核设定时再手动打开。
- 中断链路补强：前端中断会携带 conversation/run 信息，并立即把当前任务显示为“已请求中断”，减少卡在“请求模型”的假死感。
- 后端为当前 Agent run 登记可取消任务；REST/WebSocket 中断会优先命中真实运行中的 agent/task，并尝试 `task.cancel()`。
- 中断请求会立即把 AgentRun/Conversation 的持久状态收敛为 interrupted，并广播 run_event 给前端任务面板。
- 已补测试覆盖 conversation 定位 run、取消当前任务、持久状态改为 interrupted。
- LLM 请求新增统一超时配置：预设里可设置请求超时秒数，默认 120 秒；非流式请求按总时长超时，主 Agent 流式请求按空闲超时收口。
- AgentRun usage 审计会记录模型错误分类和详情：timeout、rate_limit、auth、bad_request、network、llm_error。
- 前端任务面板显示请求超时、错误类型和错误详情；失败仍保持手动重试，不做自动重发，避免烧 token。
- Agent 任务区新增轻量恢复入口：失败/中断后给出处理建议，支持重试本轮、复制错误、标记已处理。

## 2026-07-04 局域网开发启动

- 新增根目录命令 `npm run dev:lan`，用于在可信局域网内临时从手机或其他电脑访问开发版。
- `scripts/dev_lan.ps1` 会同时启动后端和 Vite 前端，绑定到 `0.0.0.0`，并打印局域网访问地址。
- Vite 开发服务增加 `/api` 代理，局域网设备访问 `http://开发机IP:1420` 时，API 和 WebSocket 会正确转发到开发机后端。
- 当前 LAN dev 没有登录/鉴权，只适合可信网络临时使用。

## 2026-07-04 手机端基础适配

本轮完成局域网手机访问的第一版低风险适配：应用外壳在小屏变为单列，侧边栏压缩为顶部导航区，对话历史、文件树和聊天任务面板都增加了小屏高度与换行约束。当前目标是让手机端能正常试用主要功能，不是完整重做移动端交互。

验证：`npm run build:frontend` 已通过；构建中仍有既有 Tauri 动态导入提示，和本轮 CSS 适配无关。

二次修正：手机端侧栏不再摊开全部项目操作，只保留项目名、刷新和项目下拉；主导航/工具导航改成紧凑横向按钮组，取消侧栏内部大滚动条，主内容区负责滚动。

三次重做：手机端不再复用桌面侧栏压缩样式，改为独立移动端应用壳：顶部显示品牌、Agent 状态、项目名和项目切换；工具入口在顶部横向滚动；主模块切换放到底部固定导航。桌面侧栏保持原结构。

聊天页修复：底部主导航改为应用壳第三行，不再悬浮覆盖内容；主内容区不抢滚动，聊天消息列表独立滚动，输入框固定在聊天面板底部。

空间回收：手机端隐藏顶部工具横条，压缩移动顶栏、聊天 header、任务摘要和输入区；失败详情与恢复建议限制高度并允许内部滚动，避免任务面板挤掉消息区。

## 2026-07-04 Agent 超时与流式稳定性

- 根因：主 Agent 虽然请求流式输出，但旧实现对整次 LLM 调用套总时长超时；长回复持续输出时也可能被 120 秒硬切。
- 已改为流式空闲超时：只要正文、reasoning 或工具参数增量还在到达，就不会被总时长误杀；长时间完全无进展才判定超时。
- DeepSeek thinking + 工具调用路径改为保流式：带工具的流式请求临时关闭 thinking，不再降成非流式，减少等待黑箱和中断无效。
- 当前仍存在非流式请求：记忆压缩、一致性检查、工作流步骤，以及没有前端 delta 回调的内部调用。
- Agent 任务报告新增请求模式和 token 估算：空闲/总超时、是否流式、消息数、工具数、本轮输入估算 token，方便判断卡顿和 token 消耗来源。
- 验证：后端目标测试 15 条通过，前端 `npm run build:frontend` 通过。

## 2026-07-05 Agent 超时个案排查

- 排查 run `5409b48c004a4b30995d43b9d1c6d517`：第一次模型请求成功返回 `search_project` 工具调用，工具 251ms 完成；失败发生在工具结果回填后的第二轮模型请求，120 秒没有返回内容后超时。
- 修正诊断统计：AgentRun usage 新增 `model_request_attempts`，前端任务报告区分“请求尝试”和“成功返回”，避免失败请求未计入 `model_requests` 导致误判。
- 已撤回“纯问候跳过历史记忆、向量上下文和工具列表”的处理：即使是 `你好/在吗/测试`，仍按同一对话上下文正常注入，避免切断连续对话能力。
- 验证：`pytest backend/tests/test_agent_prompt_packs.py -q`、`npm run build`、`compileall backend/app` 通过。

## 2026-08-01 Web-first 发布基线

- 发布版本统一为 `v0.2.0`，根目录、前端和后端版本使用同一编号。
- 移除后端 PyInstaller 发布依赖，不恢复 Tauri、NSIS、桌面安装器或内置 Python runtime。
- 增加预构建 Web ZIP：开发者构建时需要 npm，下载 Release 的使用者只需要 Python 3.11。
- 发布 ZIP 内含后端源码、构建后的前端、首次安装脚本、启动脚本、许可证、版本文件和变更记录。
- 增加发布版本一致性检查、SHA256 校验文件和 GitHub `v*` tag 自动发布工作流。
- 当前限制：首次启动需要联网安装 Python 依赖；局域网模式没有鉴权，不应直接暴露到公网。
