# 工具系统描述

## 一、目标

AiSync 的工具系统不应只是零散的后端函数，而应成为一组完整的“创作能力模块”。

每个工具都可以同时具备：

- 后端执行逻辑
- 前端交互界面
- Agent 调用能力
- 默认 Agent 配置
- 输入参数 schema
- 结果呈现方式
- 项目文件读写声明
- 执行记录与进度追踪

最终目标是让工具既能被主 Agent 在自然语言对话中自动调用，也能由用户在前端工具界面中手动使用。

---

## 二、核心理念

工具是一个完整的创作工作单元，而不是单纯 API。

一个工具应该回答这些问题：

1. 它解决什么创作问题？
2. 它需要用户输入什么？
3. 它会读取哪些项目资料？
4. 它可能修改或生成哪些文件？
5. 它默认交给哪个 Agent 执行？
6. 它的结果如何在前端展示？
7. 它执行后如何留下可追踪记录？

例如“大纲生成/续写工具”不只是返回一段文本，而应该：

- 读取世界观、角色、已有章节和已有大纲
- 调用默认的大纲 Agent
- 生成结构化大纲数据
- 写入 `plot/outline.md` 或未来的结构化大纲文件
- 在前端大纲管理页面中刷新展示
- 在 `.aisync/tool_runs/` 中留下执行记录

---

## 三、两种调用方式

### 1. 主 Agent 自动调用

用户通过自然语言提出需求，例如：

> 帮我整理前三章的大纲，并找出主线断点。

主 Agent 判断是否需要调用工具。

流程：

```text
用户自然语言
  ↓
主 Agent 判断意图
  ↓
选择合适工具
  ↓
使用工具配置的默认 Agent / 提示词
  ↓
工具读取项目资料
  ↓
工具生成结果并修改文件
  ↓
主 Agent 总结结果给用户
```

这种方式适合用户不想填写表单，只想用自然语言完成创作任务的场景。

重点：

- 主 Agent 负责判断是否需要工具。
- 工具负责专业执行。
- 工具执行前应能说明可能影响哪些文件。
- 工具结果应写入项目文件，而不只是返回聊天文本。

---

### 2. 用户手动使用工具界面

用户也可以在前端侧栏或工具中心里打开具体工具。

例如：

```text
创作工具
  - AI生成/续写大纲
  - 章节续写
  - 角色生成
  - 组织生成
  - 世界观补全
  - 剧情节奏分析
  - 伏笔分析
  - 文风统一
```

用户点击工具后，前端显示表单：

```text
目标章节数：
当前故事阶段：
希望补全内容：
是否覆盖已有大纲：
输出位置：
```

提交后：

```text
前端表单
  ↓
执行 API
  ↓
后端 Tool Runtime
  ↓
Agent 模块生成内容
  ↓
工具写入项目文件
  ↓
前端刷新结果页面
```

这种方式适合结构化任务，例如大纲生成、角色创建、组织管理、世界观补全等。

---

## 四、工具的组成

一个工具建议由以下部分组成：

```text
Tool Definition
  ├─ 基础信息
  ├─ 输入 schema
  ├─ 默认 Agent
  ├─ 文件影响声明
  ├─ 执行逻辑
  ├─ 可选前端 UI
  ├─ 结果呈现声明
  └─ 运行记录
```

### 1. 基础信息

包括：

- 工具 ID
- 工具名称
- 工具描述
- 工具分类
- 是否支持前端 UI
- 是否允许主 Agent 自动调用

示例：

```json
{
  "id": "outline.generate",
  "name": "AI生成/续写大纲",
  "category": "story_structure",
  "description": "根据当前项目资料生成或续写故事大纲"
}
```

---

### 2. 输入 schema

工具应使用 JSON Schema 描述输入参数。

这样同一份 schema 可以同时用于：

- LLM tool calling
- 前端自动生成表单
- 后端参数校验

示例：

```json
{
  "type": "object",
  "properties": {
    "target_chapter_count": {
      "type": "number",
      "title": "目标章节数"
    },
    "mode": {
      "type": "string",
      "enum": ["create", "extend", "rewrite"]
    },
    "requirements": {
      "type": "string",
      "title": "补充要求"
    }
  },
  "required": ["mode"]
}
```

---

### 3. 默认 Agent

每个工具可以设置一个默认 Agent。

例如：

```text
outline.generate      → outline_agent
create_character      → character_agent
update_worldview      → worldbuilding_agent
plot_analysis         → analysis_agent
write_chapter         → chapter_writer_agent
edit_chapter          → chapter_editor_agent
```

主 Agent 不需要亲自完成所有专业任务，而是可以把任务交给工具对应的专业 Agent。

第一阶段可以先只保留 `default_agent` 字段，不立即实现复杂多 Agent 调度。

---

### 4. 文件影响声明

工具必须提前声明可能影响的文件。

包括：

- 可能读取的文件
- 可能修改的文件
- 可能生成的文件

示例：

```json
{
  "read": [
    "world/**/*.md",
    "characters/**/*.yaml",
    "chapters/**/*.md"
  ],
  "write": [
    "plot/outline.md"
  ],
  "generate": [
    ".aisync/analysis/outline_report.json"
  ]
}
```

前端执行前应展示：

```text
此工具可能读取：
- world/**/*.md
- characters/**/*.yaml
- chapters/**/*.md

此工具可能修改：
- plot/outline.md

此工具可能生成：
- .aisync/analysis/outline_report.json
```

这样用户能提前知道工具的影响范围。

---

## 五、后端 API 设计

后端 API 建议分为两类：

1. 执行 API
2. 呈现 API

---

### 1. 执行 API

执行 API 负责启动工具、调用 Agent、写入文件、记录运行状态。

建议端点：

```http
GET /api/tools
GET /api/tools/{tool_id}
POST /api/tools/{tool_id}/execute
POST /api/tools/{tool_id}/invoke
GET /api/tools/runs
GET /api/tools/runs/{run_id}
```

含义：

- `GET /api/tools`：返回工具列表和描述符
- `GET /api/tools/{tool_id}`：返回单个工具详情
- `POST /api/tools/{tool_id}/execute`：用户手动执行工具
- `POST /api/tools/{tool_id}/invoke`：交给 Agent 生成并执行
- `GET /api/tools/runs`：查看最近工具运行记录
- `GET /api/tools/runs/{run_id}`：查看单次工具运行详情

---

### 2. 呈现 API

呈现 API 不负责执行 AI，只负责读取已经存在的项目数据并转换成前端需要的结构。

示例：

```http
GET /api/story/outline
GET /api/story/characters
GET /api/story/organizations
GET /api/story/worldview
GET /api/analysis/plot
GET /api/analysis/foreshadowing
```

原则：

```text
执行 API = 产生结果
呈现 API = 展示结果
```

前端打开“大纲管理”页面时，不应该自动触发 AI。

它应该只是读取已有大纲数据。

只有用户点击“AI生成/续写大纲”时，才调用执行 API。

---

## 六、运行记录 Tool Run

每次工具执行都应该生成运行记录。

建议存储位置：

```text
.aisync/tool_runs/{run_id}.json
```

记录内容：

```json
{
  "run_id": "toolrun_xxx",
  "tool_name": "outline.generate",
  "mode": "execute",
  "status": "completed",
  "started_at": "2026-05-03T16:00:00Z",
  "finished_at": "2026-05-03T16:01:20Z",
  "file_access": {
    "read": ["world/**/*.md", "characters/**/*.yaml"],
    "write": ["plot/outline.md"],
    "generate": []
  },
  "params": {},
  "result": {},
  "error": null
}
```

后续可以扩展状态：

```text
queued
running
waiting_user_input
writing_files
completed
failed
cancelled
```

第一阶段可以先同步执行，只记录 `completed` 和 `failed`。

---

## 七、前端工具界面

前端工具体系建议分成两层：

1. 工具中心
2. 专业管理页面

---

### 1. 工具中心

工具中心列出所有可用工具。

每个工具卡片展示：

- 工具名称
- 工具说明
- 是否有前端表单
- 默认 Agent
- 呈现类型
- 文件影响摘要

例如：

```text
AI生成/续写大纲
根据当前项目资料生成或续写故事大纲
前端表单 / outline_agent / tree:outline
读 4 / 改 1 / 生成 1
```

点击后打开 ToolDrawer。

---

### 2. ToolDrawer

ToolDrawer 是工具的通用执行界面。

它应该包含：

- 工具说明
- 默认 Agent
- 文件影响声明
- 参数表单
- “直接执行”按钮
- “AI 生成”按钮
- 执行结果
- 错误信息

对于简单工具，可以用 SchemaForm 自动生成表单。

对于复杂工具，可以绑定专门前端组件。

---

### 3. 专业管理页面

截图中展示的大纲管理、组织管理等页面，属于专业管理页面。

它们不只是工具表单，而是项目资料的可视化编辑器。

示例：

```text
世界设定
角色管理
组织管理
职业管理
关系管理
大纲管理
章节管理
伏笔管理
```

这些页面的特点：

- 展示结构化项目资料
- 支持搜索、分页、编辑、删除
- 支持局部刷新
- 页面上可以放 AI 工具按钮

例如大纲管理页面：

```text
[搜索大纲] [手动创建] [AI生成/续写大纲]

第1章 苏醒
  大纲内容...

第2章 坠入深渊
  大纲内容...
```

点击 AI 按钮时才调用对应工具。

---

## 八、自由 UI 与 SchemaForm 的取舍

建议采用混合方案。

### 1. 默认使用 SchemaForm

适合简单工具，例如：

- 搜索项目
- 写章节
- 编辑章节
- 创建角色
- 更新世界观
- 简单剧情分析

优点：

- 新增工具成本低
- 后端 schema 可以直接驱动前端
- 快速形成可用闭环

缺点：

- UI 不够精致
- 复杂交互难做

---

### 2. 复杂工具使用自定义组件

适合复杂页面，例如：

- 大纲管理
- 角色管理
- 组织管理
- 关系图
- 时间线
- 章节编辑器

工具描述符可以提供：

```json
{
  "ui_component": "OutlineManager"
}
```

如果存在 `ui_component`，前端加载专门组件。

如果没有，则回退到 SchemaForm。

---

## 九、结果应尽量结构化

工具结果不应只是一段聊天文本。

对于创作管理类工具，结果应该尽量结构化，方便前端展示和后续编辑。

例如大纲工具不应只返回：

```text
第1章主角苏醒，第2章遇到敌人...
```

而应该生成：

```json
{
  "chapters": [
    {
      "index": 1,
      "title": "苏醒",
      "summary": "...",
      "pov": "苏沐",
      "characters": ["苏沐"],
      "locations": ["零号陨坑地表"],
      "conflicts": ["生存危机", "身份谜团"]
    }
  ]
}
```

这样前端才能支持：

- 搜索
- 分页
- 折叠
- 编辑
- 删除
- 统计
- 可视化展示

---

## 十、推荐项目数据结构

长期建议逐步形成稳定的项目数据目录。

示例：

```text
project/
  .aisync/
    project.json
    conversations/
    tool_runs/
    analysis/
      plot_report.json
      foreshadowing_report.json
      consistency_report.json

  world/
    overview.md
    magic-system.md
    geography.md
    history.md
    rules.yaml

  characters/
    index.yaml
    relationships.json
    protagonist/
      profile.md
      profile.yaml

  plot/
    outline.md
    timeline.json
    arcs/

  chapters/
    vol-01/
      ch-001.md
      ch-002.md
```

如果后续需要更接近截图中的管理界面，可以逐步增加结构化 JSON/YAML 文件，例如：

```text
story/
  outline.json
  characters.json
  organizations.json
  occupations.json
  relationships.json
```

但第一阶段不必强制迁移，先兼容当前 `world/`、`characters/`、`plot/`、`chapters/` 结构。

---

## 十一、示例：AI生成/续写大纲

完整流程：

```text
1. 用户打开“大纲管理”页面

2. 页面通过呈现 API 读取已有大纲
   GET /api/story/outline

3. 用户点击“AI生成/续写大纲”

4. 前端打开工具表单

5. 用户填写目标、范围、要求

6. 前端调用执行 API
   POST /api/tools/outline.generate/execute

7. 后端创建 tool_run
   .aisync/tool_runs/toolrun_xxx.json

8. 工具读取项目资料
   - world/**/*.md
   - characters/**/*.yaml
   - plot/outline.md
   - chapters/**/*.md

9. 工具调用默认 Agent
   outline_agent

10. Agent 生成结构化大纲

11. 工具写入大纲文件
   plot/outline.md 或 outline.json

12. 前端收到完成结果

13. 大纲管理页面刷新呈现 API
```

---

## 十二、阶段规划

### 第一阶段：工具协议闭环

目标：让现有工具变成统一的能力模块。

包括：

- Tool Descriptor
- 文件影响声明
- 默认 Agent 字段
- Tool Run 记录
- 工具中心展示
- ToolDrawer 执行前说明
- 基础结果渲染

这一阶段不追求复杂 UI，而是把工具底座打通。

---

### 第二阶段：专业工具页面

目标：围绕小说创作对象建立可视化管理页。

优先级：

1. 大纲管理
2. 角色管理
3. 世界设定
4. 组织管理
5. 关系管理
6. 伏笔管理
7. 剧情分析

这些页面应调用呈现 API 读取结果，用 AI 按钮触发执行 API。

---

### 第三阶段：长任务与进度追踪

目标：让 AI 工具执行过程可见、可取消、可恢复。

包括：

- 后台任务队列
- WebSocket/SSE 进度事件
- 取消任务
- 重试任务
- 失败恢复
- 运行历史详情页

---

### 第四阶段：多 Agent 工具协作

目标：让不同工具拥有专业 Agent，并支持更复杂的创作流水线。

例如：

```text
主 Agent
  ↓
大纲 Agent
  ↓
角色 Agent
  ↓
一致性分析 Agent
  ↓
写作 Agent
```

主 Agent 负责调度，专业 Agent 负责具体创作能力。

---

## 十三、最终效果

理想中的 AiSync 工具系统应该是：

> 一个 Agent 驱动的创作工作台。用户既可以通过自然语言让主 Agent 自动调用工具，也可以进入具体工具界面手动操作；所有工具都通过统一后端运行时执行、追踪进度、写入项目文件，并通过专门的呈现 API 在前端形成类似专业小说管理软件的结构化界面。

最终用户体验应接近：

- 左侧是创作管理与创作工具导航
- 中间是大纲、角色、组织、章节等专业管理页面
- 页面上有手动编辑按钮和 AI 生成按钮
- AI 工具执行前明确告知文件影响
- 执行中显示进度
- 执行后自动刷新结构化结果
- 所有执行都有历史记录可追踪
